import type { Round } from "@/data/curriculum";

// ---------------------------------------------------------------------------
// JUNIOR PYTHON COURSE — 7 раундов
//
// Цель — провести с нуля до уверенного джуниора по Python (без ООП).
// После этого курса логично переходить в Middle. Структура каждой методички
// выдержана так, чтобы прочитав её один раз, заданий раунда было достаточно
// для уверенного решения. Все термины объясняются прямо там, где впервые появляются.
// ---------------------------------------------------------------------------

const j1: Round = {
  number: 1,
  title: "Junior · Переменные, типы данных, ввод/вывод",
  level: "Начальный",
  intro:
    "Отправная точка: переменная — это имя, связанное с объектом в памяти. Разбираем основные типы (int, float, str, bool, None), как их создавать, как печатать значения и как считывать ввод с клавиатуры.",
  lesson: {
    title: "Переменные, типы данных, ввод и вывод",
    summary:
      "Создание переменных, определение и преобразование типов (int↔str↔float), форматирование вывода через f-строки, считывание ввода через input(). Это фундамент, на котором строится весь последующий Python-код.",
    readingMinutes: 5,
    sections: [
      {
        heading: "Переменная — это имя, а не хранилище",
        tagline: "Имя ссылается на объект; объект живёт сам по себе",
        body:
          "**Переменная** в Python — это имя, которое связывается с объектом в памяти. Оператор `=` — это присваивание, а не равенство:\n\n" +
          "`name = \"Аня\"` означает: «создай в памяти строковый объект \"Аня\" и свяжи с ним имя name».\n\n" +
          "Тип задаётся не именем, а значением — Python определяет его автоматически. Узнать тип можно функцией `type(x)`. Это называется **динамическая типизация**: одно и то же имя в разных местах программы может ссылаться на объекты разных типов.",
        code:
          "name = \"Аня\"        # str — строка\n" +
          "age = 17             # int — целое число\n" +
          "height = 1.72        # float — число с плавающей точкой\n" +
          "is_student = True    # bool — логический тип (True / False)\n" +
          "nothing = None       # None — отсутствие значения\n\n" +
          "print(type(age))     # <class 'int'>\n" +
          "print(type(height))  # <class 'float'>",
        keyTakeaways: [
          "`=` — это присваивание, а не сравнение. Сравнение пишется `==`.",
          "Имя переменной — на латинице, в snake_case: `user_name`, не `userName`.",
          "Тип хранится у объекта, а не у имени. `type(x)` показывает текущий тип.",
        ],
        pitfalls: [
          "Нельзя начинать имя с цифры: `1name = ...` — ошибка.",
          "Регистр важен: `Name` и `name` — разные переменные.",
          "Зарезервированные слова (`for`, `if`, `class`, `True` и др.) использовать как имена нельзя.",
        ],
      },
      {
        heading: "Преобразование типов и ввод-вывод",
        tagline: "input() всегда возвращает строку",
        body:
          "Для явного преобразования типов используются встроенные конструкторы: `int(\"5\")`, `float(\"3.14\")`, `str(42)`, `bool(0)`. Это и называется **приведением типов** (type casting).\n\n" +
          "Принципиально важно: функция `input()` всегда возвращает **строку**, независимо от того, что ввёл пользователь. Для арифметических операций результат необходимо явно преобразовать через `int(...)` или `float(...)`.\n\n" +
          "Вывод в консоль — `print(...)`. Наиболее удобный способ форматирования строки — **f-строка** (`f\"...\"`): внутри `{...}` можно вставить любое выражение Python.",
        code:
          "raw = input(\"Возраст: \")     # raw — строка, например \"17\"\n" +
          "age = int(raw)               # теперь это число 17\n" +
          "print(f\"Через год тебе будет {age + 1}\")\n\n" +
          "# Можно сразу:\n" +
          "x = int(input(\"x: \"))\n" +
          "y = int(input(\"y: \"))\n" +
          "print(f\"Сумма: {x + y}\")",
        keyTakeaways: [
          "`input()` → всегда строка. Хочешь число — приведи: `int(input(...))`.",
          "f-строка `f\"привет, {name}!\"` — главный способ форматирования.",
          "`int(\"abc\")` упадёт с ошибкой ValueError — приводи только то, что выглядит как число.",
        ],
        pitfalls: [
          "`\"5\" + 3` — TypeError: нельзя складывать str и int напрямую.",
          "`int(\"3.14\")` — ошибка. Сначала `float`, потом, если нужно, `int`.",
          "Деление `/` всегда даёт float (3 / 2 → 1.5). Целочисленное — `//` (3 // 2 → 1).",
        ],
      },
      {
        heading: "Полезные методы строк",
        tagline: "Строка — объект с 40+ встроенными методами",
        body:
          "В Python строки — это объекты, у которых есть десятки методов. Самые нужные:\n\n" +
          "- `.upper()` / `.lower()` — привести к верхнему/нижнему регистру.\n" +
          "- `.strip()` / `.lstrip()` / `.rstrip()` — убрать пробелы и переносы строк по краям.\n" +
          "- `.replace(old, new)` — заменить все вхождения подстроки.\n" +
          "- `.split(sep)` — разбить строку по разделителю → список строк.\n" +
          "- `sep.join(lst)` — склеить список строк через разделитель.\n" +
          "- `.startswith(s)` / `.endswith(s)` — проверить начало/конец.\n" +
          "- `.find(s)` — найти индекс первого вхождения, −1 если не нашлось.\n" +
          "- `len(s)` — длина строки (это встроенная функция, не метод).\n\n" +
          "Важно: все методы строк **возвращают новую строку** — они не меняют оригинал, поскольку строки неизменяемы.",
        code:
          "raw = \"  Привет, Python!  \"\n" +
          "clean = raw.strip()              # 'Привет, Python!'\n" +
          "upper = clean.upper()            # 'ПРИВЕТ, PYTHON!'\n" +
          "replaced = clean.replace(\",\", \" -\")  # 'Привет - Python!'\n\n" +
          "# split и join — пара инструментов для разбора строк\n" +
          "csv_line = \"Аня,17,Киев\"\n" +
          "parts = csv_line.split(\",\")      # ['Аня', '17', 'Киев']\n" +
          "rejoined = \" | \".join(parts)     # 'Аня | 17 | Киев'\n\n" +
          "# Проверки\n" +
          "path = \"/home/user/data.csv\"\n" +
          "print(path.endswith(\".csv\"))     # True\n" +
          "print(path.startswith(\"/home\"))  # True\n" +
          "print(path.find(\"user\"))         # 6",
        keyTakeaways: [
          "Строки неизменяемы — каждый метод возвращает новую строку.",
          "`.strip()` — часто первое, что делают с пользовательским вводом.",
          "`.split()` и `.join()` — главная пара для работы с CSV/текстом.",
        ],
        pitfalls: [
          "`s.upper()` не меняет `s`. Надо `s = s.upper()` или использовать результат.",
          "`.find()` возвращает −1 при неудаче — не 0 и не None. Проверяй на `!= -1`.",
          "`.split()` без аргументов разбивает по любым пробелам и убирает пустые части.",
        ],
      },
      {
        heading: "Арифметика и операторы",
        tagline: "// для деления без остатка, ** для степени, % для остатка",
        body:
          "Python поддерживает стандартные арифметические операторы: `+`, `-`, `*`, `/`. Плюс три специальных:\n\n" +
          "- `//` — целочисленное деление (floor division): `7 // 2 = 3`. Всегда округляет **вниз**.\n" +
          "- `%` — остаток от деления (modulo): `7 % 2 = 1`. Незаменим для проверки чётности: `n % 2 == 0`.\n" +
          "- `**` — возведение в степень: `2 ** 10 = 1024`.\n\n" +
          "Порядок операций стандартный (PEMDAS/BODMAS), но лучше ставить скобки там, где может быть неоднозначно.\n\n" +
          "Сокращённые операторы присваивания: `x += 1` то же самое что `x = x + 1`. Аналогично `*=`, `-=`, `//=`, `**=`, `%=`.\n\n" +
          "Функции `abs(x)` — модуль, `round(x, n)` — округление до n знаков, `max(a, b)` / `min(a, b)` — максимум/минимум.",
        code:
          "# Основные операторы\n" +
          "print(10 / 3)    # 3.3333...  — обычное деление, float\n" +
          "print(10 // 3)   # 3          — целое, остаток отброшен\n" +
          "print(10 % 3)    # 1          — остаток от деления\n" +
          "print(2 ** 8)    # 256        — степень\n\n" +
          "# Сокращённые операторы\n" +
          "count = 0\n" +
          "count += 1       # теперь 1\n" +
          "count *= 5       # теперь 5\n\n" +
          "# Математические функции\n" +
          "print(abs(-42))           # 42\n" +
          "print(round(3.14159, 2))  # 3.14\n" +
          "print(max(10, 7, 99, 3))  # 99\n\n" +
          "# Чётность и кратность\n" +
          "n = 17\n" +
          "print(n % 2 == 0)   # False — нечётное\n" +
          "print(n % 5 == 0)   # False — не кратно 5",
        keyTakeaways: [
          "`/` → всегда float. `//` → всегда int (если оба аргумента int).",
          "`%` — остаток от деления. Для чётности: `n % 2 == 0`.",
          "`x += 1` эквивалентно `x = x + 1` — удобно для счётчиков.",
        ],
        pitfalls: [
          "`7 // 2` = 3, но `-7 // 2` = −4 (округляет вниз, не к нулю).",
          "`10 / 2` даёт `5.0` (float), не `5` (int) — это часто сбивает в сравнениях.",
          "`2 ^ 8` в Python — это побитовый XOR, а не степень! Степень — это `2 ** 8`.",
        ],
      },
    ],
    cheatSheet: [
      "Типы: int, float, str, bool, None.",
      "`type(x)` — узнать тип; `int(x)`, `str(x)`, `float(x)` — привести.",
      "`input()` всегда возвращает строку.",
      "f-строки: `f\"{var}\"`, `f\"{x:.2f}\"` — два знака после точки.",
      "`/` — обычное деление (float), `//` — целочисленное, `%` — остаток, `**` — степень.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j1-f1",
      title: "Переменные и типы",
      description:
        "Создай переменные правильных типов и распечатай их через f-строку.",
      code:
        "name = {{0}}\n" +
        "age = {{1}}\n" +
        "height = {{2}}\n" +
        "is_student = {{3}}\n\n" +
        "print(f\"{name}, {age} лет, рост {height} м, ученик: {is_student}\")\n" +
        "print(type(age))   # ожидаем <class 'int'>",
      answers: [
        ['"Аня"', "'Аня'", "__str__"],
        ["17", "__int__"],
        ["1.72", "__float__"],
        ["True"],
      ],
      hints: [
        "Имя — строка, в кавычках.",
        "Возраст — целое число без кавычек.",
        "Рост — число с точкой.",
        "Логический тип: True или False (с большой буквы).",
      ],
      explanation: {
        summary:
          "Здесь ты выбираешь правильный литерал для каждого типа: строка в кавычках, число без кавычек, дробное число через точку, булево с большой буквы.",
        keyPoints: [
          "Строка — `\"...\"` или `'...'`.",
          "Целое — без точки, без кавычек.",
          "Float — с точкой: `1.72`, `0.5`, `2.0`.",
          "Bool — ровно `True` или `False`, с большой буквы.",
        ],
      },
    },
    {
      type: "fill",
      id: "j1-f2",
      title: "input() и приведение типов",
      description:
        "Считай число с клавиатуры и подсчитай его квадрат. Обрати внимание: input возвращает строку.",
      code:
        "raw = {{0}}(\"Введи число: \")\n" +
        "n = {{1}}(raw)\n" +
        "print(f\"Квадрат числа {n} равен {n {{2}} 2}\")",
      answers: [["input"], ["int"], ["**"]],
      hints: [
        "Функция считывания строки с клавиатуры.",
        "Преобразование строки в целое число.",
        "Оператор возведения в степень в Python.",
      ],
      explanation: {
        summary:
          "input всегда даёт строку, поэтому для арифметики надо привести к числу через int(...). Возведение в степень — `**`, не `^`.",
        keyPoints: [
          "`input()` → str.",
          "`int(...)` → строку в число.",
          "`x ** 2` — квадрат; `x ** 0.5` — корень.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j1-q1",
      title: "Что вернёт type(input())?",
      question:
        "Какой тип у значения, которое возвращает функция input() в Python?",
      answers: ["str", "строка", "string"],
      hint: "input всегда отдаёт один и тот же тип, даже если ты ввёл цифры.",
      explanation:
        "input() всегда возвращает строку (str). Если нужно число — нужно явно вызвать int() или float() поверх результата.",
    },
    {
      type: "question",
      id: "j1-q2",
      title: "Целочисленное деление",
      question:
        "Какой оператор в Python даёт результат целочисленного деления (отбрасывая дробную часть)?",
      answers: ["//"],
      hint: "Это два одинаковых символа подряд. Просто / даёт float.",
      explanation:
        "`//` — это целочисленное деление (floor division). Например, 7 // 2 = 3. Обычное деление 7 / 2 = 3.5.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j1-w1",
      title: "Калькулятор: сумма и среднее",
      task:
        "Напиши программу, которая считывает с клавиатуры два числа (могут быть дробными), и печатает их сумму и среднее арифметическое в виде:\n\nСумма: <s>\nСреднее: <m>\n\nИспользуй input(), float(...) и f-строки.",
      hints: [
        "Считывай через input(), сразу оборачивай в float(...).",
        "Среднее — это сумма, делённая на 2.",
        "Печатай через f-строки: `print(f\"Сумма: {s}\")`.",
      ],
      required: ["input(", "float(", "print(", "f\"", "/"],
      minLines: 4,
      explanation: {
        summary:
          "Показываешь, что понимаешь связку «считал → привёл к числу → вычислил → красиво напечатал».",
        keyPoints: [
          "input() возвращает строку — float() конвертирует в число.",
          "f-строка собирает результат коротко и читаемо.",
          "/ — деление с дробной частью, для среднего самое то.",
        ],
      },
    },
  ],
};

const j2: Round = {
  number: 2,
  title: "Junior · Условия: if / elif / else",
  level: "Начальный",
  intro:
    "Ветвление — основа любой нетривиальной логики. Разбираем if, elif, else, операторы сравнения (==, !=, <, >, <=, >=) и логические and / or / not, а также понятие истинности произвольного значения в Python.",
  lesson: {
    title: "Условия и логические операторы",
    summary:
      "Условные конструкции реализуют логику «если — то — иначе». Рассматриваем идиоматичное написание if/elif/else, использование нескольких ветвей, а также концепцию истинности произвольного значения (truthy/falsy).",
    readingMinutes: 5,
    sections: [
      {
        heading: "Синтаксис if / elif / else",
        tagline: "Отступы — часть синтаксиса, а не украшение",
        body:
          "**if** — «если». **elif** — «иначе если» (можно сколько угодно раз). **else** — «иначе» (один, в конце, и опционально).\n\n" +
          "В Python **отступы важны**: тело условия — это всё, что отступлено от `if`. Стандарт — 4 пробела. Не смешивай табы и пробелы.\n\n" +
          "Операторы сравнения: `==` равно, `!=` не равно, `<`, `>`, `<=`, `>=`. Их можно цеплять: `0 <= x <= 100` — это ровно то же, что `0 <= x and x <= 100`.",
        code:
          "score = int(input(\"Оценка (0–100): \"))\n\n" +
          "if score >= 90:\n" +
          "    grade = \"A\"\n" +
          "elif score >= 75:\n" +
          "    grade = \"B\"\n" +
          "elif score >= 60:\n" +
          "    grade = \"C\"\n" +
          "else:\n" +
          "    grade = \"F\"\n\n" +
          "print(f\"Оценка: {grade}\")\n\n" +
          "# Цепочка сравнений\n" +
          "if 0 <= score <= 100:\n" +
          "    print(\"Корректный ввод\")",
        keyTakeaways: [
          "`elif` — сокращение от else if, может быть сколько угодно.",
          "Тело блока определяется отступом, а не `{ }`.",
          "Цепочка `a < b < c` — это `(a < b) and (b < c)`.",
        ],
        pitfalls: [
          "Не забыть `:` в конце `if / elif / else` строки.",
          "Не путать `=` (присваивание) и `==` (сравнение).",
          "Сравнивать с None через `is None`, а не через `== None`.",
        ],
      },
      {
        heading: "Истинность значений и логические операторы",
        tagline: "Не только True/False — почти всё может быть «истиной»",
        body:
          "Любое значение в Python приводится к bool. **Ложными** считаются: `False`, `None`, `0`, `0.0`, `\"\"` (пустая строка), `[]`, `{}`, `()`, `set()`. Всё остальное — **истинно**.\n\n" +
          "Поэтому идиома `if items:` — это «если список не пуст». А `if name:` — «если имя не пустое».\n\n" +
          "Логические операторы: `and` (и), `or` (или), `not` (не). Они **ленивые**: `a and b` не вычисляет `b`, если `a` уже ложно. Это удобно: `if user is not None and user.active:` — безопасно.",
        code:
          "name = input(\"Имя: \")\n" +
          "if not name:                   # пустая строка — ложь\n" +
          "    print(\"Имя обязательно\")\n\n" +
          "items = []\n" +
          "if items:                      # пустой список — ложь\n" +
          "    print(\"есть товары\")\n" +
          "else:\n" +
          "    print(\"корзина пуста\")\n\n" +
          "age = 18\n" +
          "if 18 <= age < 60 and name:    # совершеннолетний И имя не пустое\n" +
          "    print(\"Доступ разрешён\")",
        keyTakeaways: [
          "`if x:` — короткая идиома для «x не пусто/не ноль/не None».",
          "`and` и `or` ленивые — удобно для защиты от None.",
          "`not` инвертирует булево значение.",
        ],
        pitfalls: [
          "Не пиши `if x == True:` — пиши просто `if x:`.",
          "Не путай `or` и `|` (последний — побитовый).",
          "`if 0:` — это False, а не True. `0` — ложное значение.",
        ],
      },
    ],
    cheatSheet: [
      "Сравнение: `==`, `!=`, `<`, `>`, `<=`, `>=`. Цепочка: `0 <= x <= 100`.",
      "Логика: `and`, `or`, `not`. Ленивая.",
      "Ложь: False, None, 0, 0.0, \"\", [], {}, (), set().",
      "`if x is None:` — правильный способ проверять None.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j2-f1",
      title: "Школьная оценка",
      description: "Заверши логику выбора оценки.",
      code:
        "score = int(input())\n" +
        "if score {{0}} 90:\n" +
        "    grade = \"A\"\n" +
        "{{1}} score >= 75:\n" +
        "    grade = \"B\"\n" +
        "elif score >= 60:\n" +
        "    grade = \"C\"\n" +
        "{{2}}:\n" +
        "    grade = \"F\"\n" +
        "print(grade)",
      answers: [[">="], ["elif"], ["else"]],
      hints: [
        "Оценка A — за 90 и выше. Оператор «больше или равно».",
        "«Иначе если» — одно ключевое слово.",
        "«Иначе» — одно ключевое слово.",
      ],
      explanation: {
        summary:
          "Классическая каскадная проверка: сначала самые высокие пороги, потом ниже, и в конце — все остальные через else.",
        keyPoints: [
          "Порядок elif имеет значение: первое подошедшее условие выигрывает.",
          "else — обязательно последний и без условия.",
        ],
      },
    },
    {
      type: "fill",
      id: "j2-f2",
      title: "Совершеннолетний и имя задано",
      description:
        "Проверь два условия одновременно: возраст в диапазоне и имя не пустое.",
      code:
        "name = input()\n" +
        "age = int(input())\n" +
        "if 18 {{0}} age < 60 {{1}} name:\n" +
        "    print(\"OK\")\n" +
        "{{2}}:\n" +
        "    print(\"NO\")",
      answers: [["<="], ["and"], ["else"]],
      hints: [
        "Левая граница диапазона включается.",
        "Логическое И.",
        "Иначе.",
      ],
      explanation: {
        summary:
          "В одном if проверяем диапазон возраста и непустоту имени. Имя — строка; пустая строка ложна, поэтому достаточно `name`.",
        keyPoints: [
          "Цепочка сравнений короче `(a <= b) and (b < c)`.",
          "`name` без `== \"\"` — идиоматичная проверка непустоты.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j2-q1",
      title: "Какие значения ложны?",
      question:
        "Перечисли через запятую как минимум 4 значения, которые в Python приводятся к False (булево «ложь»).",
      answers: [
        "false, none, 0, \"\"",
        "0, none, false, \"\"",
        "0, \"\", [], none",
        "0, none, [], \"\"",
        "false, 0, none, \"\"",
        "0, false, none, \"\"",
        "0, 0.0, none, false",
        "false, none, 0, 0.0",
        "0, {}, false, none",
        "false, none, 0, {}",
        "0, (), false, none",
        "false, none, 0, ()",
        "0, [], false, none",
        "none, 0, [], false",
        "false, none, [], 0",
        "false, 0, \"\", none",
        "false, none, 0, ''",
        "0, none, false, ''",
        "false, 0, none, ''",
        "0, '', none, false",
        "false, none, 0, 0.0, ''",
        "false, none, 0, 0.0, \"\"",
        "0, 0.0, '', none, false",
        "0, 0.0, \"\", none, false",
        "false, none, 0, [], ''",
        "false, none, 0, {}, ''",
        "false, none, 0, (), ''",
      ],
      hint: "False, None, 0, пустые коллекции, пустая строка...",
      explanation:
        "Ложные значения: False, None, 0, 0.0, \"\" (пустая строка), [] (пустой список), {} (пустой словарь), () (пустой кортеж), set(). Всё остальное — истинно.",
    },
    {
      type: "question",
      id: "j2-q2",
      title: "Правильная проверка на None",
      question:
        "Как идиоматично проверить, что переменная x равна None? Напиши целиком условие if.",
      answers: ["if x is none:", "if x is none", "if x is None:", "if x is None"],
      hint: "Используй is, не ==.",
      explanation:
        "Правильно: `if x is None:`. None — единственный объект своего типа, и сравнение через `is` быстрее и идиоматичнее, чем через `==`.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j2-w1",
      title: "FizzBuzz одного числа",
      task:
        "Считай число n. Если оно делится на 15 — напечатай FizzBuzz. Если только на 3 — Fizz. Если только на 5 — Buzz. Иначе — само число.",
      hints: [
        "Делимость проверяется через остаток `%`. Например, n % 3 == 0.",
        "Сначала проверь делимость на 15 (это самый специфичный случай).",
        "Используй elif для остальных случаев и else для печати числа.",
      ],
      required: ["input(", "%", "if", "elif", "else", "print("],
      minLines: 8,
      explanation: {
        summary:
          "Классическая задача на условия. Главное — порядок: сначала самое узкое условие (делится на 15), потом более общие.",
        keyPoints: [
          "`%` — остаток от деления; ноль означает делимость.",
          "Порядок elif критичен: 15 = 3*5, поэтому проверка на 15 идёт первой.",
        ],
      },
    },
  ],
};

const j3: Round = {
  number: 3,
  title: "Junior · Циклы for и while",
  level: "Начальный",
  intro:
    "Цикл — это «сделай это много раз». Учим for (по чему-то ходим) и while (пока условие истинно). Заодно — range, break, continue и почему `for i in range(len(seq))` — почти всегда антипаттерн.",
  lesson: {
    title: "Циклы и идиомы перебора",
    summary:
      "for — для прохода по коллекциям; while — для повторения по условию. Учимся идиоматично перебирать списки, пользоваться range, break и continue.",
    readingMinutes: 5,
    sections: [
      {
        heading: "for и range",
        tagline: "for идёт по итерируемому объекту, не по индексам",
        body:
          "**for** перебирает любую **итерируемую** (iterable) штуку: список, кортеж, строку, словарь, файл, генератор. Не нужно знать индексы — Python сам выдаёт по одному элементу:\n\n" +
          "Если нужны именно числа от 0 до n — есть `range(n)`. Это «ленивый» объект: он не создаёт список в памяти, а выдаёт числа по одному.\n\n" +
          "- `range(5)` → 0, 1, 2, 3, 4\n" +
          "- `range(2, 8)` → 2, 3, 4, 5, 6, 7\n" +
          "- `range(0, 10, 2)` → 0, 2, 4, 6, 8 (третий аргумент — шаг).",
        code:
          "names = [\"Аня\", \"Боря\", \"Вася\"]\n\n" +
          "for name in names:\n" +
          "    print(name)\n\n" +
          "# Парой (индекс, значение) — enumerate\n" +
          "for i, name in enumerate(names, start=1):\n" +
          "    print(f\"{i}. {name}\")\n\n" +
          "# Числа от 1 до 10 — range\n" +
          "for n in range(1, 11):\n" +
          "    print(n, n ** 2)",
        keyTakeaways: [
          "`for x in seq:` — перебор по элементам, не по индексам.",
          "`enumerate(seq, start=1)` — пары (индекс, элемент).",
          "`range(stop)` / `range(start, stop)` / `range(start, stop, step)`.",
        ],
        pitfalls: [
          "`for i in range(len(seq)):` — почти всегда антипаттерн. Используй прямой перебор.",
          "Не путай `range(10)` (0..9) и `range(1, 10)` (1..9). Правая граница НЕ включается.",
          "Изменение списка во время прохода `for` — путь к багам. Лучше собирай новый.",
        ],
      },
      {
        heading: "Идиомы перебора: zip, enumerate, sum, max, min",
        tagline: "Используй встроенные функции вместо ручных счётчиков",
        body:
          "Python даёт мощный набор встроенных функций для работы с коллекциями:\n\n" +
          "- `enumerate(seq, start=0)` — возвращает пары `(индекс, элемент)`. Параметр `start` задаёт начальный индекс.\n" +
          "- `zip(a, b, c, ...)` — «застёгивает» несколько итерируемых в пары-тройки и т.д. Останавливается на самой короткой.\n" +
          "- `sum(iterable)` — сумма; `max(iterable)` / `min(iterable)` — максимум/минимум. Работают с любым итерируемым, не только со списком.\n" +
          "- `sorted(iterable, key=fn, reverse=False)` — возвращает НОВЫЙ список. `sort()` — сортирует на месте.\n" +
          "- `reversed(seq)` — ленивый итератор в обратном порядке.\n" +
          "- `any(iterable)` / `all(iterable)` — «хотя бы один истинен» / «все истинны». Ленивые — останавливаются как только знают ответ.",
        code:
          "scores = [75, 92, 60, 88, 45]\n\n" +
          "# enumerate — нумерация с 1\n" +
          "for i, score in enumerate(scores, start=1):\n" +
          "    status = \"OK\" if score >= 60 else \"Неуд\"\n" +
          "    print(f\"{i}. {score} — {status}\")\n\n" +
          "# zip — два списка одновременно\n" +
          "names = [\"Аня\", \"Боря\", \"Вася\", \"Галя\", \"Дима\"]\n" +
          "for name, score in zip(names, scores):\n" +
          "    print(f\"{name}: {score}\")\n\n" +
          "# Агрегатные функции\n" +
          "print(f\"Лучший: {max(scores)}, Худший: {min(scores)}\")\n" +
          "print(f\"Средний: {sum(scores) / len(scores):.1f}\")\n\n" +
          "# Сортировка по значению (не на месте)\n" +
          "top3 = sorted(scores, reverse=True)[:3]\n" +
          "print(f\"Топ-3: {top3}\")\n\n" +
          "# any / all\n" +
          "print(any(s >= 90 for s in scores))   # есть хотя бы одна отличная?\n" +
          "print(all(s >= 60 for s in scores))   # все сдали?",
        keyTakeaways: [
          "`enumerate(seq, start=1)` — пары (индекс, элемент). Лучше, чем ручной счётчик.",
          "`zip(a, b)` — параллельный перебор двух и более последовательностей.",
          "`any(gen)` / `all(gen)` ленивые — быстрее, чем цикл с флагом.",
        ],
        pitfalls: [
          "`zip` останавливается на самом коротком. Если нужно включить все — `itertools.zip_longest`.",
          "`sorted()` возвращает новый список. `list.sort()` меняет список на месте и возвращает `None`.",
          "`sum([\"a\", \"b\"])` — TypeError. `sum` работает только с числами (и можно `start=` задать начало).",
        ],
      },
      {
        heading: "while, break, continue",
        tagline: "while — для условий; break — выйти; continue — следующая итерация",
        body:
          "**while** повторяет блок, пока условие истинно. Используется там, где число итераций заранее неизвестно: ввод пользователя, условие сходимости, чтение данных.\n\n" +
          "**break** — досрочно выйти из цикла. **continue** — пропустить остаток тела и перейти к следующей итерации.\n\n" +
          "Очень частая ошибка — забыть менять условие в while. Тогда цикл становится **бесконечным**. Если случилось — Ctrl+C в терминале.",
        code:
          "# while с break\n" +
          "while True:\n" +
          "    s = input(\"Команда (q — выход): \")\n" +
          "    if s == \"q\":\n" +
          "        break\n" +
          "    print(\"Получил:\", s)\n\n" +
          "# continue: пропустить чётные\n" +
          "for n in range(1, 11):\n" +
          "    if n % 2 == 0:\n" +
          "        continue\n" +
          "    print(n)            # 1, 3, 5, 7, 9\n\n" +
          "# Идиома: пока есть данные\n" +
          "balance = 100\n" +
          "while balance > 0:\n" +
          "    balance -= 30\n" +
          "    print(balance)",
        keyTakeaways: [
          "while используют, когда нет «коллекции» для перебора.",
          "break выходит из ближайшего цикла, continue — к следующей итерации.",
          "Не забывай менять условие — иначе бесконечный цикл.",
        ],
        pitfalls: [
          "`while True:` без break внутри — бесконечный цикл.",
          "`break` ВНЕ цикла — SyntaxError.",
          "Если изменяешь счётчик внутри for — это работает, но влияет только на текущую итерацию (range уже посчитан).",
        ],
        analogy:
          "while — это охранник на посту: «пока не пришёл сменщик — стой». break — сменщик пришёл, выходи. continue — пропустить запись в журнале и идти дальше. Если сменщик никогда не придёт — бесконечная вахта.",
      },
    ],
    cheatSheet: [
      "for x in seq: — главный цикл; range(n) — для целых чисел.",
      "enumerate(seq, start=1) — пары (i, x); zip(a, b) — параллельный проход.",
      "while условие: — пока истинно. break / continue — выход / пропуск.",
      "Не пиши for i in range(len(seq)) — пиши for x in seq.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j3-f1",
      title: "Сумма от 1 до n через for",
      description: "Подсчитай сумму чисел от 1 до n включительно.",
      code:
        "n = int(input())\n" +
        "total = 0\n" +
        "for i in {{0}}(1, n + 1):\n" +
        "    total {{1}} i\n" +
        "print(total)",
      answers: [["range"], ["+="]],
      hints: [
        "Функция, которая выдаёт целые числа в диапазоне.",
        "Краткая запись «прибавь и сохрани».",
      ],
      explanation: {
        summary:
          "Чтобы пройти от 1 до n включительно, используем range(1, n+1) — правая граница не включается. += короче, чем total = total + i.",
        keyPoints: [
          "range(1, n+1) даёт 1, 2, ..., n.",
          "+= и -= работают для всех числовых типов.",
        ],
      },
    },
    {
      type: "fill",
      id: "j3-f2",
      title: "Поиск с break",
      description:
        "Найди первое чётное число в списке и сразу выйди из цикла.",
      code:
        "nums = [3, 7, 11, 12, 5, 8]\n" +
        "found = None\n" +
        "for n in {{0}}:\n" +
        "    if n {{1}} 2 == 0:\n" +
        "        found = n\n" +
        "        {{2}}\n" +
        "print(found)   # 12",
      answers: [["nums"], ["%"], ["break"]],
      hints: [
        "Имя списка, по которому ходим.",
        "Оператор остатка от деления.",
        "Ключевое слово выхода из цикла.",
      ],
      explanation: {
        summary:
          "Классический паттерн «найти первый и выйти». Без break цикл бы продолжался зря после нахождения.",
        keyPoints: [
          "n % 2 == 0 — число чётное.",
          "break экономит время на больших списках.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j3-q1",
      title: "Что напечатает range(2, 8)?",
      question:
        "Перечисли через запятую все числа, которые выдаст for x in range(2, 8): print(x).",
      answers: ["2, 3, 4, 5, 6, 7", "2,3,4,5,6,7"],
      hint: "Левая граница включается, правая — нет.",
      explanation:
        "range(start, stop) выдаёт start, start+1, ..., stop-1. Поэтому range(2, 8) → 2, 3, 4, 5, 6, 7.",
    },
    {
      type: "question",
      id: "j3-q2",
      title: "Чем отличаются break и continue?",
      question:
        "В одном предложении: что делает break и что делает continue?",
      answers: [
        "break выходит из цикла, continue переходит к следующей итерации",
        "break прерывает цикл, continue пропускает текущую итерацию",
        "break — выход из цикла, continue — следующий шаг",
      ],
      hint: "Один — выходит, другой — пропускает.",
      explanation:
        "break полностью прекращает выполнение ближайшего цикла. continue прерывает только текущую итерацию и сразу переходит к следующей.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j3-w1",
      title: "Печать таблицы умножения",
      task:
        "Выведи таблицу умножения 5 на 5: пять строк, в каждой пять чисел через пробел. Используй вложенные циклы for и end=\" \" в print, плюс пустой print() в конце строки.",
      hints: [
        "Внешний цикл — по строкам (i от 1 до 5).",
        "Внутренний — по столбцам (j от 1 до 5).",
        "print(i*j, end=\" \") печатает без переноса строки. print() в конце — перенос.",
      ],
      required: ["for", "range(", "print(", "end="],
      minLines: 4,
      explanation: {
        summary:
          "Демонстрация вложенных циклов и параметра end в print, который позволяет печатать без перевода строки.",
        keyPoints: [
          "Вложенные циклы — стандартный способ обходить таблицы.",
          "end=\" \" в print — печать с пробелом вместо переноса.",
        ],
      },
    },
  ],
};

const j4: Round = {
  number: 4,
  title: "Junior · Строки и форматирование",
  level: "Начальный",
  intro:
    "Строки в Python — это последовательности символов. Учимся резать (slicing), считать длину, искать, заменять, разбивать на части и красиво форматировать через f-строки.",
  lesson: {
    title: "Работа со строками",
    summary:
      "Длина, индексация, срезы, частые методы (.upper, .lower, .strip, .split, .join, .replace), f-строки и форматирование чисел.",
    readingMinutes: 5,
    sections: [
      {
        heading: "Индексация и срезы (slicing)",
        tagline: "s[i:j:k] — главный нож для строк",
        body:
          "Строка — это последовательность символов. Индексация с **нуля**. Отрицательные индексы считают с конца: `s[-1]` — последний символ.\n\n" +
          "**Срез** — `s[start:stop:step]`. Все три аргумента опциональны:\n\n" +
          "- `s[2:5]` — символы с индексами 2, 3, 4 (правая граница не включается).\n" +
          "- `s[:3]` — первые три.\n" +
          "- `s[-3:]` — последние три.\n" +
          "- `s[::-1]` — строка в обратном порядке.\n" +
          "- `s[::2]` — каждый второй.\n\n" +
          "Строки **неизменяемы** (immutable): `s[0] = \"X\"` — TypeError. Любая «модификация» создаёт новую строку.",
        code:
          "s = \"Python\"\n" +
          "print(len(s))     # 6\n" +
          "print(s[0])       # 'P'\n" +
          "print(s[-1])      # 'n'\n" +
          "print(s[1:4])     # 'yth'\n" +
          "print(s[::-1])    # 'nohtyP' — переворот строки\n" +
          "print(\"Py\" in s)  # True — проверка вхождения подстроки",
        keyTakeaways: [
          "Индексы с 0; отрицательные — с конца.",
          "Срез s[a:b] не включает b.",
          "Строки неизменяемы — операции возвращают новую строку.",
        ],
        pitfalls: [
          "`s[10]` для короткой строки — IndexError. Срез `s[10:20]` тихо вернёт пусто — это безопаснее.",
          "Не путай `s[-1]` (последний символ) и `s[len(s)]` (за пределами — ошибка).",
        ],
      },
      {
        heading: "Главные методы строк и f-форматирование",
        tagline: "split / join / strip / replace / upper / lower",
        body:
          "Самые частые методы: `.upper()`, `.lower()`, `.title()`, `.strip()` (убрать пробелы по краям), `.replace(a, b)`, `.split(sep)` (разбить на список), `.join(seq)` (склеить), `.startswith(...)`, `.endswith(...)`, `.find(...)`.\n\n" +
          "Форматирование чисел в f-строке через двоеточие:\n\n" +
          "- `f\"{x:.2f}\"` — два знака после точки;\n" +
          "- `f\"{x:>10}\"` — выровнять вправо в поле шириной 10;\n" +
          "- `f\"{n:_d}\"` — разделители тысяч (`1_000_000`);\n" +
          "- `f\"{x=}\"` (Py 3.8+) — `x=значение`, отличная отладка.\n\n" +
          "Метод `.count(s)` — количество вхождений подстроки. `.isdigit()` — все ли символы цифры. `.isalpha()` — все ли буквы. Эти проверки удобны перед приведением типа: `if s.isdigit(): int(s)`.",
        code:
          "raw = \"  Hello, World!  \"\n" +
          "print(raw.strip())             # 'Hello, World!'\n" +
          "print(raw.lower())             # '  hello, world!  '\n" +
          "print(raw.replace(\"l\", \"L\"))    # '  HeLLo, WorLd!  '\n\n" +
          "csv = \"яблоко,груша,слива\"\n" +
          "parts = csv.split(\",\")        # ['яблоко', 'груша', 'слива']\n" +
          "joined = \" / \".join(parts)    # 'яблоко / груша / слива'\n\n" +
          "price = 1234.5678\n" +
          "print(f\"Итого: {price:.2f} ₽\") # Итого: 1234.57 ₽",
        keyTakeaways: [
          ".strip() — обычная гигиена: убирает пробелы и переносы по краям.",
          ".split(sep) и sep.join(list) — обратные операции.",
          "Форматирование чисел через `:.2f`, разделители через `:_d`.",
        ],
        pitfalls: [
          "Метод НЕ меняет строку — нужно присвоить результат: `s = s.strip()`.",
          "split() без аргументов делит по любым пробелам (удобно), с аргументом — строго по нему.",
          "join работает только со списком/итерируемым строк. `\" \".join([1, 2])` — TypeError.",
        ],
      },
    ],
    cheatSheet: [
      "s[i], s[-1], s[a:b], s[::-1] — индексация и срезы.",
      "len(s), 'sub' in s — длина и поиск.",
      ".strip(), .lower(), .upper(), .replace(a,b), .split(sep), sep.join(list).",
      "f\"{x:.2f}\", f\"{n:_d}\", f\"{var=}\" — форматирование.",
      "Строки неизменяемы — методы возвращают новую строку.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j4-f1",
      title: "Срезы строк",
      description: "Получи разные части строки через срезы.",
      code:
        "s = \"Programming\"\n" +
        "first3 = s[{{0}}]      # 'Pro'\n" +
        "last3 = s[{{1}}]       # 'ing'\n" +
        "reversed_s = s[{{2}}]  # 'gnimmargorP'\n" +
        "print(first3, last3, reversed_s)",
      answers: [[":3"], ["-3:"], ["::-1"]],
      hints: [
        "Срез без start, до индекса 3.",
        "Срез с -3 до конца.",
        "Срез с шагом -1 — переворот.",
      ],
      explanation: {
        summary:
          "Срезы — самый мощный инструмент работы со строками. Шаг -1 — стандартная идиома реверса.",
        keyPoints: [
          "s[:n] — первые n.",
          "s[-n:] — последние n.",
          "s[::-1] — переворот.",
        ],
      },
    },
    {
      type: "fill",
      id: "j4-f2",
      title: "split / strip / join",
      description: "Очисти строку, разбей по запятой и склей через ' | '.",
      code:
        "raw = \"  яблоко,  груша,слива  \"\n" +
        "parts = raw.{{0}}().split(\"{{1}}\")\n" +
        "parts = [p.strip() for p in parts]\n" +
        "result = \"{{2}}\".join(parts)\n" +
        "print(result)   # 'яблоко | груша | слива'",
      answers: [["strip"], [","], [" | "]],
      hints: [
        "Метод, убирающий пробелы по краям.",
        "Разделитель — запятая.",
        "Чем склеиваем (с пробелами вокруг палочки).",
      ],
      explanation: {
        summary:
          "Идиома очистки CSV: strip → split → strip каждой части → join. После split каждый кусок может иметь лишние пробелы — поэтому второй strip.",
        keyPoints: [
          "split(\",\") разбивает строго по запятой.",
          "Внутренний strip в comprehension убирает пробелы у каждого кусочка.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j4-q1",
      title: "Длина строки",
      question:
        "Какой функцией узнать количество символов в строке s?",
      answers: ["len(s)", "len"],
      hint: "Эта же функция работает для списков, словарей, кортежей.",
      explanation:
        "`len(s)` возвращает количество элементов: символов в строке, элементов в списке, ключей в словаре, и т.д.",
    },
    {
      type: "question",
      id: "j4-q2",
      title: "Изменяемость строк",
      question:
        "Строки в Python — изменяемые или неизменяемые? Ответь одним словом.",
      answers: ["неизменяемые", "неизменяема", "неизменяемый", "immutable"],
      hint: "Можно ли написать s[0] = 'X'?",
      explanation:
        "Строки неизменяемы (immutable). Операции типа `s.upper()` возвращают НОВУЮ строку, а старая остаётся в памяти как была.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j4-w1",
      title: "Палиндром",
      task:
        "Напиши программу, которая считывает строку и печатает 'YES', если она палиндром (читается одинаково в обе стороны), иначе 'NO'. Учитывай регистр без разницы и игнорируй пробелы.",
      hints: [
        "Приведи строку к нижнему регистру через .lower() и убери пробелы через .replace(' ', '').",
        "Палиндром: s == s[::-1].",
        "Печатай 'YES' или 'NO' через if/else.",
      ],
      required: ["input(", ".lower()", ".replace(", "[::-1]", "if", "else"],
      minLines: 5,
      explanation: {
        summary:
          "Идеальная задача на срезы и методы строк. Палиндром = строка равна своему перевёрнутому виду.",
        keyPoints: [
          "lower() и replace() нормализуют ввод.",
          "[::-1] — компактный способ перевернуть.",
        ],
      },
    },
  ],
};

const j5: Round = {
  number: 5,
  title: "Junior · Списки и кортежи",
  level: "Начальный → Средний",
  intro:
    "Список — упорядоченная изменяемая последовательность. Кортеж — почти то же, но неизменяемый. Учимся создавать, добавлять/удалять, перебирать, сортировать, и понимать, когда брать что.",
  lesson: {
    title: "Списки (list) и кортежи (tuple)",
    summary:
      "Базовые операции, основные методы (append, extend, insert, remove, pop, index, sort, reverse), срезы, копирование, и в чём практический смысл tuple.",
    readingMinutes: 5,
    sections: [
      {
        heading: "Список — главный контейнер Python",
        tagline: "list изменяем, упорядочен, допускает дубликаты",
        body:
          "Создаётся `[]` или `list(...)`. Доступ — по индексу, как в строке. Длина — `len(lst)`. Проверка вхождения — `x in lst`. Срезы работают так же, как в строках.\n\n" +
          "**Главные методы**:\n" +
          "- `.append(x)` — добавить в конец (O(1)).\n" +
          "- `.extend(iter)` — добавить сразу много (эффективнее цикла с append).\n" +
          "- `.insert(i, x)` — вставить по индексу (O(n) — сдвигает все элементы после).\n" +
          "- `.remove(x)` — удалить первое совпадение (ValueError если нет).\n" +
          "- `.pop(i=-1)` — удалить и вернуть (по умолчанию — последний).\n" +
          "- `.index(x)` — найти индекс (ValueError если нет).\n" +
          "- `.count(x)` — сколько раз встречается.\n" +
          "- `.sort()` / `sorted(lst)` — сортировка на месте / новый список.\n" +
          "- `.reverse()` / `lst[::-1]` — переворот на месте / новый список.\n" +
          "- `.clear()` — очистить список.\n" +
          "- `.copy()` — **поверхностная** копия (shallow copy).\n\n" +
          "Списки — **изменяемые** (mutable). Это значит: если передал список в функцию, она может его поменять. Если хочешь копию — `b = a.copy()` или `b = list(a)`.",
        code:
          "items = [3, 1, 4, 1, 5]\n" +
          "items.append(9)            # [3, 1, 4, 1, 5, 9]\n" +
          "items.remove(1)            # удалит ПЕРВУЮ единицу → [3, 4, 1, 5, 9]\n" +
          "last = items.pop()         # 9; items = [3, 4, 1, 5]\n" +
          "items.sort()               # [1, 3, 4, 5]\n" +
          "print(sorted(items, reverse=True))  # [5, 4, 3, 1] — новый список\n" +
          "print(items)                        # [1, 3, 4, 5]   — оригинал не тронут",
        keyTakeaways: [
          "list изменяем; копировать через .copy() или list(a).",
          ".sort() меняет на месте, sorted() возвращает новый.",
          ".remove(x) удаляет первое вхождение, .pop(i) — по индексу с возвратом.",
        ],
        pitfalls: [
          "`b = a` — это НЕ копия, это второй ярлык на тот же список.",
          ".sort() возвращает None, а не сам список. `b = a.sort()` → b is None.",
          "Удаление элементов во время for по тому же списку — путь к багам.",
        ],
        analogy:
          "list — это блокнот: листаешь, добавляешь, вырываешь страницы. `b = a` — это не новый блокнот, а второй человек смотрит в тот же блокнот. Чтобы получить копию — нужно его переписать (`a.copy()`).",
      },
      {
        heading: "Кортеж — список, который нельзя менять",
        tagline: "tuple — для констант, ключей словаря и распаковки",
        body:
          "**Кортеж** — это неизменяемая упорядоченная последовательность. Записывается через `(...)` или просто через запятую: `point = (3, 5)` или `point = 3, 5`.\n\n" +
          "Зачем он нужен:\n\n" +
          "- Когда логически нужна **константная** группа: координаты, версия, цвет RGB.\n" +
          "- Когда возвращаешь из функции **несколько значений** — это и есть tuple.\n" +
          "- Когда нужно использовать как **ключ словаря** или элемент **множества** (list нельзя — он изменяемый).\n\n" +
          "Очень полезная штука — **распаковка** (unpacking): `a, b = point`.",
        code:
          "point = (3, 5)\n" +
          "x, y = point              # распаковка\n" +
          "print(x, y)               # 3 5\n\n" +
          "def divmod_(a, b):\n" +
          "    return a // b, a % b  # фактически возвращает tuple\n\n" +
          "q, r = divmod_(17, 5)\n" +
          "print(q, r)               # 3 2\n\n" +
          "# tuple как ключ словаря — потому что неизменяем\n" +
          "scores = {(\"Аня\", \"матем\"): 12, (\"Боря\", \"физика\"): 10}",
        keyTakeaways: [
          "Кортеж = неизменяемый список. (1,) — кортеж из одного элемента, (1) — просто число.",
          "Распаковка — главный кейс: a, b, *rest = seq.",
          "Кортежи можно использовать как ключи словаря.",
        ],
        pitfalls: [
          "`(1)` — это число, а не кортеж. Правильно: `(1,)`.",
          "Внутри tuple могут быть изменяемые объекты: `t = ([], 1); t[0].append(5)` сработает, хотя сам t «неизменяем».",
        ],
        analogy:
          "Кортеж — это GPS-координата на карте. Нельзя переместить точку — она зафиксирована навсегда. Зато можно использовать её как ключ («найди кафе у координаты (55.7, 37.6)»). Список — это маршрут: можно добавлять точки по ходу.",
      },
    ],
    cheatSheet: [
      "list = [...] — изменяемый. tuple = (...,) — неизменяемый.",
      ".append, .extend, .insert, .remove, .pop, .index, .sort, .reverse, .count.",
      "sorted(seq) — новый отсортированный; sorted(seq, key=..., reverse=True).",
      "Копирование: list(a) или a.copy(). Срез a[:] — тоже копия.",
      "Распаковка: a, b, *rest = seq.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j5-f1",
      title: "Базовые операции со списком",
      description: "Заверши последовательность операций.",
      code:
        "items = [3, 1, 4, 1, 5]\n" +
        "items.{{0}}(9)         # добавить 9 в конец\n" +
        "items.{{1}}(1)         # удалить первую единицу\n" +
        "items.{{2}}()           # отсортировать на месте\n" +
        "print(items)\n" +
        "print({{3}}(items))     # длина списка",
      answers: [["append"], ["remove"], ["sort"], ["len"]],
      hints: [
        "Метод добавления в конец.",
        "Метод удаления по значению (первое вхождение).",
        "Метод сортировки на месте.",
        "Функция длины.",
      ],
      explanation: {
        summary:
          "Самые частые операции на собеседованиях. Различай append/extend, remove/pop, sort/sorted.",
        keyPoints: [
          "append добавляет один элемент, extend — много.",
          "sort на месте → возвращает None.",
        ],
      },
    },
    {
      type: "fill",
      id: "j5-f2",
      title: "Распаковка кортежа",
      description: "Раздели tuple на отдельные переменные.",
      code:
        "def stats(nums):\n" +
        "    return min(nums), max(nums), sum(nums) / len(nums)\n\n" +
        "lo, hi, {{0}} = {{1}}([10, 20, 30, 40])\n" +
        "print(lo, hi, avg)   # 10 40 25.0",
      answers: [["avg"], ["stats"]],
      hints: [
        "Третья распаковываемая переменная — среднее.",
        "Имя функции, которая возвращает три значения.",
      ],
      explanation: {
        summary:
          "Функция возвращает tuple из 3-х значений, мы сразу распаковываем в три имени. Очень частая идиома.",
        keyPoints: [
          "Сколько имён слева — столько значений справа.",
          "Можно `*rest` для «всех остальных»: `first, *rest = [1, 2, 3, 4]`.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j5-q1",
      title: "list или tuple?",
      question:
        "Если нужна структура для координат точки (x, y), которая никогда не меняется и может использоваться как ключ словаря — что выбрать: list или tuple? Ответь одним словом.",
      answers: ["tuple", "кортеж"],
      hint: "Какая из двух структур неизменяема?",
      explanation:
        "tuple. Он неизменяем (поэтому может быть ключом словаря/элементом set), легче и явнее показывает «это константная группа».",
    },
    {
      type: "question",
      id: "j5-q2",
      title: "sort vs sorted",
      question:
        "В чём ключевое различие между методом list.sort() и функцией sorted(list)?",
      answers: [
        "sort меняет список на месте и возвращает none, sorted возвращает новый отсортированный список",
        "sort — на месте, sorted — новый список",
        "sort изменяет исходный список, sorted создаёт новый",
        "sort возвращает none, sorted возвращает новый список",
      ],
      hint: "Один меняет оригинал, другой возвращает копию.",
      explanation:
        "list.sort() сортирует список на месте и возвращает None. sorted(list) не трогает оригинал, а возвращает НОВЫЙ отсортированный список (работает с любым итерируемым).",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j5-w1",
      title: "Топ-3 наибольших",
      task:
        "Получи список чисел, разделённых пробелом, и напечатай через пробел три наибольших числа в порядке убывания. Используй input().split(), map(int, ...), sorted с reverse=True и срез.",
      hints: [
        "nums = list(map(int, input().split())) — стандартная идиома считывания списка чисел.",
        "sorted(nums, reverse=True) даёт новый отсортированный по убыванию список.",
        "Срез [:3] возьмёт первые три. Печатай через ' '.join(map(str, ...)).",
      ],
      required: ["input(", "split(", "map(", "sorted(", "reverse=True", "[:3]"],
      minLines: 4,
      explanation: {
        summary:
          "Связка input().split() + map(int, ...) + sorted + срез — джентельменский набор для задач со списками.",
        keyPoints: [
          "split() без аргументов делит по любым пробелам.",
          "map(int, seq) возвращает итератор; list() или unpacking превращает в список.",
        ],
      },
    },
  ],
};

const j6: Round = {
  number: 6,
  title: "Junior · Функции",
  level: "Средний",
  intro:
    "Функция — это именованный кусок кода, который можно вызвать столько раз, сколько нужно. Учим def, параметры, return, значения по умолчанию, *args/**kwargs и базовую область видимости.",
  lesson: {
    title: "Определение и вызов функций",
    summary:
      "Как объявлять, какие параметры бывают, что такое return и где живут переменные. Это фундамент для всего: классов, декораторов, замыканий.",
    readingMinutes: 6,
    sections: [
      {
        heading: "def, параметры и return",
        tagline: "Функция превращает «как сделать» в «что сделать»",
        body:
          "Определение функции:\n\n" +
          "```\n" +
          "def имя(параметры):\n" +
          "    тело\n" +
          "    return значение\n" +
          "```\n\n" +
          "**Параметры** — это переменные, которые получает функция. **Аргументы** — это значения, с которыми её вызывают.\n\n" +
          "**return** возвращает значение и сразу выходит из функции. Если return нет — функция возвращает None.\n\n" +
          "Параметры могут быть **позиционными** (по порядку) и **именованными**: `divide(a=10, b=2)`. Можно задать **значение по умолчанию**: `def greet(name=\"друг\"):` — параметр станет необязательным.",
        code:
          "def add(a, b):\n" +
          "    return a + b\n\n" +
          "print(add(2, 3))                # 5\n" +
          "print(add(b=10, a=1))           # 11 — именованные\n\n" +
          "def greet(name, greeting=\"Привет\"):\n" +
          "    return f\"{greeting}, {name}!\"\n\n" +
          "print(greet(\"Аня\"))            # Привет, Аня!\n" +
          "print(greet(\"Боря\", \"Хей\"))    # Хей, Боря!",
        keyTakeaways: [
          "Имя функции — глагол: `add`, `parse`, `save`. Имя данных — существительное.",
          "Аргумент по умолчанию делает параметр необязательным.",
          "Без return функция возвращает None.",
        ],
        pitfalls: [
          "**Никогда не используй изменяемые объекты как значение по умолчанию**: `def f(x=[]):` — список общий между вызовами! Используй `def f(x=None): if x is None: x = []`.",
          "Именованные аргументы должны идти ПОСЛЕ позиционных в вызове.",
          "После return код не выполнится.",
        ],
        analogy:
          "Функция — это рецепт. Параметры — ингредиенты. `return` — блюдо на выходе. Значение по умолчанию — как «если нет соли — бери щепотку». Без return — как рецепт, который готовит блюдо, но не подаёт на стол (возвращает None).",
      },
      {
        heading: "*args, **kwargs и область видимости",
        tagline: "Когда не знаешь, сколько аргументов — звёзды спасают",
        body:
          "**`*args`** — собирает оставшиеся **позиционные** аргументы в кортеж. **`**kwargs`** — собирает оставшиеся **именованные** в словарь.\n\n" +
          "**Область видимости** (scope) в Python простая: переменная, созданная внутри функции, **не видна снаружи**. Переменные снаружи (глобальные) видны внутри для **чтения**, но для записи нужно слово `global` (и это почти всегда плохая практика — лучше передавать и возвращать).\n\n" +
          "Внутри вложенной функции есть ещё **enclosing** scope — переменные внешней функции. Туда заглядывают замыкания, но это тема Middle.",
        code:
          "def total(*nums):\n" +
          "    return sum(nums)\n\n" +
          "print(total(1, 2, 3, 4))        # 10\n\n" +
          "def make_user(**fields):\n" +
          "    return fields                # просто словарь\n\n" +
          "print(make_user(name=\"Аня\", age=17))\n" +
          "# {'name': 'Аня', 'age': 17}\n\n" +
          "x = 10\n" +
          "def show():\n" +
          "    print(x)                    # читаем глобальную — OK\n" +
          "show()",
        keyTakeaways: [
          "*args = tuple, **kwargs = dict.",
          "Внутри функции — своя scope. Снаружи переменные не видны.",
          "Глобальные читать можно, писать — только через global (но почти не нужно).",
        ],
        pitfalls: [
          "Аргументы по умолчанию вычисляются ОДИН раз — отсюда баг с `=[]`.",
          "Именованный аргумент после `*args` — становится keyword-only: `def f(*args, mode=\"sum\")`.",
        ],
        analogy:
          "*args — это «принеси всё, что угодно» с рюкзаком (кортеж). **kwargs — это «принеси всё именованное» с папкой (словарь). Scope — стены комнаты: то, что создал внутри комнаты, не видно снаружи.",
      },
      {
        heading: "Аннотации типов и документационные строки",
        tagline: "Type hints — контракт функции, docstring — её объяснение",
        body:
          "**Аннотации типов** (`->` и `: type`) появились в Python 3.5+. Интерпретатор их **не проверяет** — они для IDE и типчекеров (mypy/pyright). Делают код читаемее и помогают поймать баги заранее.\n\n" +
          "**Docstring** — строка документации, первое выражение функции. Доступна через `fn.__doc__`. Соглашение:\n\n" +
          "```\n" +
          "def fn(x: int) -> str:\n" +
          "    \"\"\"Одна строка — кратко, что делает функция.\"\"\"\n" +
          "```\n\n" +
          "Для сложных функций — многострочный docstring с описанием параметров и возврата (форматы: Google, NumPy, reStructuredText).\n\n" +
          "Правило: каждая функция, которую ты отдаёшь другому человеку, должна иметь хотя бы одну строку docstring.",
        code:
          "from typing import Union\n\n" +
          "def clamp(value: float, lo: float, hi: float) -> float:\n" +
          "    \"\"\"Ограничить значение диапазоном [lo, hi].\"\"\"\n" +
          "    return max(lo, min(hi, value))\n\n" +
          "def safe_divide(a: int, b: int) -> Union[float, None]:\n" +
          "    \"\"\"Разделить a на b. Вернёт None при делении на ноль.\"\"\"\n" +
          "    if b == 0:\n" +
          "        return None\n" +
          "    return a / b\n\n" +
          "print(clamp(150, 0, 100))   # 100.0\n" +
          "print(safe_divide(10, 0))   # None\n" +
          "print(clamp.__doc__)        # Ограничить значение...",
        keyTakeaways: [
          "Аннотации типов — для людей и инструментов, не для интерпретатора.",
          "`-> тип` после закрывающей скобки — тип возвращаемого значения.",
          "Docstring через тройные кавычки — стандарт Python.",
        ],
        pitfalls: [
          "Не верь, что `def f(x: int)` запретит передать строку в runtime. Python проигнорирует — проверяет только mypy.",
          "Пустой docstring бесполезен. Лучше вообще без него, чем `\"\"\"TODO\"\"\"`.",
        ],
      },
    ],
    cheatSheet: [
      "def name(a, b=10): return ... — функция с дефолтом.",
      "*args — кортеж позиционных, **kwargs — словарь именованных.",
      "Без return функция возвращает None.",
      "Не используй изменяемые объекты как значения по умолчанию.",
      "Глобальные читаются, пишутся только через global (избегай).",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j6-f1",
      title: "Функция со значением по умолчанию",
      description: "Заверши функцию приветствия.",
      code:
        "def greet(name, greeting{{0}}\"Привет\"):\n" +
        "    {{1}} f\"{greeting}, {name}!\"\n\n" +
        "print(greet(\"Аня\"))\n" +
        "print(greet(\"Боря\", greeting=\"Хей\"))",
      answers: [["="], ["return"]],
      hints: [
        "Между параметром и значением по умолчанию.",
        "Ключевое слово возврата результата.",
      ],
      explanation: {
        summary:
          "Дефолт делает параметр необязательным. Имя параметра можно задать при вызове через `=`.",
        keyPoints: [
          "Дефолтные параметры идут ПОСЛЕ обычных.",
          "При вызове можно указать имя параметра для ясности.",
        ],
      },
    },
    {
      type: "fill",
      id: "j6-f2",
      title: "*args и **kwargs",
      description:
        "Заверши функцию, которая принимает любое число чисел и любые именованные параметры.",
      code:
        "def report({{0}}numbers, {{1}}flags):\n" +
        "    print(\"sum:\", sum(numbers))\n" +
        "    print(\"flags:\", flags)\n\n" +
        "report(1, 2, 3, debug=True, fast=False)",
      answers: [["*"], ["**"]],
      hints: [
        "Звезда — кортеж позиционных аргументов.",
        "Две звезды — словарь именованных.",
      ],
      explanation: {
        summary:
          "*numbers — это все позиционные аргументы (tuple). **flags — все именованные (dict). Самая частая комбинация в декораторах и обёртках.",
        keyPoints: [
          "* и ** — это «соберать всё остальное».",
          "В вызове те же *seq и **dct делают распаковку.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j6-q1",
      title: "Функция без return",
      question:
        "Если функция не содержит инструкцию return, что она вернёт при вызове?",
      answers: ["none", "None"],
      hint: "Особое значение, обозначающее «ничего».",
      explanation:
        "Любая функция в Python всегда возвращает значение. Если return не указан — возвращается None.",
    },
    {
      type: "question",
      id: "j6-q2",
      title: "Опасное значение по умолчанию",
      question:
        "Почему в Python НЕЛЬЗЯ писать def f(items=[]): items.append(1) — что здесь не так?",
      answers: [
        "значение по умолчанию вычисляется один раз и общее для всех вызовов",
        "список общий для всех вызовов функции",
        "дефолтное значение создаётся однажды при определении функции",
        "изменяемое значение по умолчанию — общий объект между вызовами",
      ],
      hint: "Когда именно создаётся объект [] для дефолта?",
      explanation:
        "Дефолтное значение создаётся ОДИН раз при определении функции и остаётся одним и тем же объектом между вызовами. Поэтому изменения накапливаются. Правильно: `def f(items=None): items = items or []`.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j6-w1",
      title: "Функция clamp",
      task:
        "Напиши функцию clamp(value, lo=0, hi=100), которая возвращает value, ограниченное диапазоном [lo, hi]. То есть если value меньше lo — вернуть lo, больше hi — hi, иначе — само value. Покажи 3 примера вызова с print.",
      hints: [
        "Используй def clamp(value, lo=0, hi=100):",
        "Можно через if/elif/else или одной строкой через max(lo, min(value, hi)).",
        "Покажи: clamp(50), clamp(-5), clamp(200).",
      ],
      required: ["def clamp(", "return", "print(clamp("],
      minLines: 5,
      explanation: {
        summary:
          "Полезная утилита, которая встречается везде. Параметры с дефолтами — ровно то, что нужно для удобного API.",
        keyPoints: [
          "max(lo, min(value, hi)) — компактная идиома для clamp.",
          "Дефолты делают функцию удобнее в большинстве случаев.",
        ],
      },
    },
  ],
};

const j7: Round = {
  number: 7,
  title: "Junior · Словари и множества",
  level: "Средний",
  intro:
    "Словарь (dict) — это «ключ → значение». Множество (set) — коллекция уникальных элементов. Обе структуры внутри устроены на хешах, поэтому работают очень быстро.",
  lesson: {
    title: "dict и set: словари и множества",
    summary:
      "Создание, чтение, обновление, удаление; ключи и значения; обход; .get vs []; .setdefault; in; set-операции (union, intersection, difference). Когда выбирать что.",
    readingMinutes: 6,
    sections: [
      {
        heading: "Словарь — главная рабочая лошадка",
        tagline: "dict — таблица «ключ → значение» с очень быстрым поиском",
        body:
          "Создаётся `{}` или `dict(...)`. Доступ — `d[key]`. Обновление — `d[key] = value`. Удаление — `del d[key]`. Проверка — `key in d`.\n\n" +
          "Очень важная разница: `d[key]` падает **KeyError**, если ключа нет. А `d.get(key, default)` тихо вернёт default. Используй `.get` для опциональных полей.\n\n" +
          "Для накопления (например, считаем повторы) — `.setdefault(key, default)` или модуль `collections.defaultdict`.\n\n" +
          "Обход:\n\n" +
          "- `for key in d:` — по ключам;\n" +
          "- `for value in d.values():` — по значениям;\n" +
          "- `for key, value in d.items():` — пары.\n\n" +
          "Полезные методы:\n\n" +
          "- `.pop(key, default)` — удалить и вернуть (без default → KeyError при отсутствии).\n" +
          "- `.update(other)` — обновить из другого словаря или пар ключ-значение.\n" +
          "- `.copy()` — поверхностная копия.\n" +
          "- `{**a, **b}` или `a | b` (Py 3.9+) — слить два словаря в новый.",
        code:
          "user = {\"name\": \"Аня\", \"age\": 17}\n" +
          "print(user[\"name\"])               # 'Аня'\n" +
          "print(user.get(\"city\", \"—\"))     # '—' (нет ключа)\n\n" +
          "user[\"city\"] = \"Москва\"          # добавить\n" +
          "del user[\"age\"]                  # удалить\n\n" +
          "for k, v in user.items():\n" +
          "    print(f\"{k}: {v}\")\n\n" +
          "# Подсчёт повторов\n" +
          "counts = {}\n" +
          "for ch in \"banana\":\n" +
          "    counts[ch] = counts.get(ch, 0) + 1\n" +
          "print(counts)                    # {'b': 1, 'a': 3, 'n': 2}",
        keyTakeaways: [
          "d[key] — может упасть KeyError; d.get(key, default) — безопасный.",
          "Ключом может быть любой неизменяемый объект: str, int, tuple.",
          ".items() даёт пары (ключ, значение) — главный способ обхода.",
        ],
        pitfalls: [
          "Список не может быть ключом — он изменяемый. Tuple — может.",
          "Порядок вставки сохраняется (с Python 3.7+), но не путай dict с OrderedDict.",
          "При итерации по dict нельзя добавлять/удалять ключи — RuntimeError.",
        ],
        analogy:
          "Словарь — как настоящий бумажный словарь или телефонная книга: ты ищешь по имени (ключу) и мгновенно находишь номер (значение). Не нужно перебирать все страницы — поиск за O(1). `d.get(key, '—')` — это как: «если нет такого имени — верни прочерк».",
      },
      {
        heading: "Множества — для уникальности и быстрого поиска",
        tagline: "set — без повторов, без порядка, поиск за O(1)",
        body:
          "Создаётся `{1, 2, 3}` или `set(seq)`. Пустое множество — **`set()`**, не `{}` (это словарь!).\n\n" +
          "Когда брать set:\n\n" +
          "- нужно убрать дубликаты: `unique = set(items)`;\n" +
          "- нужно быстро проверять «есть ли элемент»: `x in s` за O(1);\n" +
          "- нужны теоретико-множественные операции: пересечение, объединение, разность.\n\n" +
          "Главные операции: `s | t` (объединение), `s & t` (пересечение), `s - t` (разность), `s ^ t` (симметрическая разность). Те же операции есть как методы: `.union`, `.intersection`, `.difference`.",
        code:
          "a = {1, 2, 3, 4}\n" +
          "b = {3, 4, 5, 6}\n\n" +
          "print(a | b)        # {1, 2, 3, 4, 5, 6}\n" +
          "print(a & b)        # {3, 4}\n" +
          "print(a - b)        # {1, 2}\n" +
          "print(a ^ b)        # {1, 2, 5, 6}\n\n" +
          "# Убрать дубликаты, сохранить тип list\n" +
          "items = [1, 2, 2, 3, 3, 3]\n" +
          "unique = list(set(items))\n" +
          "print(unique)       # порядок не гарантирован",
        keyTakeaways: [
          "set — уникальные элементы, очень быстрый in.",
          "{} — это dict, не set! Для пустого set — set().",
          "set теряет порядок. Если порядок важен — dict.fromkeys(seq) сохранит порядок и уберёт дубликаты.",
        ],
        pitfalls: [
          "В set нельзя класть изменяемые объекты (list, dict). Только хешируемые.",
          "set != frozenset. frozenset — неизменяемая версия, может быть ключом dict.",
        ],
        analogy:
          "Множество — как список гостей на вечеринке без дублей. «Аня» не может быть записана дважды. Пересечение (& ) — кто пришёл на обе вечеринки. Разность (-) — кто пришёл только на первую. Важно: список гостей без порядка — кто записан раньше, не помнит.",
      },
    ],
    cheatSheet: [
      "dict: d[k], d.get(k, default), d.setdefault(k, []), d.items(), d.keys(), d.values().",
      "set: {1,2}, set(), |, &, -, ^.",
      "Пустой set — set(), не {} (это dict).",
      "in для dict проверяет ключ; для set — наличие; и то, и другое — O(1).",
      "Ключи dict и элементы set должны быть неизменяемыми.",
    ],
  },
  fills: [
    {
      type: "fill",
      id: "j7-f1",
      title: "Подсчёт повторов",
      description: "Подсчитай количество повторов каждой буквы в строке.",
      code:
        "text = \"banana\"\n" +
        "counts = {}\n" +
        "for ch in text:\n" +
        "    counts[ch] = counts.{{0}}(ch, {{1}}) + 1\n" +
        "print(counts)   # {'b': 1, 'a': 3, 'n': 2}",
      answers: [["get"], ["0"]],
      hints: [
        "Метод словаря, который безопасно достаёт значение или default.",
        "Какое начальное значение для счётчика?",
      ],
      explanation: {
        summary:
          "Идиома подсчёта частот: для каждого ключа берём текущее значение (или 0, если нет) и прибавляем 1.",
        keyPoints: [
          ".get(key, default) — безопасный доступ.",
          "Альтернатива — collections.Counter(text).",
        ],
      },
    },
    {
      type: "fill",
      id: "j7-f2",
      title: "Множества",
      description: "Найди общих и уникальных учеников двух кружков.",
      code:
        "math = {\"Аня\", \"Боря\", \"Вера\"}\n" +
        "phys = {\"Боря\", \"Вера\", \"Гриша\"}\n\n" +
        "both = math {{0}} phys     # пересечение\n" +
        "either = math {{1}} phys   # объединение\n" +
        "only_math = math {{2}} phys  # разность\n\n" +
        "print(both, either, only_math)",
      answers: [["&"], ["|"], ["-"]],
      hints: [
        "Пересечение — побитовое И.",
        "Объединение — побитовое ИЛИ.",
        "Разность — обычный минус.",
      ],
      explanation: {
        summary:
          "Set-операции через символы — самый компактный способ. Те же действия есть и как методы.",
        keyPoints: [
          "& интерсекция, | юнион, - разность, ^ симметрическая.",
          "Все операции возвращают новый set, оригиналы не трогают.",
        ],
      },
    },
  ],
  questions: [
    {
      type: "question",
      id: "j7-q1",
      title: "Опасное чтение из словаря",
      question:
        "Какое исключение возникнет, если обратиться к несуществующему ключу как d[\"missing\"]?",
      answers: ["keyerror", "KeyError"],
      hint: "Имя исключения для отсутствующего ключа.",
      explanation:
        "KeyError. Чтобы избежать — используй d.get(key, default) или сначала проверяй `if key in d`.",
    },
    {
      type: "question",
      id: "j7-q2",
      title: "Пустой set",
      question:
        "Как создать пустое множество в Python? Напиши минимальный код.",
      answers: ["set()", "set ()"],
      hint: "{} — это уже занято под другую структуру.",
      explanation:
        "set() — это вызов конструктора. {} — это пустой словарь, не set. Нужно явно вызвать set().",
    },
  ],
  writes: [
    {
      type: "write",
      id: "j7-w1",
      title: "Уникальные слова в тексте",
      task:
        "Считай строку, разбей по пробелам, приведи к нижнему регистру и напечатай: общее число слов, количество уникальных слов и сами уникальные слова, отсортированные по алфавиту через запятую.",
      hints: [
        "words = input().lower().split() — нормализуем.",
        "len(words) — общее, len(set(words)) — уникальных.",
        "sorted(set(words)) — отсортированный список уникальных. Склеить через ', '.join(...).",
      ],
      required: ["input(", ".lower()", ".split()", "set(", "sorted(", "join("],
      minLines: 5,
      explanation: {
        summary:
          "Идеальная задача на set: убираем дубли, считаем, сортируем. lower() важно — иначе 'Слово' и 'слово' будут разными.",
        keyPoints: [
          "set(seq) убирает дубликаты.",
          "sorted работает с любым итерируемым.",
        ],
      },
    },
  ],
};

export const JUNIOR_ROUNDS: Round[] = [j1, j2, j3, j4, j5, j6, j7];
export const JUNIOR_TOTAL_ROUNDS = JUNIOR_ROUNDS.length;
