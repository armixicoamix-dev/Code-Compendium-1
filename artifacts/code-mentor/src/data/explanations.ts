import type { ExerciseExplanation } from "./curriculum";

/**
 * Detailed explanations shown to the learner AFTER they check their answer.
 * Keyed by exercise id (e.g. "r1-f1", "r3-w2"). Russian content.
 */
export const EXPLANATIONS: Record<string, ExerciseExplanation> = {
  // ------------------------------------------------------------------ ROUND 1
  "r1-f1": {
    summary:
      "Это самый базовый паттерн ООП: класс — это шаблон, экземпляр — конкретный объект, созданный по этому шаблону. Метод __init__ запускается автоматически при вызове Cat(...) и инициализирует атрибуты экземпляра. self — первый параметр любого метода экземпляра, это явная ссылка на «текущий объект».",
    keyPoints: [
      "class Cat: — объявление шаблона. Сам по себе он не объект, это «чертёж» из которого можно делать сколько угодно кошек.",
      "__init__(self, name, age, breed) — конструктор. Python зовёт его автоматически при barsik = Cat(...). Имена параметров (кроме self) — это то, что мы передаём в скобках.",
      "self.name = name записывает в каждый ОТДЕЛЬНЫЙ объект свои данные. У barsik и murka получаются разные self.name — это и есть разница между классом (общим) и экземпляром (личным).",
      "Атрибут is_full = False задаётся в __init__ заранее, чтобы у любой только что созданной кошки уже был этот флаг. Без этого первый вызов play() упал бы с AttributeError.",
      "feed() МЕНЯЕТ состояние объекта (self.is_full = True, self.mood = 'happy'). Поведение методов — это всегда работа с self.<что-то>.",
      "play() сначала ЧИТАЕТ состояние (self.is_full), а потом решает: играть или отказаться. Это и есть «бизнес-логика» класса.",
      "describe() возвращает строку (а не печатает), это полезно: вызывающий код сам решит, что с ней делать — print, logger, сохранить и т.п.",
    ],
    pitfalls: [
      "Часто забывают self перед атрибутом и пишут просто name = name — это создаёт локальную переменную, и снаружи name не будет.",
      "Имя метода __init__ — обязательно с двумя подчёркиваниями с обеих сторон. _init_, init или __init — всё неправильно.",
      "barsik.feed() без аргумента — ошибка, потому что feed требует food. self передаётся автоматически, остальные аргументы — нет.",
    ],
    realWorld:
      "Точно такой же паттерн ты будешь использовать для User, Product, Order, ApiClient — любого объекта со своим состоянием и поведением. Классы — основной способ моделировать сущности предметной области.",
  },

  "r1-f2": {
    summary:
      "Класс инкапсулирует не только данные (баланс), но и операции над ними. Здесь объект Сам отвечает за свою корректность: deposit/withdraw проверяют входные данные, а transfer — что вторая сторона тоже BankAccount. Это базовая идея инкапсуляции — спрятать логику внутри объекта, чтобы снаружи нельзя было повредить состояние.",
    keyPoints: [
      "owner и balance передаются в __init__. balance имеет default 0 — счёт можно создать без указания начального баланса.",
      "history хранится как СПИСОК внутри объекта. Каждый объект ведёт свой собственный журнал (это атрибут экземпляра, а не класса).",
      "_log() начинается с подчёркивания — соглашение Python: «это внутренний метод, не трогай его снаружи». Технически Python не запрещает, но программисты так не делают.",
      "isinstance(other, BankAccount) — защита от того, что кто-то передаст в transfer строку или число. Метод сам проверяет тип входных данных.",
      "transfer переиспользует withdraw и deposit — не пишет одно и то же дважды (DRY-принцип: Don't Repeat Yourself).",
      "self.balance += amount эквивалентно self.balance = self.balance + amount. Этот составной оператор работает в Python для всех чисел и для списков (там это +=).",
      "withdraw возвращает True/False, чтобы вызывающий код мог понять — прошло снятие или нет. transfer проверяет результат и не зачисляет, если не сняли.",
    ],
    pitfalls: [
      "Если положить history = [] в АТРИБУТ КЛАССА (не в __init__), все объекты разделят один и тот же список — это известный баг новичков.",
      "Без isinstance в transfer — другой объект с методом deposit мог бы «прикинуться» BankAccount. Проверка типа защищает от подмены.",
      "transfer без проверки результата withdraw приведёт к ситуации «у Анны деньги не сняли, но Ивану зачислили» — деньги «появятся из воздуха».",
    ],
    realWorld:
      "Реальные финтех-классы устроены так же: журнал операций, проверки на стороне модели, методы high-level (transfer), которые опираются на low-level (withdraw + deposit).",
  },

  "r1-f3": {
    summary:
      "Прямоугольник — классический пример геометрического класса. Здесь видна полезная закономерность: все математические свойства (площадь, периметр) лучше делать МЕТОДАМИ, которые ВЫЧИСЛЯЮТ значение, а не атрибутами. Тогда после scale() всё пересчитается автоматически.",
    keyPoints: [
      "Проверка width <= 0 or height <= 0 в __init__ — единственный момент, когда мы можем гарантировать корректность сторон. Если пустить туда мусор, потом починить будет нельзя.",
      "raise ValueError(...) — стандартный способ сообщить, что аргумент не подходит. Python остановит создание объекта и выбросит исключение.",
      "area() и perimeter() возвращают вычисляемое значение. Если бы мы хранили self.area отдельным атрибутом — после scale() он бы устарел.",
      "scale(factor) — пример МУТИРУЮЩЕГО метода: меняет состояние объекта (две стороны) одним вызовом.",
      "is_square() — пример «производного предиката»: чисто на основе других атрибутов решает True/False.",
      "__repr__ возвращает «техническое» представление объекта — то, что видно в REPL и в логах. По нему должно быть понятно, как объект был создан.",
    ],
    pitfalls: [
      "scale(2) у прямоугольника 4×5 даёт 8×10, а площадь увеличивается в 4 раза (а не в 2). Это нормально и проверяется тем, что area() пересчитывается.",
      "Отрицательный factor сделает стороны отрицательными — наша проверка только в __init__. В реальном коде стоит проверять и в scale().",
    ],
  },

  // ------------------------------------------------------------------ ROUND 2
  "r2-f1": {
    summary:
      "Здесь видно различие между атрибутами класса (общими для всех экземпляров) и атрибутами экземпляра (личными). species, count и all_dogs — общие, name и age — личные. classmethod получает не self, а cls — ссылку на сам класс, что позволяет работать с общим состоянием.",
    keyPoints: [
      "species = 'Canis familiaris' объявлен НА УРОВНЕ КЛАССА (с отступом класса, но не внутри метода). Все собаки разделяют это значение.",
      "Dog.count += 1 в __init__ обращается к атрибуту класса через ИМЯ КЛАССА. Если написать self.count += 1 — Python создаст у объекта свой собственный counter и общий не изменит.",
      "Dog.all_dogs.append(self) — собака сама регистрирует себя в общем реестре. Так класс «знает обо всех своих экземплярах».",
      "@classmethod превращает метод в метод КЛАССА. Первый параметр — cls, не self. Метод можно вызвать через Dog.how_many(), без создания экземпляра.",
      "В average_age мы перебираем cls.all_dogs (а не Dog.all_dogs) — это лучше, потому что в наследнике (например, ServiceDog(Dog)) cls будет указывать на правильный класс.",
      "reset_registry показывает, что classmethod может и МЕНЯТЬ состояние класса, не только читать.",
    ],
    pitfalls: [
      "Главная ловушка: написать `count = 0` внутри __init__. Тогда у каждой собаки появится СВОЙ counter=0, а общего вообще не будет.",
      "Если в __init__ написать self.count += 1, Python СНАЧАЛА прочитает Dog.count (наследование атрибутов), а ЗАПИШЕТ в self.count. Это создаёт «теневой» атрибут и общий счётчик не растёт.",
      "Изменяемые атрибуты класса (списки, словари) разделяются между всеми объектами. all_dogs — здесь это ХОРОШО, потому что мы именно общий реестр и хотим. Но как default-значение в __init__ (например, grades=[]) это плохо: все объекты получат один и тот же список.",
    ],
    realWorld:
      "Атрибуты класса используются для констант (MAX_LENGTH, DEFAULT_TIMEOUT), реестров всех созданных объектов (как тут) и счётчиков. classmethod — это альтернативные конструкторы и методы, работающие с коллекцией всех экземпляров.",
  },

  "r2-f2": {
    summary:
      "Инкапсуляция в Python работает через соглашения: один подчёркиваний (_x) означает «не трогай», два подчёркивания (__x) запускают механизм name mangling — Python переименует атрибут в _ClassName__x, чтобы избежать коллизий с подклассами. Это не «защита от взлома», а защита от случайного переопределения.",
    keyPoints: [
      "self.__balance — снаружи обращение через obj.__balance не работает (вернее, ищет атрибут с буквальным именем __balance, которого нет).",
      "На самом деле атрибут хранится под именем _SafeAccount__balance — это и есть name mangling. Доступ через это имя работает, но это явно «я лезу в чужое».",
      "_transactions с одним подчёркиванием — это просто соглашение «не трогай», без переименования. Подкласс может его переопределить и сломать.",
      "transactions() возвращает self._transactions.copy() — защита от того, что внешний код модифицирует наш внутренний список. Если бы вернули self._transactions, кто-то снаружи мог бы сделать .clear() и убить нашу историю.",
      "raise ValueError в deposit/withdraw — единый стиль ошибок для невалидных входов.",
      "В __repr__ через {self.__balance} сработает потому, что это ВНУТРИ класса SafeAccount — name mangling действует и в строке кода метода.",
    ],
    pitfalls: [
      "Двойное подчёркивание создано НЕ для безопасности (любой может прочитать через _ClassName__attr). Цель — избежать конфликтов имён в иерархии классов.",
      "Если ты НЕ планируешь сложную иерархию — используй один подчёркивание. Двойное усложняет отладку и интроспекцию.",
      "Если бы transactions() возвращал self._transactions без copy(), то acc.transactions().clear() стёр бы всю историю. Это «утечка изменяемого состояния».",
    ],
    realWorld:
      "В библиотеках чаще встречается одиночный подчёркивание (Django, Flask, requests). Двойной — в основном в фреймворках с глубокими иерархиями классов (например, SQLAlchemy внутри).",
  },

  "r2-f3": {
    summary:
      "Класс может предоставлять НЕСКОЛЬКО способов создания объекта — основной __init__ и альтернативные классовые методы (from_string, today, from_iso). Это классический паттерн «фабричных методов». staticmethod нужен для чисто вспомогательных функций, которым не нужны ни self, ни cls.",
    keyPoints: [
      "@classmethod from_string(cls, s) принимает строку и парсит её в день/месяц/год. Возвращает cls(d, m, y) — это создаёт ОБЫЧНЫЙ экземпляр через __init__, со всеми его проверками.",
      "Использование cls вместо имени класса (Date) важно: если кто-то отнаследует SuperDate(Date), то SuperDate.from_string('...') вернёт SuperDate, а не Date. Полиморфизм работает «правильно».",
      "today() — пример classmethod, который не получает входных данных вообще, но создаёт объект из текущего времени.",
      "is_valid — staticmethod: ему не нужны ни self, ни cls, потому что он работает только с переданными аргументами. По смыслу он принадлежит классу Date (логически связан), но технически это просто функция в неймспейсе класса.",
      "to_iso использует строковый формат :04d / :02d — это форматирование числа с ведущими нулями: 4 — минимум 4 символа, 02 — минимум 2 (2025-04-12).",
      "В __init__ мы вызываем Date.is_valid(...) — статический метод можно вызвать как через имя класса, так и через self.",
    ],
    pitfalls: [
      "Если в from_string написать Date(d, m, y) вместо cls(d, m, y), наследники сломаются — у них этот метод будет создавать родителя.",
      "is_valid у нас принимает три отдельных аргумента, а не self. Это правильный признак, что метод должен быть @staticmethod.",
      "from_string('31-13-2025') пройдёт парсинг, а потом __init__ вызовет ValueError из is_valid. Так и задумано — провал должен быть громким.",
    ],
    realWorld:
      "datetime.datetime.now(), datetime.date.today(), Path.cwd(), dict.fromkeys() — всё это classmethod-фабрики из стандартной библиотеки. Это очень распространённый паттерн.",
  },

  // ------------------------------------------------------------------ ROUND 3
  "r3-f1": {
    summary:
      "@property превращает метод в «вычисляемый атрибут» — ты обращаешься к temp.celsius без скобок, как к обычному полю, но за этим стоит код. С помощью @celsius.setter можно перехватить присвоение и провалидировать. Это позволяет ИЗМЕНИТЬ внутреннее представление, не ломая старый код.",
    keyPoints: [
      "В __init__ self.celsius = celsius выглядит как обычное присвоение, но реально вызывается СЕТТЕР, который проверит «не ниже абсолютного нуля».",
      "@property def celsius(self) — геттер. Возвращает _celsius (с одним подчёркиванием — внутреннее поле).",
      "@celsius.setter def celsius(self, value) — сеттер. Принимает value, проверяет и пишет в _celsius. Без сеттера свойство было бы read-only.",
      "fahrenheit — ВЫЧИСЛЯЕМОЕ свойство. Хранится только celsius, а fahrenheit считается на лету. Так данные всегда консистентны.",
      "У fahrenheit ТОЖЕ есть setter — он переводит F в C и пишет ТУДА. То есть в системе только одна «правда» — celsius.",
      "kelvin — read-only (нет setter), потому что мы не дали возможность установить.",
      "В __repr__ обращение к self.celsius вызывает геттер, всё работает прозрачно.",
    ],
    pitfalls: [
      "Если в setter написать self.celsius = value (рекурсивно через свойство) — Python зациклится и упадёт с RecursionError.",
      "Имя _celsius (с подчёркиванием) и celsius (свойство) — это РАЗНЫЕ вещи. Один подчёркивание — приватное хранилище, без — публичный API.",
      "Если убрать @celsius.setter — свойство станет read-only и temp.celsius = 100 упадёт с AttributeError.",
    ],
    realWorld:
      "Property — главный способ начать с простого атрибута, а потом добавить логику без поломки кода. Тысячи библиотек: Django ORM, SQLAlchemy, pydantic — везде используют property.",
  },

  "r3-f2": {
    summary:
      "Наследование позволяет не переписывать общее поведение. Animal знает только то, что общее у всех (name, age, describe), а каждое животное добавляет своё (breed у собаки, indoor у кошки). super().__init__(...) вызывает родительский конструктор, чтобы общая часть инициализировалась.",
    keyPoints: [
      "class Dog(Animal): — синтаксис «Dog НАСЛЕДУЕТСЯ от Animal». Dog получает все методы и поля Animal автоматически.",
      "super().__init__(name, age) — вызов __init__ родителя. Это нужно делать ВСЕГДА в начале наследника, иначе name и age не запишутся.",
      "raise NotImplementedError в Animal.sound() — сигнал «этот метод нужно переопределить в наследнике». Это «полу-абстрактный» способ; настоящий — через ABC (раунд 4).",
      "type(self).__name__ в describe — мощный приём: он берёт ИМЯ ТЕКУЩЕГО класса (Dog/Cat/Bird), даже если метод describe вызывается на наследнике. Полиморфизм во всей красе.",
      "Каждый наследник переопределяет sound() по-своему — это и есть полиморфизм: один и тот же интерфейс, разное поведение.",
      "В цикле for a in zoo: a.sound() Python САМ выбирает, какую версию sound() вызвать, в зависимости от типа объекта. Тебе не нужны if-else.",
    ],
    pitfalls: [
      "Если забыть super().__init__() в наследнике — атрибуты name и age не установятся, и при первом обращении к ним будет AttributeError.",
      "super() работает только если класс наследуется от Animal явно. Без указания (Animal) в скобках Python наследует от object и super() обратится к нему.",
      "Если переопределить sound() с другой сигнатурой (например, sound(self, volume)) — полиморфизм сломается там, где код вызывает a.sound() без аргументов.",
    ],
    realWorld:
      "Иерархия Vehicle → Car → ElectricCar, Exception → ValueError → ZeroDivisionError, View → ListView → APIListView (Django) — везде используется ровно этот паттерн.",
  },

  "r3-f3": {
    summary:
      "Когда наследник РАСШИРЯЕТ родительское поведение (а не заменяет полностью), удобно вызвать super().method() в первой же строке и потом ДОБАВИТЬ что-то своё. Здесь Manager сначала повышает зарплату как обычный сотрудник, а потом ещё бонус.",
    keyPoints: [
      "В Manager.give_raise сначала идёт new_salary = super().give_raise(percent). Этим мы ПЕРЕИСПОЛЬЗУЕМ родительскую логику расчёта зарплаты.",
      "А затем ДОПОЛНИТЕЛЬНО self.bonus += ... — это уже специфика менеджера. Так мы не дублируем код Employee.give_raise, но добавляем своё.",
      "В info() тоже используется super().info() и потом дописывается ', +бонус, команда: N'. Это паттерн «обогащения вывода».",
      "team — список других сотрудников. Это пример КОМПОЗИЦИИ внутри наследования: Manager одновременно ЯВЛЯЕТСЯ сотрудником (наследование) и ИМЕЕТ команду (композиция).",
      "isinstance(employee, Employee) защищает invariant: в команду нельзя добавить случайный объект. Менеджер тоже Employee, поэтому Manager в команду тоже пройдёт.",
      "total_payroll показывает мощь композиции: один метод считает фонд по всем подчинённым, без копи-пасты кода.",
    ],
    pitfalls: [
      "Если в Manager.give_raise забыть про super() и написать self.salary += ... — будет дубль логики родителя, и потом сложно поддерживать.",
      "team=None и team=[] в default — НЕЛЬЗЯ ставить team=[]! Все менеджеры разделят один и тот же список. Поэтому используем `team=None` и потом `team or []`.",
      "Manager НАСЛЕДУЕТСЯ от Employee, поэтому isinstance(manager, Employee) тоже True. Менеджер сам может быть в команде другого менеджера.",
    ],
    realWorld:
      "Иерархии User → AdminUser, Order → SubscriptionOrder, Request → ApiRequest — повсюду переопределяют методы с вызовом super(), чтобы добавить свою логику поверх базовой.",
  },

  // ------------------------------------------------------------------ ROUND 4
  "r4-f1": {
    summary:
      "ABC (Abstract Base Class) — это формальный КОНТРАКТ. Класс Shape объявляет, что любой наследник ОБЯЗАН иметь area() и draw(). Если не реализуешь — Python даже не даст создать объект. Это превращает «можно забыть переопределить» в «компилятор не пропустит».",
    keyPoints: [
      "from abc import ABC, abstractmethod — стандартный модуль для абстрактных классов.",
      "class Shape(ABC) — наследование от ABC помечает класс как абстрактный.",
      "@abstractmethod перед методом делает его обязательным к реализации в подклассе. Если метод не реализован — Shape() выбросит TypeError.",
      "describe() — НЕабстрактный метод: общий для всех фигур. Все наследники получают его «бесплатно». Он опирается на полиморфные area() и name.",
      "Каждый наследник (Circle, Rectangle, Triangle) реализует ОБА абстрактных метода. После этого его можно создавать.",
      "В цикле for s in shapes: s.area() Python вызывает РАЗНУЮ area() для каждого типа фигуры — без if-else, через полиморфизм.",
      "Попытка Shape('кривое') — TypeError с сообщением «Can't instantiate abstract class». Контракт работает.",
    ],
    pitfalls: [
      "Если в наследнике переопределить только ОДИН из абстрактных методов — Python всё ещё не даст создать экземпляр. Нужно реализовать ВСЕ.",
      "Можно создать промежуточный абстрактный класс (например, RegularPolygon(Shape) с реализованной area, но новым abstractmethod sides()). Тогда RegularPolygon тоже остаётся абстрактным.",
      "@abstractmethod должен идти НЕПОСРЕДСТВЕННО перед def — без других декораторов между ними (или со специальной комбинацией @property + @abstractmethod).",
    ],
    realWorld:
      "Любой плагинный API устроен через ABC: пишешь базовый PaymentProvider, и каждая компания реализует его по-своему (StripeProvider, PayPalProvider). Главный код работает с любым через полиморфизм.",
  },

  "r4-f2": {
    summary:
      "Storage — формальный интерфейс хранилища. InMemoryStorage и FileStorage — две разные реализации одного контракта. Функция use_storage не знает, КАКОЕ перед ней хранилище — она просто полагается на интерфейс. Это и есть полиморфизм + dependency injection в одном флаконе.",
    keyPoints: [
      "Storage(ABC) с тремя @abstractmethod: get, set, delete. Любая реализация ОБЯЗАНА предоставить все три.",
      "has() — НЕабстрактный, на основе абстрактного get(). Это «миксин-логика»: каждая реализация бесплатно получает has(), которая работает через её собственный get().",
      "InMemoryStorage хранит данные в обычном dict. KeyError при отсутствующем ключе — стандартная семантика Python.",
      "FileStorage добавляет префикс к каждому ключу через _full(). Это инкапсулировано — снаружи не видно.",
      "use_storage(s: Storage) — type hint. Технически Python это не проверяет, но IDE и mypy подсветят, если передать что-то не то.",
      "Storage() — TypeError. Абстрактный класс инстанцировать нельзя. Это и есть «защита контракта».",
      "_full с подчёркиванием — внутренний метод FileStorage. Он не часть контракта Storage, поэтому к нему нельзя обращаться через Storage-интерфейс.",
    ],
    pitfalls: [
      "Можно случайно «переопределить» сигнатуру метода в реализации — например, def set(self, key, value, ttl=0). С точки зрения контракта это уже расширение, и use_storage заработает, но код становится менее предсказуемым.",
      "В FileStorage.delete мы используем self._data.pop(full, None) — это безопасно: если ключа нет, ничего не падает. В InMemoryStorage.delete мы проверяем сначала. Оба варианта правильные, но РАЗНЫЕ.",
      "Если в has() поймать слишком широкое исключение (Exception), можно «съесть» баг в get(). Поэтому ловим именно KeyError.",
    ],
    realWorld:
      "Точно так же устроены кэши, ORM-бэкенды, файловые системы (os, pathlib) — единый интерфейс, разные имплементации. Это позволяет писать тесты с in-memory имплементацией, а в проде использовать настоящую БД.",
  },

  "r4-f3": {
    summary:
      "Множественное наследование — мощный, но опасный инструмент. MRO (Method Resolution Order) — алгоритм, по которому Python ищет метод в иерархии. Миксины — паттерн «маленьких классов, добавляющих одно поведение». Loggable добавляет .log(), Serializable — .to_json().",
    keyPoints: [
      "User(Entity, Loggable, Serializable) — наследование от ТРЁХ классов. Порядок важен!",
      "super().__init__(id_) идёт по MRO: User → Entity → object. То есть super() в User вызовет Entity.__init__.",
      "Loggable и Serializable — миксины: маленькие классы без своего __init__ и без полей, добавляющие методы. Они не должны быть полноценными родителями.",
      "User.__mro__ — порядок поиска методов. Если в нескольких родителях есть метод с одним именем, Python берёт ПЕРВЫЙ по MRO.",
      "Алмаз (D(B, C), B(A), C(A)) — классический случай. Без MRO непонятно, какой класс победит. Python через C3-линеаризацию даёт детерминированный ответ: D → B → C → A → object.",
      "self.__dict__ — словарь со всеми атрибутами экземпляра. json.dumps умеет его сериализовать, если внутри простые типы.",
      "type(self).__name__ в Loggable.log — берёт имя ТЕКУЩЕГО класса (User), даже хотя метод определён в Loggable.",
    ],
    pitfalls: [
      "Если User(Loggable, Entity), а у Loggable есть свой __init__ — super().__init__(id_) пойдёт в Loggable, а не в Entity. Порядок реально важен.",
      "Миксины НЕ должны содержать __init__ с обязательными параметрами — иначе сложно вписать в MRO. Если нужно — пиши __init__(self, *args, **kwargs) и прокидывай super().__init__(*args, **kwargs).",
      "Алмазное наследование без super() (с явным B.__init__(self) и C.__init__(self)) приводит к двойному вызову общего родителя A. Поэтому везде super().",
    ],
    realWorld:
      "Django CBV (Class-Based Views) — массово используют миксины: LoginRequiredMixin, PermissionRequiredMixin, JsonResponseMixin. Ты комбинируешь их с базовыми View, чтобы получить готовое поведение.",
  },

  // ------------------------------------------------------------------ ROUND 5
  "r5-f1": {
    summary:
      "Магические методы (dunder, double underscore) — способ сделать твой класс похожим на встроенные типы. Vector с __add__, __sub__, __mul__ работает с операторами +, -, *. С __eq__ и __hash__ его можно класть в set и сравнивать. С __abs__ работает встроенная abs().",
    keyPoints: [
      "__add__(self, other) вызывается при self + other. Должен вернуть НОВЫЙ объект, не модифицировать self (иначе a + b изменит a).",
      "__mul__(self, k) — умножение на скаляр СПРАВА: vector * 2. Если k не число — возвращаем NotImplemented (специальная константа), и Python попробует поискать __rmul__ у второго операнда.",
      "__rmul__(self, k) — умножение СЛЕВА: 2 * vector. Без него 2 * vector упадёт с TypeError, потому что int.__mul__ не знает Vector.",
      "__eq__(self, other) — оператор ==. Должен возвращать True/False, и желательно проверять isinstance, чтобы Vector(1,2) == 'строка' давало False, а не падало.",
      "__hash__ ОБЯЗАТЕЛЕН, если ты определил __eq__ — иначе hash(vector) перестанет работать и vector нельзя класть в set/dict-key. Базируется на тех же полях, что и __eq__.",
      "__abs__ → встроенная abs(). __neg__ → унарный минус. __repr__ → repr() и REPL-вывод. __str__ → str() и print().",
      "Если __str__ не определён, print() использует __repr__. Поэтому для отладки __repr__ важнее.",
    ],
    pitfalls: [
      "__eq__ без __hash__ ломает использование объекта в set/dict. Если хочешь явно запретить хэширование — поставь __hash__ = None.",
      "isinstance(other, Vector) в __eq__ важен. Без него Vector(1,2) == 'строка' попытается сравнить self.x == 'строка'.x — упадёт с AttributeError.",
      "__add__ должен возвращать новый Vector, а не self. Иначе a + b модифицирует a (это сломает immutability и удивит пользователя).",
      "NotImplemented (без скобок) — это значение. NotImplementedError — это ИСКЛЮЧЕНИЕ. В __mul__ нужно именно `return NotImplemented`, не `raise`.",
    ],
    realWorld:
      "decimal.Decimal, fractions.Fraction, numpy.array, pandas.Series — везде определяют десятки dunder-методов, чтобы вести себя как числа/массивы. Это и есть идиоматичный дизайн API Python.",
  },

  "r5-f2": {
    summary:
      "Чтобы твой объект работал как контейнер Python (с len(), for, [], in), нужно реализовать соответствующие dunder-методы: __len__, __iter__, __getitem__, __contains__. Это превращает LinkedList из «каркаса с методами» в полноправный гражданин Python.",
    keyPoints: [
      "__len__ → len(obj). Должен возвращать неотрицательное int. У нас это _size — счётчик, обновляющийся в append.",
      "__iter__ — итератор. Простейший способ — генератор: while/for + yield. Каждый yield отдаёт следующее значение, потом функция «приостанавливается» до следующего шага.",
      "__getitem__(self, index) → obj[index]. Поддержка отрицательных индексов: index += len(self). Без этого list[-1] не работало бы.",
      "__contains__(self, value) → value in obj. Если не определить — Python использует __iter__ как fallback. Но явно — быстрее и нагляднее.",
      "__repr__ ИСПОЛЬЗУЕТ self в качестве итерируемого: ', '.join(repr(v) for v in self). Это работает потому, что мы определили __iter__.",
      "__slots__ = ('value', 'next') в _Node — экономит память (не создаёт __dict__ для каждой ноды). Полезно, когда нод тысячи.",
      "raise IndexError(index) — стандартное исключение для выхода за границы. list тоже его выбрасывает.",
    ],
    pitfalls: [
      "Если __iter__ возвращает self (вместо генератора), нужно ещё __next__ и контроль состояния. Генератор (yield) проще и не требует ничего лишнего.",
      "__getitem__ без проверки диапазона приведёт к бесконечному циклу или AttributeError на cur.next, если cur уже None.",
      "После __iter__ для одного цикла генератор «исчерпан» — повторный for создаст НОВЫЙ итератор (потому что __iter__ возвращает новый каждый раз).",
      "len(LinkedList()) == 0, и в Python это считается False. Если хочешь нестандартное поведение «пустой список — True» — определи __bool__.",
    ],
    realWorld:
      "collections.deque, OrderedDict, любой ORM-QuerySet, pandas.Series — все они реализуют контейнерные dunder-методы, чтобы поддерживать привычный синтаксис len/for/in.",
  },

  "r5-f3": {
    summary:
      "Финал: композиция (Bank ВЛАДЕЕТ счетами, Account ВЛАДЕЕТ транзакциями) + dataclasses (автогенерация __init__, __repr__, __eq__) + frozen=True для иммутабельных Transaction. Это профессиональный архитектурный паттерн небольшого финансового модуля.",
    keyPoints: [
      "@dataclass(frozen=True) для Transaction делает её НЕИЗМЕНЯЕМОЙ. Раз создал — никто не подменит amount или дату. Это критично для финансовых данных (audit trail).",
      "field(default_factory=datetime.now) — фабрика default-значений. Каждая транзакция получает СВОЁ время. Если бы мы написали at: datetime = datetime.now() — все транзакции получили бы одно и то же время момента импорта модуля!",
      "@dataclass без frozen — для Account. Он МУТИРУЕТ список транзакций, поэтому frozen ему не подходит.",
      "balance() считается по сумме транзакций — единственный источник правды. Нет отдельного поля balance, которое могло бы рассинхронизироваться.",
      "Withdraw добавляет ОТРИЦАТЕЛЬНУЮ транзакцию (-amount). Так в журнале всё видно: и приход, и расход.",
      "Bank — композиция: dict[str, Account]. Banque не наследуется от Account, он его ИСПОЛЬЗУЕТ.",
      "transfer переиспользует withdraw + deposit обоих счетов. Если в withdraw упадёт ValueError — deposit не выполнится. Атомарность через исключения.",
    ],
    pitfalls: [
      "Если бы field(default_factory=list) забыли в transactions — Python бы НЕ ДАЛ компилировать (изменяемый default в dataclass запрещён, защита от того самого бага).",
      "frozen=True у Transaction означает, что поменять что-либо нельзя даже внутри методов. Если нужна модификация — создавай новую через dataclasses.replace.",
      "В реальной финансовой системе transfer должна быть АТОМАРНОЙ (всё или ничего). Здесь у нас она «последовательная» — если withdraw сработал, а deposit упал, у нас минус деньги без зачисления. В проде это бы решали через транзакции БД.",
      "balance() пересчитывается КАЖДЫЙ ВЫЗОВ. Для миллионов транзакций это медленно — кэшировать или хранить snapshot.",
    ],
    realWorld:
      "Так устроены event-sourcing системы: состояние выводится из журнала событий, а не хранится отдельно. dataclass(frozen=True) — стандарт для DTO, событий, value objects.",
  },

  // -------------------------------------------------- ROUND 1 — WRITE TASKS
  "r1-w1": {
    summary:
      "Этот класс закрепляет базовый паттерн: __init__ принимает данные, методы их используют и/или меняют. Person — простейшая модель «сущности» (entity) — то, чем чаще всего занимается ООП в реальной жизни.",
    keyPoints: [
      "В __init__ обязательно сохрани ВСЕ параметры в self.<атрибут>. Без self.* их потом никак не достать.",
      "greet() ничего не возвращает — просто print. Это «процедурный» метод. bio() ВОЗВРАЩАЕТ строку — «функциональный».",
      "Различие важно: возвращающий метод можно использовать в выражениях (logger.info(p.bio())), а печатающий — нельзя.",
      "have_birthday() МУТИРУЕТ состояние (self.age += 1). После вызова объект изменился навсегда.",
      "f-строки (f'Привет, я {self.name}') — самый удобный способ форматирования с Python 3.6+. Раньше использовали .format() или %.",
      "Создание двух объектов и вызов методов на каждом — демонстрация, что ОДИН класс порождает РАЗНЫЕ независимые экземпляры.",
    ],
    pitfalls: [
      "Не путай self.age = age (запись) с self.age == age (сравнение). Первое — нужно в __init__, второе — просто проверка.",
      "have_birthday не должен принимать новый возраст параметром — он сам его увеличивает.",
      "bio() НЕ должен print-ить, иначе нельзя будет, например, сохранить эту строку в файл.",
    ],
  },

  "r1-w2": {
    summary:
      "Counter — отличный пример объекта с СОСТОЯНИЕМ и ИСТОРИЕЙ. Здесь видно, что атрибуты класса могут быть простыми (число) и сложными (список). Метод reset() показывает, что объект может полностью «перезапустить» себя.",
    keyPoints: [
      "self.value = 0 в __init__ — стартовое состояние. Без этого первый increment упал бы с AttributeError.",
      "self.history = [] — пустой список В __init__. КАЖДЫЙ Counter получит СВОЙ список.",
      "by=1 — параметр со значением по умолчанию. counter.increment() работает, и counter.increment(5) тоже.",
      "self.history.append(f'+{by}') — добавляем строку в журнал. f-строка с подстановкой.",
      "show() — метод вывода. По умолчанию печатает (а не возвращает) — это нормально для метода-«отчёта».",
      "Демонстрация в конце — это «сценарий», который показывает, что класс работает по задумке.",
    ],
    pitfalls: [
      "САМАЯ ЧАСТАЯ ОШИБКА: положить history = [] на уровень класса, а не в __init__. Тогда все Counter будут разделять один список.",
      "increment без проверки by < 0 — позволяет «увеличить» на отрицательное число. В реальном коде нужно решить, можно ли так.",
      "reset() обнуляет value, но НЕ history. Спорный выбор — возможно, история должна сохраняться даже после reset (это и есть «журнал»). У нас по ТЗ — добавляется запись 'reset'.",
    ],
  },

  "r1-w3": {
    summary:
      "Book + цикл — переход от ОДНОГО объекта к КОЛЛЕКЦИИ объектов. Это типичная задача в реальных программах: список книг, заказов, пользователей, сообщений. Полиморфизм цикла: один и тот же info() работает для всех книг.",
    keyPoints: [
      "Класс Book хранит данные одной книги — это «структура с поведением».",
      "info() возвращает строку — позволяет печатать или сохранять её отдельно.",
      "is_long() — предикат, возвращает True/False. Имена методов-предикатов часто начинаются с is_ или has_.",
      "books = [Book(...), Book(...), Book(...)] — список объектов. Список может содержать что угодно.",
      "for b in books: — ИТЕРАЦИЯ по списку. b — поочерёдно каждая книга.",
      "Условная ветвь if b.is_long(): print(...) — отбираем «длинные» книги без отдельного списка.",
    ],
    pitfalls: [
      "info() должен RETURN-ить, а не print. Иначе print(b.info()) напечатает None в конце (потому что print без return возвращает None).",
      "Сравнение pages > 300 vs pages >= 300 — мелочь, но важна (граничный случай).",
      "Если поле года называется year, а в ТЗ написано {year} — внимательно следи за именами в f-строках.",
    ],
  },

  // -------------------------------------------------- ROUND 2 — WRITE TASKS
  "r2-w1": {
    summary:
      "Student с реестром группы — твой первый «реальный» класс с classmethod, staticmethod и общим состоянием. Это паттерн «класс знает обо всех своих экземплярах», который используется в системах, где нужна аналитика по всем объектам.",
    keyPoints: [
      "Student.count и Student.registry — атрибуты КЛАССА. Один общий счётчик и один общий список на всех студентов.",
      "В __init__ Student.count += 1 и Student.registry.append(self) — каждый новый студент автоматически регистрируется.",
      "average() — instance-метод, работает с self.grades. Защита от пустого списка через if not self.grades: return 0.",
      "@classmethod total_students(cls) — возвращает cls.count. Использование cls (а не Student) — правильно для наследников.",
      "@classmethod best_student(cls) — пример агрегатной функции по всем экземплярам. max(..., key=lambda s: s.average()).",
      "@staticmethod is_passing(avg) — pure-функция: ни self, ни cls не нужны. Просто помещается в неймспейс класса для логической связности.",
    ],
    pitfalls: [
      "registry на уровне класса — здесь это И ЕСТЬ задумка. Все студенты разделяют общий реестр. Но grades=[] на уровне __init__ — у каждого свой.",
      "best_student() на пустом registry упадёт с ValueError из max(). Можно добавить if not cls.registry: return None.",
      "is_passing(avg) — staticmethod, потому что нет операций с self/cls. classmethod был бы избыточен.",
    ],
  },

  "r2-w2": {
    summary:
      "Temperature с скрытым __celsius — закрепление инкапсуляции через name mangling и валидации. Это паттерн «единый источник правды»: все свойства считаются от одного скрытого поля.",
    keyPoints: [
      "self.__celsius (два подчёркивания) реально станет self._Temperature__celsius. Это name mangling — Python сам переименовывает.",
      "В __init__ обычно вызывают self.set_celsius(initial), чтобы пройти валидацию. Иначе можно было бы создать объект с −1000°C.",
      "set_celsius проверяет value >= -273.15 (абсолютный ноль) и raise ValueError при нарушении. Это «защита инварианта».",
      "set_fahrenheit ПЕРЕИСПОЛЬЗУЕТ set_celsius: переводит F в C и пишет туда. Один источник правды.",
      "get_fahrenheit() и get_kelvin() — вычисляемые getters. Хранится только celsius, остальные считаются на лету.",
      "__repr__ возвращает строку 'Temperature(<celsius>°C)' — техническое представление.",
    ],
    pitfalls: [
      "raise ValueError — без скобок не сработает. Должно быть raise ValueError('сообщение').",
      "Формула F → C: (F - 32) * 5/9, а C → F: C * 9/5 + 32. Часто путают (5/9 vs 9/5).",
      "Если в __init__ написать self.__celsius = initial напрямую — пройдёт мимо валидации. Лучше всегда через сеттер.",
    ],
  },

  "r2-w3": {
    summary:
      "Calculator с staticmethod и журналом — паттерн «утилитного класса». Создавать экземпляры не нужно: Calculator.add(2, 3) вызывается прямо на классе. История ведётся как атрибут класса — общая для всех вызовов.",
    keyPoints: [
      "Все 4 операции — @staticmethod. Им не нужен ни self, ни cls — они работают только с переданными числами.",
      "Calculator.history — атрибут класса. Один общий журнал на весь класс. Внутри статических методов обращаемся через имя класса.",
      "ZeroDivisionError — стандартное Python-исключение для деления на ноль. raise ZeroDivisionError('сообщение').",
      "После каждой операции — Calculator.history.append(f'add({a}, {b}) = {result}'). Запись вида 'операция(аргументы) = результат'.",
      "@classmethod last(cls, n) — возвращает cls.history[-n:]. Здесь нужен cls (или классовое имя), чтобы прочитать список.",
      "Использование без экземпляра: Calculator.add(2, 3) — никогда не вызываем Calculator(). Это «namespace для функций».",
    ],
    pitfalls: [
      "Если внутри @staticmethod написать self.history — будет NameError, self не определён. Нужно Calculator.history.",
      "div(a, 0) должен поднимать исключение СНАЧАЛА, до записи в журнал. Иначе в журнале появится несостоявшаяся операция.",
      "@staticmethod не наследуется так гибко, как @classmethod. Если кто-то сделает SuperCalc(Calculator).add(...), то Calculator.history (по имени) не «увидит» подкласс. Поэтому в реальном коде, если нужна полиморфия, лучше @classmethod.",
    ],
  },

  // -------------------------------------------------- ROUND 3 — WRITE TASKS
  "r3-w1": {
    summary:
      "Vehicle → Car — твой первый собственный наследник с super(). Здесь видно, как наследник РАСШИРЯЕТ родителя: сохраняет общие поля через super().__init__, добавляет своё, и обогащает родительский метод describe.",
    keyPoints: [
      "В Car.__init__ — super().__init__(brand, max_speed) ОБЯЗАТЕЛЬНО, чтобы поля родителя установились.",
      "Затем self.fuel_type = fuel_type — уникальное для Car поле.",
      "Car.describe() вызывает родительский метод через super().describe() и склеивает строку. Это «расширение» (extension) поведения.",
      "Без super() в describe — пришлось бы дублировать логику родителя. С super() — переиспользование.",
      "В сцене сначала vehicle = Vehicle(...), потом car1 = Car(...), car2 = Car(...). У всех вызывается describe — у каждого получается своя строка.",
    ],
    pitfalls: [
      "super().__init__ нужен ДО self.fuel_type. Если наоборот — родительский __init__ может перезаписать что-то твоё. Здесь не критично, но привычка важна.",
      "Если забыть наследование (class Car без (Vehicle)) — super() будет работать с object, не с Vehicle.",
      "Car.describe() без super() — забыл, что это РАСШИРЕНИЕ родительского. Это легальный способ «полностью переопределить», но обычно нужно именно расширить.",
    ],
  },

  "r3-w2": {
    summary:
      "Account с @property balance — ключевой паттерн инкапсуляции через свойства. Снаружи это выглядит как обычное поле, но внутри валидация. Главный приём — использовать СВОЙСТВО (а не _balance) даже внутри класса в deposit/withdraw.",
    keyPoints: [
      "@property def balance(self): return self._balance — getter, доступен как обычный атрибут.",
      "@balance.setter — перехватывает присвоение. raise ValueError при отрицательном значении — защита инварианта.",
      "deposit/withdraw делают self.balance += amount (а не self._balance). Это вызывает СЕТТЕР и валидацию.",
      "В __init__: self.balance = initial — тоже через сеттер, чтобы стартовое значение прошло валидацию.",
      "try/except ValueError в сцене — демонстрирует, что попытка установить отрицательное значение действительно вызовет исключение.",
      "Account имеет ОДИН публичный API (balance), под которым прячется приватное хранилище (_balance). Это и есть инкапсуляция.",
    ],
    pitfalls: [
      "Внутри сеттера написать self.balance = value — РЕКУРСИЯ! Снова вызовется сеттер, и так до RecursionError. Нужно self._balance = value.",
      "Если deposit использует self._balance += amount — обходит валидацию. Лучше self.balance += amount, чтобы все правила соблюдались всегда.",
      "Без @balance.setter свойство будет read-only. Попытка acc.balance = 100 упадёт с AttributeError.",
    ],
  },

  "r3-w3": {
    summary:
      "Иерархия Shape → Circle/Square с полиморфной функцией total_area — это классический пример «полиморфизма через единый интерфейс». Функция работает с ЛЮБЫМИ фигурами, лишь бы у них был метод area().",
    keyPoints: [
      "Shape с raise NotImplementedError в area() — «полу-абстрактный» способ. Если кто-то унаследуется и забудет area, при первом вызове упадёт.",
      "Circle и Square вызывают super().__init__('круг'/'квадрат'), чтобы поле name установилось через родителя.",
      "Площадь круга — math.pi * radius ** 2. import math обязательно.",
      "total_area(shapes) — обычная ФУНКЦИЯ, не метод. Принимает любую коллекцию фигур. Использует sum(s.area() for s in shapes) — generator expression.",
      "В цикле for s in shapes: print(s.area()) — для каждой фигуры вызывается СВОЯ area(). Полиморфизм в действии.",
      "Это паттерн Strategy: единый интерфейс, разные реализации, клиентский код их не различает.",
    ],
    pitfalls: [
      "Забыть math.pi и написать просто 3.14 — менее точно. math.pi даёт максимальную точность.",
      "В Square писать area = side * side — это атрибут, а не метод. Должно быть def area(self): return self.side * self.side.",
      "total_area в виде метода Shape — неправильно, потому что метод работает с self (одной фигурой), а нам нужна сумма по списку.",
    ],
  },

  // -------------------------------------------------- ROUND 4 — WRITE TASKS
  "r4-w1": {
    summary:
      "Абстрактный PaymentMethod с тремя реализациями — реальный архитектурный паттерн. Каждый способ оплаты ведёт себя по-своему, но клиентский код (process) работает со всеми единообразно. Это и есть Open/Closed Principle: открыто для расширения, закрыто для модификации.",
    keyPoints: [
      "from abc import ABC, abstractmethod — стандартный импорт для ABC.",
      "PaymentMethod(ABC) с @abstractmethod charge и refund — формальный контракт.",
      "Каждая реализация (CreditCard, Cash, BankTransfer) РЕАЛИЗУЕТ оба метода. Только тогда её можно создавать.",
      "В __init__ каждого класса — свои уникальные данные (номер карты, IBAN, и т.п.). Контракт ABC не требует одинаковой сигнатуры __init__.",
      "Функция process(payments, amount) принимает ЛЮБОЙ список PaymentMethod-ов. Полиморфизм даёт «бесплатное» расширение: добавишь Crypto(PaymentMethod) — process будет работать сразу.",
      "Возврат True/False из charge — стандартный шаблон «успех/неуспех». В реальной жизни — мощнее (исключения, объекты результата).",
    ],
    pitfalls: [
      "Если в одной из реализаций НЕ переопределить charge или refund — Python не даст создать объект (TypeError).",
      "Хотеть передать чарджу разные параметры (amount + currency у одного, amount + ttl у другого) — наруш контракта. Контракт должен быть единым.",
      "Слишком много логики в process (if isinstance(p, CreditCard): ...) — антипаттерн. Если ты пишешь if по типу — значит, метод должен быть В классе, не в process.",
    ],
    realWorld:
      "Stripe, PayPal, Apple Pay — все реализуют единый интерфейс PaymentProvider в любом серьёзном e-commerce коде. Добавление нового платёжного метода = новый класс, без изменений в checkout-логике.",
  },

  "r4-w2": {
    summary:
      "Employee → Developer/Designer с расчётом бонуса — классический полиморфизм. Каждая роль считает бонус по своей формуле, но total_bonus работает с любыми. Это типичная задача в HR/payroll-системах.",
    keyPoints: [
      "Employee.calculate_bonus() — raise NotImplementedError. Это сигнал «должен быть переопределён», но не такой формальный, как ABC.",
      "Developer переопределяет: self.salary * 0.2.",
      "Designer переопределяет: self.salary * 0.1 + 50 * len(self.tools).",
      "В __init__ наследников — super().__init__(name, salary), затем своё.",
      "total_bonus(employees) — функция, не метод. sum(e.calculate_bonus() for e in employees).",
      "Полиморфизм: total_bonus не знает, кто там — Developer или Designer. Просто вызывает calculate_bonus у каждого.",
    ],
    pitfalls: [
      "Если использовать ABC вместо NotImplementedError — Employee нельзя будет создать вообще. Это плюс (формальный контракт) и минус (теряется возможность создать «обычного сотрудника без специализации»).",
      "len(tools) когда tools=None — упадёт. Лучше в __init__ Designer: self.tools = tools or [].",
      "Если кто-то наследуется от Developer и не переопределит calculate_bonus — будет работать формула Developer (наследование).",
    ],
  },

  "r4-w3": {
    summary:
      "TimestampMixin — твой первый собственный миксин. Он добавляет общее поведение (created_at + age_seconds) к разным классам без наследования логики. Article и Comment — два независимых класса, объединённых одной функциональностью через миксин.",
    keyPoints: [
      "Миксин не наследуется от Article или Comment — он самостоятелен. Его нельзя «использовать» в одиночку (он не содержит достаточно данных), но он добавляет поведение к тем, кто его примешает.",
      "TimestampMixin.__init__ принимает *args, **kwargs и пробрасывает в super().__init__(*args, **kwargs) — это позволяет встраивать его в любую цепочку наследования без поломки.",
      "datetime.now() — текущее время. Хранится в self.created_at в момент создания объекта.",
      "age_seconds() — (datetime.now() - self.created_at).total_seconds(). datetime поддерживает арифметику; разница даёт timedelta.",
      "В Article и Comment ОБЯЗАТЕЛЬНО super().__init__() — иначе TimestampMixin.__init__ не вызовется и created_at не появится.",
      "ClassName.__mro__ показывает порядок поиска: Article → TimestampMixin → object.",
    ],
    pitfalls: [
      "Если в Article.__init__ забыть super().__init__() — created_at не установится, и age_seconds упадёт с AttributeError.",
      "Миксин с обязательными параметрами в __init__ ломает универсальность. Лучше — без параметров или со всеми по умолчанию.",
      "Можно сделать миксин с собственным __init__ и параметрами — но тогда сложнее использовать в множественном наследовании.",
    ],
    realWorld:
      "Django: TimestampMixin (с created_at, updated_at), SoftDeleteMixin (с deleted_at), AuditMixin — все добавляются к моделям, чтобы получить общее поведение без копи-паста.",
  },

  // -------------------------------------------------- ROUND 5 — WRITE TASKS
  "r5-w1": {
    summary:
      "Money — пример «value object»: два Money с одинаковыми полями ДОЛЖНЫ быть равны и хэшироваться одинаково. Здесь dunder-методы превращают класс в полноценный «гражданин» арифметики и сравнения.",
    keyPoints: [
      "В __init__ — raise ValueError если amount < 0. Money не может быть отрицательным.",
      "__add__ и __sub__ проверяют self.currency == other.currency. Если валюты разные — TypeError. Это защита от логических ошибок.",
      "__eq__ — равны при одинаковых amount И currency. Без isinstance — может упасть.",
      "__lt__ нужен для sorted(). Тоже только в одной валюте.",
      "__hash__ должен совпадать у равных объектов. Базируется на (round(amount, 2), currency).",
      "__str__ → '100.00 USD' (для пользователя). __repr__ → 'Money(100.0, \"USD\")' (для отладки/REPL).",
      "После всего этого Money работает в set, dict, sorted, ==, +, -, < — как настоящее число.",
    ],
    pitfalls: [
      "__eq__ без __hash__ ЛОМАЕТ хэширование. Если хочешь mutable объект, который нельзя хэшировать — поставь __hash__ = None.",
      "Использовать float для денег — потеря точности. В реальном коде — decimal.Decimal. В нашем учебном — float ОК.",
      "__lt__ при разных валютах — поднимать TypeError, а не возвращать False. Иначе sorted([USD, EUR]) даст странный результат.",
    ],
    realWorld:
      "decimal.Decimal, fractions.Fraction, любая денежная библиотека (py-moneyed) — все реализуют такой же набор dunder-методов. Это стандарт «value object».",
  },

  "r5-w2": {
    summary:
      "Stack как полноценный контейнер — закрепление dunder-методов __len__, __bool__, __iter__, __contains__, __repr__. После этого стек работает с привычным синтаксисом Python: len(s), if s, for x in s, x in s.",
    keyPoints: [
      "self._items = [] — внутреннее хранилище. Подчёркивание сигнализирует «не трогай снаружи».",
      "push(x) → append, pop() → list.pop() (берёт с конца), peek() → self._items[-1] (читает без удаления).",
      "pop из пустого стека — raise IndexError. list.pop() сам так делает, можно положиться, но явная проверка читаемее.",
      "__len__ → len(self._items). После этого len(stack) работает.",
      "__bool__ → bool(self._items). По умолчанию объект всегда True; __bool__ позволяет: if stack: ...",
      "__iter__ возвращает элементы от вершины к дну: for x in reversed(self._items): yield x.",
      "__contains__ — Python может использовать __iter__ как fallback, но явный __contains__ быстрее и нагляднее.",
      "__repr__ → 'Stack([5, 4, 3, 2, 1])' — порядок от вершины к дну.",
    ],
    pitfalls: [
      "Если __iter__ возвращает self._items напрямую — нарушает инкапсуляцию (внешний код получит ссылку на внутренний список).",
      "Stack без __bool__ — пустой stack всё равно True по умолчанию. if stack будет True даже когда там 0 элементов.",
      "pop без проверки на пустоту — IndexError из list.pop, а у нас в задаче нужно raise IndexError с понятным сообщением.",
    ],
  },

  "r5-w3": {
    summary:
      "TodoList с frozen Task и автоматической сортировкой — пример «маленькой архитектуры». dataclass(order=True) автоматически генерирует __lt__ и т.д. Поле priority стоит ПЕРВЫМ, потому что order сравнивает поля в порядке объявления.",
    keyPoints: [
      "@dataclass(order=True) — автогенерация __init__, __repr__, __eq__, __lt__, __le__, __gt__, __ge__. Сравнение идёт по ВСЕМ полям, в порядке объявления.",
      "priority должна стоять ПЕРВОЙ — иначе сортировка пойдёт по title (алфавитно), а не по приоритету.",
      "done: bool = False — поле с default. Может быть в любой позиции, но default-поля должны идти ПОСЛЕ обязательных.",
      "TodoList.add(title, priority) — обёртка над созданием Task.",
      "complete(title) — находит Task с таким title и помечает done=True. Если Task НЕ frozen — просто t.done = True. Если frozen — нужно через dataclasses.replace.",
      "pending() → sorted([t for t in self._tasks if not t.done]). sorted работает потому, что у Task есть __lt__ (от order=True).",
      "__iter__ → yield from self._tasks. Самый простой способ.",
    ],
    pitfalls: [
      "Если priority не первая — sorted сначала будет сравнивать другое поле, и приоритет «не сработает».",
      "frozen=True И order=True — комбинируются. Тогда complete() требует dataclasses.replace.",
      "Без default у done в позиции после обязательных полей — TypeError при объявлении (defaults после non-defaults запрещены).",
    ],
    realWorld:
      "Любая система задач (Trello, Jira) внутри использует подобные value objects + сервисный класс. dataclass с order — стандарт для сортируемых immutable-данных.",
  },
};
