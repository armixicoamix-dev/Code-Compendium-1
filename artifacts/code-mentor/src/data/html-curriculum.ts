import type { Round } from "@/data/curriculum";

// ─────────────────────────────────────────────────────────────────────────────
// Round 1 · HTML — структура и семантика
// ─────────────────────────────────────────────────────────────────────────────
const h1: Round = {
  number: 1,
  title: "HTML · Структура документа и семантические теги",
  level: "Начальный",
  intro:
    "HTML — скелет любой веб-страницы. В этом раунде узнаешь, как браузер читает HTML-документ и почему правильные теги важнее красивого кода.\n\n" +
    "**Что изучим:**\n" +
    "• Базовая структура HTML5 (DOCTYPE, html, head, body)\n" +
    "• Семантические теги: header, nav, main, article, section, footer\n" +
    "• Заголовки h1–h6, параграфы, ссылки, изображения\n" +
    "• Атрибуты тегов: id, class, href, src, alt",
  lesson: {
    title: "HTML: структура и семантика",
    summary:
      "Как браузер читает HTML; базовая структура документа; семантические теги и зачем они нужны; атрибуты и их роль.",
    readingMinutes: 10,
    sections: [
      {
        heading: "Базовая структура HTML-документа",
        tagline: "Каждая страница начинается с одной и той же конструкции",
        body:
          "**Что такое HTML?** HTML (HyperText Markup Language) — это язык разметки, который описывает структуру веб-страницы. Браузер читает HTML-файл и строит из него дерево элементов (DOM), которое затем отображает на экране.\n\n" +
          "**DOCTYPE** — самая первая строка каждого HTML-документа. Она сообщает браузеру, по каким правилам разбирать файл. `<!DOCTYPE html>` означает HTML5 — актуальный стандарт. Без этой строки браузер включает «режим совместимости» (quirks mode), где старые баги становятся «фичами» — верстка ломается.\n\n" +
          "**`<html lang=\"ru\">`** — корневой элемент, в который вложено всё остальное. Атрибут `lang` указывает язык страницы. Это важно для:\n" +
          "- **Доступности**: программы чтения экрана (скринридеры) произносят текст с правильным акцентом\n" +
          "- **SEO**: поисковики знают, для какой аудитории страница\n" +
          "- **Браузера**: автоперевод предлагается корректно\n\n" +
          "**`<head>`** — невидимая часть страницы с метаданными:\n" +
          "- `<meta charset=\"UTF-8\">` — кодировка. UTF-8 поддерживает кириллицу, эмодзи и все мировые языки. Без неё — кракозябры.\n" +
          "- `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">` — без этого тега на мобильных страница выглядит как уменьшенная версия десктопа. С ним — адаптируется под размер экрана.\n" +
          "- `<title>` — текст в заголовке вкладки браузера. Также отображается в результатах поисковика.\n" +
          "- `<link rel=\"stylesheet\" href=\"style.css\">` — подключение CSS-файла.\n\n" +
          "**`<body>`** — всё видимое содержимое страницы: тексты, кнопки, изображения, формы.\n\n" +
          "**Как браузер обрабатывает HTML:** браузер читает файл сверху вниз, строит DOM, загружает ресурсы (CSS, JS, картинки) и рисует страницу. Это называется критическим путём рендеринга (critical rendering path).",
        code:
          "<!DOCTYPE html>\n" +
          "<html lang=\"ru\">\n" +
          "  <head>\n" +
          "    <!-- Кодировка — всегда первым в <head> -->\n" +
          "    <meta charset=\"UTF-8\" />\n\n" +
          "    <!-- Адаптивность на мобильных -->\n" +
          "    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n\n" +
          "    <!-- Заголовок вкладки и поисковика -->\n" +
          "    <title>Моя первая страница</title>\n\n" +
          "    <!-- SEO-описание для поисковиков -->\n" +
          "    <meta name=\"description\" content=\"Краткое описание страницы до 160 символов\" />\n\n" +
          "    <!-- Подключение CSS -->\n" +
          "    <link rel=\"stylesheet\" href=\"style.css\" />\n" +
          "  </head>\n" +
          "  <body>\n" +
          "    <h1>Привет, мир!</h1>\n" +
          "    <p>Это мой первый абзац.</p>\n\n" +
          "    <!-- JS подключают перед </body> — страница грузится быстрее -->\n" +
          "    <script src=\"main.js\"></script>\n" +
          "  </body>\n" +
          "</html>",
        keyTakeaways: [
          "DOCTYPE — первая строка файла без пробелов и пустых строк перед ней.",
          "charset=\"UTF-8\" — ставь первым в <head>, иначе браузер может неправильно прочесть следующие теги.",
          "viewport мета-тег обязателен для корректного отображения на мобильных: без него Google снизит позиции.",
          "<script> лучше размещать перед закрывающим </body>, а не в <head> — страница будет рисоваться быстрее.",
          "HTML-теги можно писать строчными буквами (современный стандарт) или заглавными (устаревший стиль XHTML).",
        ],
        pitfalls: [
          "Если charset не первый в <head>, браузер может уже прочитать часть файла в неправильной кодировке.",
          "Не путай <title> (название вкладки) и <h1> (заголовок на странице) — это разные вещи.",
          "Никогда не размещай видимый контент внутри <head> — браузер его проигнорирует или отобразит некорректно.",
        ],
      },
      {
        heading: "Семантические теги",
        tagline: "Называй теги по смыслу, а не по внешнему виду",
        body:
          "**Проблема до HTML5.** Раньше весь макет делали из `<div>` и `<span>`. Внешне страница выглядела нормально, но с точки зрения кода — всё одно и то же: браузер, поисковик и скринридер не знали, где шапка, где навигация, а где основной текст.\n\n" +
          "**Семантика** — это смысл тегов. HTML5 ввёл теги, которые описывают роль содержимого:\n\n" +
          "| Тег | Назначение | Когда использовать |\n" +
          "|-----|-----------|-------------------|\n" +
          "| `<header>` | Шапка сайта или раздела | Логотип, навигация, слоган |\n" +
          "| `<nav>` | Блок навигации | Главное меню, хлебные крошки |\n" +
          "| `<main>` | Основной контент | Один на страницу |\n" +
          "| `<article>` | Самостоятельный материал | Пост в блоге, новость, комментарий |\n" +
          "| `<section>` | Логический раздел | Часть страницы с заголовком |\n" +
          "| `<aside>` | Боковая колонка / врезка | Реклама, похожие статьи |\n" +
          "| `<footer>` | Подвал сайта или раздела | Копирайт, контакты, карта |\n\n" +
          "**Заголовки h1–h6** — иерархия структуры, не размер текста:\n" +
          "- `<h1>` — один на страницу, главная тема\n" +
          "- `<h2>` — подразделы\n" +
          "- `<h3>` — подподразделы\n" +
          "- Не пропускай уровни (h1 → h3 без h2 — ошибка структуры)\n\n" +
          "**Зачем это важно:**\n" +
          "- **SEO**: поисковик Google лучше понимает структуру страницы и ставит её выше в выдаче\n" +
          "- **Доступность (a11y)**: программы для незрячих пользователей ориентируются по `<nav>` и `<main>`\n" +
          "- **Читаемость кода**: другой разработчик сразу понимает структуру без комментариев\n" +
          "- **Семантика в CSS**: `.article > h2` работает предсказуемо\n\n" +
          "**`<div>` и `<span>` нужны** для стилизации, когда ни один семантический тег не подходит. `<div>` — блочный элемент, `<span>` — строчный.",
        code:
          "<body>\n" +
          "  <!-- Шапка сайта (header) -->\n" +
          "  <header>\n" +
          "    <a href=\"/\" class=\"logo\">МойБренд</a>\n" +
          "    <!-- Навигация всегда внутри nav -->\n" +
          "    <nav aria-label=\"Главное меню\">\n" +
          "      <a href=\"/\">Главная</a>\n" +
          "      <a href=\"/about\">О нас</a>\n" +
          "      <a href=\"/contact\">Контакты</a>\n" +
          "    </nav>\n" +
          "  </header>\n\n" +
          "  <!-- Основной контент — строго один на страницу -->\n" +
          "  <main>\n" +
          "    <!-- Самостоятельный материал (статья, пост) -->\n" +
          "    <article>\n" +
          "      <h1>Главный заголовок страницы</h1>\n" +
          "      <p>Вводный текст...</p>\n\n" +
          "      <section>\n" +
          "        <h2>Раздел 1: введение</h2>\n" +
          "        <p>Текст раздела...</p>\n" +
          "      </section>\n\n" +
          "      <section>\n" +
          "        <h2>Раздел 2: примеры</h2>\n" +
          "        <p>Примеры...</p>\n" +
          "      </section>\n" +
          "    </article>\n\n" +
          "    <!-- Боковая панель -->\n" +
          "    <aside>\n" +
          "      <h2>Похожие статьи</h2>\n" +
          "      <ul>...</ul>\n" +
          "    </aside>\n" +
          "  </main>\n\n" +
          "  <!-- Подвал -->\n" +
          "  <footer>\n" +
          "    <p>© 2025 МойБренд. Все права защищены.</p>\n" +
          "    <nav aria-label=\"Ссылки в подвале\">\n" +
          "      <a href=\"/privacy\">Конфиденциальность</a>\n" +
          "    </nav>\n" +
          "  </footer>\n" +
          "</body>",
        keyTakeaways: [
          "На странице должен быть ровно один <main> — он указывает на основной контент.",
          "<article> описывает независимый материал: если вырвать из контекста — всё равно понятно.",
          "<section> — логический раздел, всегда должен иметь заголовок (<h2>, <h3> и т.д.).",
          "aria-label на <nav> помогает скринридерам различать несколько навигационных блоков.",
          "<div> и <span> — для стилизации, когда ни один семантический тег не подходит по смыслу.",
        ],
        pitfalls: [
          "Не используй заголовки h1–h6 для размера текста — только для логической иерархии документа.",
          "Не вкладывай <button> внутрь <a> или наоборот — это нарушает HTML-спецификацию.",
          "Не используй <section> вместо <div> для стилизации — <section> требует смыслового заголовка.",
          "Не пропускай уровни заголовков (например h1 → h3): это нарушает доступность.",
        ],
      },
      {
        heading: "Ссылки, изображения и атрибуты",
        tagline: "Атрибуты — это параметры, которые уточняют поведение тега",
        body:
          "**Атрибуты** — пары `name=\"value\"` внутри открывающего тега. Они уточняют поведение или добавляют информацию.\n\n" +
          "**Ссылка `<a>`:**\n" +
          "- `href` — URL (адрес). Обязателен для работы ссылки.\n" +
          "- `target=\"_blank\"` — открыть в новой вкладке\n" +
          "- `rel=\"noopener noreferrer\"` — безопасность: новая вкладка не получает доступ к исходной странице через `window.opener`\n" +
          "- `href=\"#section-id\"` — якорная ссылка, прокручивает к элементу с данным `id`\n\n" +
          "**Изображение `<img>`:**\n" +
          "- `src` — путь к файлу (локальный или URL)\n" +
          "- `alt` — альтернативный текст. **Обязателен**: если картинка не загрузилась — показывается alt. Скринридер его произносит. Поисковик индексирует.\n" +
          "- `width` и `height` — рекомендуется указывать: браузер заранее резервирует место и страница не «прыгает» при загрузке\n" +
          "- `loading=\"lazy\"` — ленивая загрузка: картинки за экраном грузятся только когда к ним скролят\n\n" +
          "**Глобальные атрибуты** (работают у любого тега):\n" +
          "- `id` — уникальный идентификатор (один на страницу)\n" +
          "- `class` — CSS-класс (можно несколько через пробел: `class=\"card featured\"`)\n" +
          "- `style` — инлайн-стили (избегай в большом проекте)\n" +
          "- `title` — всплывающая подсказка при наведении\n" +
          "- `data-*` — пользовательские атрибуты для JS: `data-id=\"42\"` → `el.dataset.id`",
        code:
          "<!-- Ссылки -->\n" +
          "<a href=\"https://example.com\">Обычная ссылка</a>\n" +
          "<a href=\"https://example.com\" target=\"_blank\" rel=\"noopener noreferrer\">\n" +
          "  Открыть в новой вкладке (безопасно)\n" +
          "</a>\n" +
          "<a href=\"#contacts\">Перейти к контактам (якорь)</a>\n\n" +
          "<!-- Изображения -->\n" +
          "<img\n" +
          "  src=\"photo.jpg\"\n" +
          "  alt=\"Портрет улыбающегося кота в шляпе\"\n" +
          "  width=\"400\"\n" +
          "  height=\"300\"\n" +
          "  loading=\"lazy\"\n" +
          "/>\n\n" +
          "<!-- Декоративное изображение (alt пустой — скринридер пропускает) -->\n" +
          "<img src=\"decorative-line.svg\" alt=\"\" />\n\n" +
          "<!-- data-атрибуты для JavaScript -->\n" +
          "<button data-user-id=\"42\" data-action=\"delete\">\n" +
          "  Удалить\n" +
          "</button>\n" +
          "<script>\n" +
          "  const btn = document.querySelector('button');\n" +
          "  console.log(btn.dataset.userId);  // '42'\n" +
          "  console.log(btn.dataset.action);  // 'delete'\n" +
          "</script>",
        keyTakeaways: [
          "alt у <img> — обязателен. Для декоративных картинок пишут alt=\"\" (пустой, не отсутствующий).",
          "target=\"_blank\" требует rel=\"noopener noreferrer\" по соображениям безопасности.",
          "width и height у <img> предотвращают CLS (Cumulative Layout Shift) — страница не прыгает.",
          "loading=\"lazy\" ускоряет начальную загрузку страницы.",
          "data-* атрибуты — официальный способ хранить произвольные данные в HTML для чтения через JS.",
        ],
      }
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h1-f1",
      title: "Базовая структура HTML",
      description: "Заполни пропуски в минимальном HTML5-документе.",
      code:
        "<!{{0}} html>\n" +
        "<html lang=\"ru\">\n" +
        "  <{{1}}>\n" +
        "    <meta {{2}}=\"UTF-8\" />\n" +
        "    <title>Привет</title>\n" +
        "  </head>\n" +
        "  <{{3}}>\n" +
        "    <h1>Привет!</h1>\n" +
        "  </body>\n" +
        "</html>",
      answers: [["DOCTYPE"], ["head"], ["charset"], ["body"]],
      hints: [
        "DOCTYPE — объявление версии HTML, пишется заглавными буквами",
        "Метаданные (title, meta, link) находятся в этом теге",
        "Атрибут кодировки называется charset",
        "Видимое содержимое страницы находится в этом теге",
      ],
    },
    {
      type: "fill",
      id: "h1-f2",
      title: "Ссылка и изображение",
      description: "Заполни атрибуты тега ссылки и изображения.",
      code:
        "<!-- Ссылка открывается в новой вкладке -->\n" +
        "<a {{0}}=\"https://example.com\" {{1}}=\"_blank\">Открыть сайт</a>\n\n" +
        "<!-- Изображение с подписью для скринридеров -->\n" +
        "<img {{2}}=\"photo.jpg\" {{3}}=\"Портрет кота\" width=\"300\" />",
      answers: [["href"], ["target"], ["src"], ["alt"]],
      hints: [
        "Атрибут URL-адреса ссылки — от слова hypertext reference",
        "Атрибут, указывающий куда открыть ссылку",
        "Атрибут источника изображения (source)",
        "Альтернативный текст для изображения (alternative)",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h1-q1",
      title: "Семантика: тег для навигации",
      question: "Какой семантический HTML5-тег используется для блока навигации сайта?",
      answers: [
        "nav", "<nav>", "<nav/>", "тег nav", "html nav", "navigation",
        "nav тег", "nav элемент", "тег навигации",
      ],
      hint: "Тег является сокращением от слова navigation",
      explanation:
        "Тег <nav> предназначен для блоков навигации: главное меню, хлебные крошки, ссылки на разделы. Поисковики и скринридеры обрабатывают его особым образом.",
    },
    {
      type: "question",
      id: "h1-q2",
      title: "Правила иерархии заголовков",
      question: "Сколько тегов <h1> рекомендуется использовать на одной HTML-странице?",
      answers: [
        "1", "один", "одного", "1 шт", "один h1", "одно", "1 штука",
        "один на страницу", "только один", "ровно один", "1 раз",
        "one", "1 h1", "единственный",
      ],
      hint: "Подумай о структуре документа как о книге с одним главным названием",
      explanation:
        "По современным стандартам SEO и доступности на странице должен быть ровно один <h1> — главный заголовок страницы. Следующий уровень — <h2>, потом <h3>. Пропускать уровни не рекомендуется.",
    },
    {
      type: "question",
      id: "h1-q3",
      title: "Для чего нужен тег <head>?",
      question: "Что хранится в теге <head> HTML-документа — метаданные или видимый контент?",
      answers: [
        "метаданные", "metadata", "meta", "метаданные документа", "мета",
        "невидимые данные", "не отображается пользователю", "meta information",
        "данные о странице", "информация о документе", "мета теги",
        "заголовок", "title и meta", "title meta link",
      ],
      hint: "Это данные о странице, которые браузер не отображает пользователю напрямую",
      explanation:
        "<head> содержит метаданные: <title> (заголовок вкладки), <meta charset>, <meta name=\"viewport\">, подключения CSS (<link>). Пользователь это не видит, но браузер и поисковики читают.",
    },
    {
      type: "question",
      id: "h1-q4",
      title: "Зачем нужен атрибут alt у img?",
      question: "Для чего предназначен атрибут alt у тега <img>?",
      answers: [
        "альтернативный текст", "alt текст", "для скринридеров", "доступность",
        "текст для незрячих", "текст если картинка не загрузилась",
        "accessibility", "описание изображения", "alternative text",
        "для поисковиков", "seo", "описание картинки", "замена изображения",
        "для доступности и seo", "когда картинка не загружается",
      ],
      hint: "Что покажет браузер если картинка не загрузится? Кому ещё это нужно?",
      explanation:
        "Атрибут alt выполняет три функции: 1) Показывается если изображение не загружается; 2) Читается скринридерами для незрячих пользователей; 3) Индексируется поисковиками для SEO. Для декоративных картинок alt оставляют пустым (alt=\"\"), а не убирают совсем.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h1-w1",
      title: "Семантическая страница",
      task:
        "Напиши HTML-каркас страницы-блога с семантическими тегами:\n" +
        "• Шапка (header) с навигацией (nav) с двумя ссылками\n" +
        "• Основная часть (main) со статьёй (article) с заголовком h1\n" +
        "• Подвал (footer) с копирайтом",
      hints: [
        "Начни с <header> и <nav> внутри него с двумя тегами <a>",
        "Добавь <main> с <article> внутри. Внутри article — <h1> и <p>",
        "Добавь <footer> с параграфом © после закрывающего </main>",
      ],
      required: ["<header>", "<nav>", "<main>", "<article>", "<h1>", "<footer>"],
      minLines: 10,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 2 · CSS — селекторы, каскад, box-model
// ─────────────────────────────────────────────────────────────────────────────
const h2: Round = {
  number: 2,
  title: "CSS · Селекторы, каскад и блочная модель",
  level: "Начальный",
  intro:
    "CSS управляет внешним видом страницы. В этом раунде освоишь три ключевые концепции: как выбирать элементы, как работает каскад и специфичность, и что такое блочная модель.\n\n" +
    "**Что изучим:**\n" +
    "• Типы селекторов: теговый, классовый, id, псевдоклассы\n" +
    "• Каскад и специфичность — почему один стиль перебивает другой\n" +
    "• Box-model: margin, border, padding, content",
  lesson: {
    title: "CSS: селекторы и box-model",
    summary:
      "Типы CSS-селекторов; приоритет и специфичность; блочная модель margin/border/padding.",
    readingMinutes: 11,
    sections: [
      {
        heading: "Типы CSS-селекторов",
        tagline: "Сначала выбери элемент — потом стилизуй",
        body:
          "**CSS-селектор** — это паттерн, который указывает к каким HTML-элементам применить стиль. Понимание селекторов — основа умения писать CSS.\n\n" +
          "**Теговый (элементный)**: `p { color: red; }` — применяется ко ВСЕМ тегам данного типа на странице. Удобен для глобальных сбросов и типографики.\n\n" +
          "**Классовый** (`.класс`): `.card { border: 1px solid; }` — один класс можно дать многим элементам. Это основной инструмент в реальных проектах. Один элемент может иметь несколько классов: `class=\"card featured large\"`.\n\n" +
          "**ID** (`#ид`): `#hero { background: url(...); }` — уникальный идентификатор. На странице каждый id должен быть в единственном числе. Используют реже классов из-за высокой специфичности.\n\n" +
          "**Универсальный**: `* { box-sizing: border-box; }` — выбирает все элементы.\n\n" +
          "**Псевдоклассы** (одно двоеточие) — реагируют на состояние или позицию:\n" +
          "- `:hover` — при наведении мыши\n" +
          "- `:focus` — при фокусе (Tab или клик)\n" +
          "- `:active` — при нажатии\n" +
          "- `:first-child` / `:last-child` — первый/последний дочерний\n" +
          "- `:nth-child(2n)` — чётные элементы\n" +
          "- `:not(.disabled)` — все кроме .disabled\n\n" +
          "**Псевдоэлементы** (два двоеточия) — создают виртуальные части элемента:\n" +
          "- `::before` / `::after` — содержимое до/после (content: '')\n" +
          "- `::placeholder` — стиль плейсхолдера input\n" +
          "- `::selection` — стиль выделения текста\n\n" +
          "**Комбинаторы** (объединяют селекторы):\n" +
          "- `A B` — любой потомок B внутри A (любой уровень вложенности)\n" +
          "- `A > B` — только прямой дочерний элемент\n" +
          "- `A + B` — следующий соседний (immediately after)\n" +
          "- `A ~ B` — все следующие соседи\n\n" +
          "**Атрибутные селекторы:**\n" +
          "- `[type=\"email\"]` — элементы с точным атрибутом\n" +
          "- `[href^=\"https\"]` — начинается с https\n" +
          "- `[src$=\".png\"]` — заканчивается на .png",
        code:
          "/* Теговый — все параграфы */\np { line-height: 1.6; color: #333; }\n\n" +
          "/* Классовый — основной инструмент */\n.btn { padding: 8px 16px; border-radius: 6px; }\n.btn.primary { background: #3b82f6; color: white; }\n\n" +
          "/* ID — уникальный элемент */\n#hero { min-height: 100vh; background: #000; }\n\n" +
          "/* Псевдоклассы */\na:hover { color: #2563eb; text-decoration: underline; }\ninput:focus { outline: 2px solid #3b82f6; outline-offset: 2px; }\nli:first-child { font-weight: bold; }\nli:nth-child(odd) { background: #f9fafb; }\n\n" +
          "/* Псевдоэлементы */\n.quote::before { content: '«'; }\n.quote::after  { content: '»'; }\n\n" +
          "/* Комбинаторы */\n.nav a { text-decoration: none; }       /* потомок */\n.nav > li { display: flex; }            /* прямой потомок */\nh2 + p { font-size: 1.1rem; }           /* первый абзац после h2 */\n\n" +
          "/* Атрибутный */\ninput[type=\"email\"] { background: #f0f9ff; }\na[target=\"_blank\"]::after { content: ' ↗'; }",
        keyTakeaways: [
          "Классы (.class) — основной инструмент. ID (#id) — только для JS и уникальных элементов.",
          ":hover и :focus нужны для интерактивности и доступности (особенно :focus для клавиатурной навигации).",
          "Два двоеточия (::before, ::after) — псевдоэлементы. Одно двоеточие (:hover, :focus) — псевдоклассы.",
          "A > B строже, чем A B: используй '>' когда важна именно прямая вложенность.",
          "Универсальный * { box-sizing: border-box } — стандартный сброс для всех элементов.",
        ],
        pitfalls: [
          "ID имеет очень высокую специфичность — переопределить его классом почти невозможно.",
          "Не используй inline style (style=\"...\") — невозможно переопределить из CSS-файла.",
          ":focus нельзя убирать без замены (outline: none без альтернативы ломает доступность).",
        ],
      },
      {
        heading: "Каскад и специфичность",
        tagline: "Когда два правила конфликтуют — кто побеждает?",
        body:
          "**Каскад** — алгоритм, по которому браузер решает, какое CSS-правило применить, если несколько правил нацелены на один элемент.\n\n" +
          "**Порядок приоритета (от низшего к высшему):**\n" +
          "1. Стили браузера по умолчанию (user agent stylesheet)\n" +
          "2. Внешние CSS-файлы (link rel=\"stylesheet\")\n" +
          "3. Инлайн-стили (`style=\"...\"` в HTML)\n" +
          "4. `!important` — переопределяет всё (опасно, используй только в крайнем случае)\n\n" +
          "**Специфичность** (вес селектора) — числовое значение (a, b, c):\n" +
          "- (1,0,0) — ID-селектор `#id`\n" +
          "- (0,1,0) — класс `.class`, псевдокласс `:hover`, атрибутный `[type]`\n" +
          "- (0,0,1) — тег `div`, `p`, псевдоэлемент `::before`\n" +
          "- (0,0,0) — универсальный `*`, комбинаторы\n\n" +
          "**Пример:** `div.card#hero` = (1,1,1). Сравнивают слева направо: сначала a, потом b, потом c.\n\n" +
          "**Когда специфичность одинакова** — побеждает последнее по порядку правило в CSS-файле (или по порядку файлов).",
        code:
          "/* Специфичность примеров: */\n\n" +
          "p           { color: black; }    /* (0,0,1) */\n" +
          ".text       { color: blue; }     /* (0,1,0) ← победит над p */\n" +
          "#intro      { color: green; }    /* (1,0,0) ← победит над .text */\n\n" +
          "/* Комплексные селекторы суммируются: */\n" +
          "p.text           /* (0,1,1) — тег + класс */\n" +
          ".card .title     /* (0,2,0) — два класса */\n" +
          "#nav .link:hover /* (1,1,0) — id + класс + псевдокласс */\n\n" +
          "/* !important — последний аргумент */\n" +
          ".btn { color: red !important; }  /* никакой селектор не переопределит без !important */\n\n" +
          "/* Вычисление: кто победит? */\n" +
          ".article p.intro          /* (0,2,1) */\n" +
          "#content > p              /* (1,0,1) ← победит: первая колонка больше */",
        keyTakeaways: [
          "Специфичность: ID (100) > класс/псевдокласс (10) > тег (1). Сравниваем поколонно.",
          "!important перебивает всё — используй только для сброса сторонних библиотек.",
          "При одинаковой специфичности побеждает правило, написанное позже в CSS.",
          "Инлайн-стили (style=\"\") имеют специфичность (1,0,0,0) — выше любого CSS-файла.",
        ],
      },
      {
        heading: "Блочная модель (Box Model)",
        tagline: "Каждый элемент — прямоугольник с четырьмя слоями",
        body:
          "Каждый HTML-элемент — это прямоугольный блок. CSS-блочная модель описывает, из каких частей он состоит, слоями изнутри наружу:\n\n" +
          "1. **content** — сам контент: текст, картинка, дочерние элементы. Размер задаётся через `width` и `height`.\n" +
          "2. **padding** — внутренний отступ между контентом и рамкой. Фон элемента распространяется на padding.\n" +
          "3. **border** — рамка вокруг padding. Может быть разной толщины, стиля и цвета.\n" +
          "4. **margin** — внешний отступ между элементами. Прозрачный (фон не распространяется).\n\n" +
          "**Проблема `content-box`** (поведение по умолчанию): `width` задаёт только контентную зону. Итоговая ширина = width + padding-left + padding-right + border-left + border-right. Это неинтуитивно.\n\n" +
          "**Решение `border-box`**: `width` включает padding и border. `width: 300px` — это и есть 300px. Почти всегда предпочтительно.\n\n" +
          "**Схлопывание margin (margin collapse)**: вертикальные margin соседних блоков схлопываются в наибольший из них. `margin-bottom: 20px` + `margin-top: 16px` = 20px (не 36px). Горизонтальные margin НЕ схлопываются.\n\n" +
          "**Краткая запись:**\n" +
          "- `padding: 16px` — все стороны\n" +
          "- `padding: 8px 16px` — вертикаль | горизонталь\n" +
          "- `padding: 4px 8px 12px 16px` — верх | право | низ | лево (по часовой стрелке)\n" +
          "- `margin: 0 auto` — центрирование блочного элемента (0 вертикаль, auto горизонталь)",
        code:
          "/* Сброс box-sizing — всегда в начале CSS */\n*, *::before, *::after {\n  box-sizing: border-box;\n}\n\n" +
          "/* content-box (по умолчанию, без сброса) */\n.box-content {\n  width: 200px;\n  padding: 20px;\n  border: 2px solid;\n  /* Итоговая ширина: 200+20+20+2+2 = 244px */\n}\n\n" +
          "/* border-box (с нашим сбросом) */\n.box-border {\n  width: 200px;\n  padding: 20px;\n  border: 2px solid;\n  /* Итоговая ширина: ровно 200px */\n}\n\n" +
          ".card {\n  width: 320px;\n  padding: 20px 24px;       /* вертикаль | горизонталь */\n  border: 1px solid #e5e7eb;\n  border-radius: 8px;\n  margin: 0 auto 24px;      /* верх 0, auto горизонталь, низ 24px */\n}\n\n" +
          "/* Схлопывание margin — осторожно! */\n.block-a { margin-bottom: 30px; }\n.block-b { margin-top: 20px; }\n/* Итоговый отступ между ними: 30px (не 50px!) */",
        keyTakeaways: [
          "Всегда добавляй *, *::before, *::after { box-sizing: border-box } в начало CSS — без этого размеры непредсказуемы.",
          "margin: 0 auto — классический способ центрировать блочный элемент (требует явной ширины).",
          "padding (внутри рамки, фон распространяется) ≠ margin (снаружи рамки, прозрачный).",
          "Вертикальные margin схлопываются в наибольший. Горизонтальные — нет.",
          "outline не влияет на box model и не вызывает reflow — идеален для стилей фокуса.",
        ],
        pitfalls: [
          "Не задавай width в % и padding в px одновременно без border-box — результат будет неожиданным.",
          "margin: auto не работает на flex/grid-детях по вертикали без явной высоты контейнера.",
          "Схлопывание margin отключается внутри flex/grid контейнеров — это частая причина удивления.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h2-f1",
      title: "Типы селекторов",
      description: "Определи начало каждого CSS-селектора.",
      code:
        "/* Классовый селектор */\n{{0}}card { padding: 16px; }\n\n" +
        "/* ID-селектор */\n{{1}}header { position: sticky; }\n\n" +
        "/* Псевдокласс */\na{{2}}hover { color: blue; }\n\n" +
        "/* box-sizing сброс */\n*, *{{3}}before, *{{3}}after { box-sizing: border-box; }",
      answers: [["."], ["#"], [":"], ["::"]],
      hints: [
        "Классовый начинается с символа точки",
        "ID-селектор начинается с символа решётки",
        "Псевдокласс присоединяется через одинарное двоеточие",
        "Псевдоэлемент присоединяется через двойное двоеточие",
      ],
    },
    {
      type: "fill",
      id: "h2-f2",
      title: "Box model: отступы и рамки",
      description: "Заполни свойства блочной модели.",
      code:
        ".box {\n" +
        "  width: 200px;\n" +
        "  {{0}}: 16px;           /* внутренний отступ */\n" +
        "  {{1}}: 2px solid #000; /* рамка */\n" +
        "  {{2}}: 24px auto;      /* внешний отступ, центрирование */\n" +
        "  box-{{3}}: border-box;  /* ширина включает padding и border */\n" +
        "}",
      answers: [["padding"], ["border"], ["margin"], ["sizing"]],
      hints: [
        "Внутренний отступ — пространство между контентом и рамкой",
        "Рамка вокруг элемента",
        "Внешний отступ — пространство снаружи от рамки",
        "box-sizing: border-box — самое распространённое значение",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h2-q1",
      title: "Специфичность",
      question:
        "Какой из этих трёх селекторов имеет наибольшую специфичность?\nA) p.text\nB) #title\nC) .article .text",
      answers: ["B", "b", "#title", "id", "b) #title", "вариант b", "второй", "b — id селектор",
        "#title — id", "id селектор", "id имеет наибольшую специфичность", "решётка",
      ],
      hint: "ID-селектор (#) имеет вес (1,0,0) по трёхколоночной системе",
      explanation:
        "A) p.text = (0,1,1)\nB) #title = (1,0,0)\nC) .article .text = (0,2,0)\n\nСпецифичность сравнивается поколонно слева направо: у B первая колонка (id) равна 1, что выше любого числа в колонках b и c. Поэтому #title (B) побеждает над двумя классами.",
    },
    {
      type: "question",
      id: "h2-q2",
      title: "box-sizing",
      question:
        "У элемента width: 200px; padding: 20px; border: 2px solid. Какова итоговая ширина при box-sizing: content-box?",
      answers: ["244px", "244", "244 px", "244 пикселей", "244px итого", "244 пкс"],
      hint: "content-box: итоговая ширина = width + 2*padding + 2*border",
      explanation:
        "content-box: width(200) + padding-left(20) + padding-right(20) + border-left(2) + border-right(2) = 244px. Именно поэтому рекомендуют border-box: тогда ширина элемента — ровно те 200px, что написано в коде.",
    },
    {
      type: "question",
      id: "h2-q3",
      title: "Разница padding и margin",
      question: "В чём ключевое отличие padding от margin в CSS блочной модели?",
      answers: [
        "padding внутри, margin снаружи",
        "padding внутренний отступ margin внешний",
        "padding внутри рамки margin снаружи рамки",
        "padding до border margin после border",
        "внутренний и внешний",
        "padding — внутри элемента, margin — снаружи",
        "padding внутри, margin между элементами",
        "padding — от контента до рамки, margin — от рамки до соседей",
        "padding increases element size, margin adds space between elements",
        "padding is inside border, margin is outside",
      ],
      hint: "Подумай: где фон элемента распространяется — внутрь padding или нет?",
      explanation:
        "padding — внутренний отступ: пространство между контентом и рамкой (border). Фон элемента распространяется на padding. margin — внешний отступ: пространство между границей элемента и соседями. Фон на margin не распространяется (прозрачный).",
    },
    {
      type: "question",
      id: "h2-q4",
      title: "Зачем нужен box-sizing: border-box?",
      question: "Зачем практически все проекты добавляют * { box-sizing: border-box } в начало CSS?",
      answers: [
        "чтобы ширина включала padding и border",
        "padding и border входят в width",
        "предсказуемые размеры",
        "width включает отступы и рамку",
        "ширина не меняется от padding",
        "упрощает расчёт размеров",
        "border и padding не увеличивают размер",
        "makes sizing predictable",
        "width равна видимому размеру",
        "размер элемента равен заданной ширине",
      ],
      hint: "Без border-box элемент в 300px может занять больше 300px — почему?",
      explanation:
        "По умолчанию (content-box) width задаёт только зону контента, а padding и border добавляются сверху. border-box включает padding и border в заданную ширину. Это делает вёрстку интуитивной: width: 300px означает ровно 300px независимо от отступов.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h2-w1",
      title: "Стили для карточки",
      task:
        "Напиши CSS для карточки `.card`:\n" +
        "• box-sizing: border-box\n" +
        "• ширина 320px, padding 20px\n" +
        "• рамка 1px solid #ddd, скругление 8px\n" +
        "• внешний отступ снизу 16px\n" +
        "• при наведении (:hover) цвет рамки меняется на #999",
      hints: [
        "Начни с .card { box-sizing: border-box; width: 320px; padding: 20px; }",
        "Добавь border: 1px solid #ddd; border-radius: 8px; margin-bottom: 16px;",
        "Добавь правило .card:hover { border-color: #999; }",
      ],
      required: [".card", "box-sizing", "border-radius", ":hover", "border"],
      minLines: 8,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 3 · CSS Flexbox
// ─────────────────────────────────────────────────────────────────────────────
const h3: Round = {
  number: 3,
  title: "CSS · Flexbox",
  level: "Начальный → Средний",
  intro:
    "Flexbox — главный инструмент современной вёрстки для одномерных макетов. В этом раунде научишься выравнивать и распределять элементы профессионально.\n\n" +
    "**Что изучим:**\n" +
    "• display: flex и flex-direction\n" +
    "• justify-content, align-items — оси и выравнивание\n" +
    "• flex-wrap, gap — перенос и расстояния\n" +
    "• flex-grow, flex-shrink, flex-basis",
  lesson: {
    title: "CSS Flexbox: выравнивание и распределение",
    summary:
      "Как работает flex-контейнер; главная и поперечная оси; выравнивание justify-content/align-items; flex-grow/shrink.",
    readingMinutes: 12,
    sections: [
      {
        heading: "Flex-контейнер и оси",
        tagline: "display: flex — и дети выстраиваются в ряд",
        body:
          "**Что такое Flexbox?** Flexible Box Layout — CSS-модуль для одномерного расположения элементов: либо в строку (row), либо в колонку (column). До Flexbox для центрирования использовали хаки с `float`, `vertical-align` и абсолютным позиционированием. Flexbox сделал это тривиально простым.\n\n" +
          "**Как включить:** достаточно написать `display: flex` на родительском элементе. Он становится **flex-контейнером**, а его прямые дети автоматически становятся **flex-элементами**.\n\n" +
          "**Две оси:**\n" +
          "- **Главная ось (main axis)** — вдоль неё выстраиваются flex-элементы\n" +
          "- **Поперечная ось (cross axis)** — перпендикулярна главной\n\n" +
          "`flex-direction` задаёт направление главной оси:\n" +
          "- `row` (умолчание) — горизонтально, слева направо\n" +
          "- `row-reverse` — горизонтально, справа налево\n" +
          "- `column` — вертикально, сверху вниз\n" +
          "- `column-reverse` — вертикально, снизу вверх\n\n" +
          "`flex-wrap` — что делать если элементы не влезают:\n" +
          "- `nowrap` (умолчание) — сжимаются, не переносятся\n" +
          "- `wrap` — переносятся на следующую строку/колонку\n" +
          "- `wrap-reverse` — переносятся в обратном направлении\n\n" +
          "`gap` — расстояние между элементами (современная замена `margin`). Работает для row-gap (между строками) и column-gap. Можно писать `gap: 16px` (все стороны) или `gap: 12px 24px` (строки | колонки).\n\n" +
          "**Сокращение `flex-flow`**: объединяет direction и wrap: `flex-flow: row wrap`.",
        code:
          "/* Горизонтальный список с переносом */\n.card-grid {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 16px;\n}\n\n" +
          "/* Вертикальный стек */\n.sidebar {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n}\n\n" +
          "/* Горизонтальная навигация */\n.nav {\n  display: flex;\n  flex-flow: row wrap; /* shorthand для direction + wrap */\n  gap: 4px 16px;      /* 4px между строками, 16px между колонками */\n}\n\n" +
          "/* Полезный паттерн: элемент занимает всё доступное место */\n.spacer { flex: 1; }  /* растянется, отталкивая соседей */",
        keyTakeaways: [
          "display: flex на РОДИТЕЛЕ делает его детей flex-элементами.",
          "flex-direction меняет ось: row — горизонталь, column — вертикаль.",
          "flex-wrap: wrap — обязателен для адаптивных сеток карточек.",
          "gap — современный способ задать расстояние, не нужен margin между дочерними элементами.",
          "flex: 1 на дочернем элементе заставляет его занять всё свободное место.",
        ],
        pitfalls: [
          "Flexbox работает только на прямых детях. Глубже вложенные элементы не affected.",
          "flex-wrap по умолчанию nowrap — без него элементы сужаются ниже min-content.",
          "gap не работает как margin-collapse — не схлопывается.",
        ],
      },
      {
        heading: "justify-content и align-items",
        tagline: "justify — по главной, align — по поперечной",
        body:
          "После того как задана ось (`flex-direction`), два главных свойства контролируют размещение:\n\n" +
          "**`justify-content`** — распределение вдоль **главной** оси (в row — горизонталь):\n" +
          "- `flex-start` — все к началу (умолчание)\n" +
          "- `flex-end` — все к концу\n" +
          "- `center` — все по центру\n" +
          "- `space-between` — первый к левому краю, последний к правому, остальные равномерно между ними\n" +
          "- `space-around` — равные отступы по обеим сторонам каждого элемента (крайние = половина)\n" +
          "- `space-evenly` — абсолютно равные промежутки везде, включая края\n\n" +
          "**`align-items`** — выравнивание по **поперечной** оси (в row — вертикаль):\n" +
          "- `stretch` — растягивает до высоты контейнера (умолчание)\n" +
          "- `flex-start` — к верхнему краю\n" +
          "- `flex-end` — к нижнему краю\n" +
          "- `center` — по центру\n" +
          "- `baseline` — выравнивает по базовой линии текста\n\n" +
          "**`align-content`** — как `align-items`, но для **строк** при `flex-wrap: wrap` (когда несколько строк).\n\n" +
          "**`align-self`** — переопределяет `align-items` для конкретного дочернего элемента.\n\n" +
          "**Трюк идеального центрирования** (работает всегда):\n" +
          "```\ndisplay: flex;\njustify-content: center;\nalign-items: center;\n```\n\n" +
          "**flex, flex-grow, flex-shrink, flex-basis:**\n" +
          "- `flex-grow: 1` — элемент занимает всё свободное место пропорционально\n" +
          "- `flex-shrink: 1` — элемент сжимается если не хватает места\n" +
          "- `flex-basis: 280px` — начальный размер перед распределением свободного места\n" +
          "- Сокращение: `flex: 1 1 280px` (grow shrink basis)\n" +
          "- `flex: 1` = `flex: 1 1 0` — равномерно делит пространство\n" +
          "- `flex: none` = `flex: 0 0 auto` — не растёт и не сжимается",
        code:
          "/* Навигационная панель: лого слева, меню справа */\n.navbar {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 0 24px;\n  height: 64px;\n}\n\n" +
          "/* Идеальное вертикальное и горизонтальное центрирование */\n.hero {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  min-height: 100vh;\n}\n\n" +
          "/* Адаптивные карточки: одинаковая ширина, минимум 280px */\n.card-container {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 16px;\n}\n.card {\n  flex: 1 1 280px;  /* grow | shrink | basis */\n  max-width: 400px; /* чтобы не растягивались слишком */\n}\n\n" +
          "/* align-self — индивидуальное выравнивание */\n.sidebar-content {\n  display: flex;\n  align-items: flex-start; /* большинство элементов — вверх */\n}\n.sidebar-content .badge {\n  align-self: center; /* только бейдж — по центру */\n}",
        keyTakeaways: [
          "justify — главная ось (в row это горизонталь). align — поперечная ось (в row это вертикаль).",
          "space-between: равные промежутки МЕЖДУ элементами, крайние прижаты. space-evenly: включая края.",
          "align-items: stretch (умолчание) — все дети одинаковой высоты. center — каждый своей.",
          "flex: 1 на дочерних элементах делает их одинаковой ширины независимо от контента.",
          "align-self переопределяет align-items для одного конкретного элемента.",
        ],
        pitfalls: [
          "justify-content не работает если flex-elements: 1 уже заняли всё пространство.",
          "align-content (не align-items!) управляет строками при flex-wrap: wrap с несколькими строками.",
          "flex: 1 = flex: 1 1 0 (basis=0), а не flex: 1 1 auto. Разница в том, как считается free space.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h3-f1",
      title: "Включить flexbox и задать направление",
      description: "Создай вертикальный flex-стек с отступами.",
      code:
        ".stack {\n" +
        "  {{0}}: flex;\n" +
        "  flex-{{1}}: column;\n" +
        "  {{2}}: 12px;\n" +
        "}",
      answers: [["display"], ["direction"], ["gap"]],
      hints: [
        "Свойство для включения flex-режима",
        "Свойство, задающее направление главной оси",
        "Современный способ задать расстояние между flex-элементами",
      ],
    },
    {
      type: "fill",
      id: "h3-f2",
      title: "Выравнивание: навигационная панель",
      description: "Логотип слева, меню справа, по центру по вертикали.",
      code:
        ".navbar {\n" +
        "  display: flex;\n" +
        "  justify-content: {{0}};\n" +
        "  align-items: {{1}};\n" +
        "  padding: 0 24px;\n" +
        "  height: 64px;\n" +
        "}",
      answers: [["space-between"], ["center"]],
      hints: [
        "Первый элемент прижат к левому краю, последний — к правому",
        "Значение для выравнивания по центру поперечной оси",
      ],
    },
    {
      type: "fill",
      id: "h3-f3",
      title: "flex shorthand",
      description: "Запись flex: 1 1 280px задаёт три значения.",
      code:
        ".card {\n" +
        "  flex-{{0}}: 1;    /* элемент может расти */\n" +
        "  flex-{{1}}: 1;    /* элемент может сжиматься */\n" +
        "  flex-{{2}}: 280px; /* базовый размер */\n" +
        "}",
      answers: [["grow"], ["shrink"], ["basis"]],
      hints: [
        "Рост — элемент берёт свободное пространство",
        "Сжатие — элемент может уменьшаться",
        "Основа — начальный размер до распределения свободного места",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h3-q1",
      title: "flex-wrap",
      question:
        "По умолчанию flex-контейнер не переносит элементы на новую строку. Какое значение свойства flex-wrap включает перенос?",
      answers: [
        "wrap", "flex-wrap: wrap", "flex-wrap:wrap", "wrap — перенос",
        "значение wrap", "wrap строк", "перенос wrap",
      ],
      hint: "Значение — одно слово, противоположное nowrap",
      explanation:
        "flex-wrap: wrap позволяет элементам переноситься на следующую строку, когда не хватает места. Это основа для адаптивных сеток карточек.",
    },
    {
      type: "question",
      id: "h3-q2",
      title: "justify-content и align-items",
      question: "Вы хотите центрировать элемент и по горизонтали, и по вертикали. Какие два CSS-свойства нужно использовать на flex-контейнере?",
      answers: [
        "justify-content: center и align-items: center",
        "justify-content center align-items center",
        "justify-content: center, align-items: center",
        "justify-content и align-items со значением center",
        "justify-content:center align-items:center",
        "оба center",
        "justify-content: center; align-items: center",
        "justify center align center",
        "justify-content и align-items",
      ],
      hint: "justify — главная ось, align — поперечная ось. Оба нужны для 2D центрирования",
      explanation:
        "Для идеального центрирования в flex-контейнере нужны два свойства: justify-content: center (главная ось, в row это горизонталь) и align-items: center (поперечная ось, в row это вертикаль). Это классический паттерн для центрирования любого блока.",
    },
    {
      type: "question",
      id: "h3-q3",
      title: "flex: 1 — что означает?",
      question: "Что делает CSS-правило flex: 1 применённое к дочернему flex-элементу?",
      answers: [
        "занимает всё свободное место",
        "растягивается на всё доступное пространство",
        "flex-grow: 1",
        "элемент растёт и занимает все свободное место",
        "равномерно делит пространство",
        "растёт пропорционально",
        "takes all available space",
        "grows to fill remaining space",
        "1 доля свободного пространства",
      ],
      hint: "flex: 1 — это сокращение от flex-grow: 1, flex-shrink: 1, flex-basis: 0",
      explanation:
        "flex: 1 = flex-grow: 1, flex-shrink: 1, flex-basis: 0. Элемент может расти и занимать всё свободное пространство пропорционально. Если все дочерние элементы имеют flex: 1 — они делят пространство поровну. Часто используется вместе с flex: none для элемента с фиксированным размером.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h3-w1",
      title: "Адаптивная сетка карточек",
      task:
        "Напиши CSS для контейнера `.grid` и дочернего `.item`:\n" +
        "• Контейнер: flexbox, перенос строк, gap 16px\n" +
        "• Элемент: flex-grow: 1, базовая ширина 220px, минимальная 200px\n" +
        "• Элемент: padding 20px, border-radius 8px, background #f5f5f5",
      hints: [
        "Начни: .grid { display: flex; flex-wrap: wrap; gap: 16px; }",
        "Добавь: .item { flex: 1 1 220px; min-width: 200px; }",
        "Добавь стили: padding, border-radius, background",
      ],
      required: ["display: flex", "flex-wrap", "gap", "flex", "padding", "border-radius"],
      minLines: 10,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 4 · JavaScript — основы
// ─────────────────────────────────────────────────────────────────────────────
const h4: Round = {
  number: 4,
  title: "JavaScript · Переменные, функции и массивы",
  level: "Средний",
  intro:
    "JavaScript делает страницы интерактивными. В этом раунде освоишь синтаксис современного JS: const/let, стрелочные функции, деструктуризация и методы массивов.\n\n" +
    "**Что изучим:**\n" +
    "• const / let vs var\n" +
    "• Стрелочные функции\n" +
    "• Массивы: map, filter, reduce, find\n" +
    "• Деструктуризация и spread-оператор",
  lesson: {
    title: "JavaScript: синтаксис ES6+",
    summary:
      "const/let; стрелочные функции; методы массивов map/filter/reduce; деструктуризация.",
    readingMinutes: 13,
    sections: [
      {
        heading: "const и let — забудь про var",
        tagline: "const — по умолчанию, let — только когда нужно изменить",
        body:
          "В JavaScript три способа объявить переменную. Понять разницу — значит писать предсказуемый код.\n\n" +
          "**`var`** (устарел, не использовать):\n" +
          "- Область видимости: функция (не блок `{}`)\n" +
          "- **Всплытие (hoisting)**: переменная поднимается в начало функции, но инициализация нет → `undefined`\n" +
          "- Повторное объявление не вызывает ошибку — источник многих багов\n\n" +
          "**`let`** (ES6+):\n" +
          "- Область видимости: блок `{}` (if, for, while)\n" +
          "- Нет всплытия (точнее — есть Temporal Dead Zone)\n" +
          "- Можно переприсвоить: `let x = 1; x = 2; // OK`\n\n" +
          "**`const`** (ES6+):\n" +
          "- Область видимости: блок `{}`\n" +
          "- Нельзя переприсвоить после инициализации\n" +
          "- Объект/массив можно **мутировать** (менять поля) — нельзя только заменить ссылку\n\n" +
          "**Правило большого пальца**: пиши `const` по умолчанию. Если видишь, что значение изменится — меняй на `let`. `var` не трогай.\n\n" +
          "**Стрелочные функции (Arrow Functions)**:\n" +
          "- Обычная функция: `function sum(a, b) { return a + b; }`\n" +
          "- Стрелочная: `const sum = (a, b) => a + b;`\n" +
          "- Если тело — одно выражение, фигурные скобки и `return` не нужны\n" +
          "- Если один параметр, скобки не нужны: `n => n * 2`\n" +
          "- Ключевое отличие: стрелочные функции не имеют собственного `this` — берут его из окружающего контекста (важно в event handlers)\n\n" +
          "**Деструктуризация и spread:**\n" +
          "- Деструктуризация массива: `const [first, , third] = [1, 2, 3];`\n" +
          "- Деструктуризация объекта: `const { name, age } = user;`\n" +
          "- Spread: `const copy = [...arr];` или `const merged = {...obj1, ...obj2};`\n" +
          "- Rest параметры: `function log(...args) { console.log(args); }`",
        code:
          "// ── const vs let vs var ─────────────────────────────\n" +
          "const name = 'Иван';        // нельзя: name = 'Пётр'\nlet count = 0;              // можно: count++\n\n" +
          "const user = { age: 25 };\nuser.age = 26;              // OK — мутируем поле объекта\n// user = {};               // TypeError — переприсвоение const\n\n" +
          "// var — всплытие и проблемы с областью видимости\nconsole.log(x); // undefined (не ошибка!)\nvar x = 5;\n\n" +
          "for (var i = 0; i < 3; i++) {} console.log(i); // 3 (утечка!)\nfor (let j = 0; j < 3; j++) {} // console.log(j); // ReferenceError\n\n" +
          "// ── Стрелочные функции ─────────────────────────────\n" +
          "// Обычная\nfunction double(n) { return n * 2; }\n\n" +
          "// Стрелочная (однострочная)\nconst triple = n => n * 3;\n\n" +
          "// Многострочная\nconst greet = (name, lang) => {\n  const greeting = lang === 'ru' ? 'Привет' : 'Hello';\n  return `${greeting}, ${name}!`;\n};\n\n" +
          "// ── Деструктуризация ───────────────────────────────\n" +
          "const [a, , c] = [1, 2, 3];   // a=1, c=3\nconst { title, author = 'Аноним' } = book; // со значением по умолчанию\n\n" +
          "// ── Spread и Rest ──────────────────────────────────\n" +
          "const arr2 = [...arr1, 4, 5];              // копия + новые элементы\nconst config = { ...defaults, debug: true }; // merged\n\nconst sum = (...nums) => nums.reduce((a, b) => a + b, 0);\nsum(1, 2, 3, 4); // 10",
        keyTakeaways: [
          "const — умолчание. let — для значений, которые изменяются. var — никогда в новом коде.",
          "const защищает ссылку, не объект: поля объекта можно менять даже в const.",
          "Стрелочные функции: нет собственного this, нет arguments, всегда anonymous.",
          "Деструктуризация: const { a, b } = obj вместо obj.a, obj.b — код короче и понятнее.",
          "Spread (...) — для копирования и слияния. Rest (...) в параметрах — для переменного числа аргументов.",
        ],
        pitfalls: [
          "const массива не защищает его содержимое — push/pop работают. Для защиты — Object.freeze().",
          "Стрелочные функции не подходят как методы объекта если нужен this.",
          "Деструктуризация с переименованием: const { name: userName } = user (не const userName = user.name).",
        ],
      },
      {
        heading: "Методы массивов",
        tagline: "map, filter, reduce — функциональный стиль без циклов",
        body:
          "Вместо `for` циклов современный JS предлагает методы массивов — они чище, выразительнее и без побочных эффектов.\n\n" +
          "**`map(fn)`** — преобразует КАЖДЫЙ элемент. Возвращает новый массив той же длины.\n" +
          "```\n[1, 2, 3].map(n => n * 2)  // [2, 4, 6]\n```\n\n" +
          "**`filter(fn)`** — оставляет только элементы, где предикат вернул `true`.\n" +
          "```\n[1, 2, 3, 4].filter(n => n % 2 === 0)  // [2, 4]\n```\n\n" +
          "**`reduce(fn, initial)`** — сворачивает массив в одно значение (сумму, объект, строку).\n" +
          "```\n[1, 2, 3].reduce((acc, n) => acc + n, 0)  // 6\n```\n\n" +
          "**`find(fn)`** — первый элемент, где условие истинно (или `undefined`).\n" +
          "**`findIndex(fn)`** — индекс первого найденного (или `-1`).\n" +
          "**`some(fn)`** — хотя бы один? Возвращает `true/false`.\n" +
          "**`every(fn)`** — все? Возвращает `true/false`.\n" +
          "**`includes(val)`** — есть такое значение? (`===` сравнение).\n" +
          "**`flat(depth)`** — разворачивает вложенные массивы.\n" +
          "**`flatMap(fn)`** — map + flat(1) за один проход.\n" +
          "**`sort(compareFn)`** — сортировка на месте. Для чисел нужен compareFn: `arr.sort((a, b) => a - b)`.\n\n" +
          "Все методы кроме `sort` и `reverse` **не мутируют** исходный массив.\n\n" +
          "**Мнемоника:** map→преобразуй, filter→отбери, reduce→сожми в одно, find→найди первый.",
        code:
          "const users = [\n  { id: 1, name: 'Аня',  age: 28, active: true },\n  { id: 2, name: 'Иван', age: 15, active: false },\n  { id: 3, name: 'Катя', age: 31, active: true },\n];\n\n" +
          "// map — преобразовать в массив имён\nconst names = users.map(u => u.name);\n// ['Аня', 'Иван', 'Катя']\n\n" +
          "// filter — только активные взрослые\nconst activeAdults = users.filter(u => u.active && u.age >= 18);\n// [{id:1, name:'Аня',...}, {id:3, name:'Катя',...}]\n\n" +
          "// find — найти первого с id=2\nconst ivan = users.find(u => u.id === 2);\n// {id:2, name:'Иван',...}\n\n" +
          "// reduce — посчитать средний возраст\nconst avgAge = users.reduce((sum, u) => sum + u.age, 0) / users.length;\n// 24.67\n\n" +
          "// reduce в объект { id: user }\nconst byId = users.reduce((acc, u) => {\n  acc[u.id] = u;\n  return acc;\n}, {});\n\n" +
          "// Цепочка методов — читается как конвейер\nconst result = users\n  .filter(u => u.active)    // оставляем активных\n  .map(u => u.name)         // берём имена\n  .sort()                   // сортируем\n  .join(', ');              // 'Аня, Катя'\n\n" +
          "// some / every\nconst anyMinor = users.some(u => u.age < 18);  // true\nconst allActive = users.every(u => u.active);   // false",
        keyTakeaways: [
          "map → новый массив той же длины. filter → подмножество. reduce → одно значение (любого типа).",
          "find → первый элемент или undefined. findIndex → индекс или -1.",
          "some/every → логические вопросы про коллекцию. includes → есть ли значение.",
          "sort мутирует массив! Для безопасной сортировки: [...arr].sort(...)",
          "Цепочка методов читается как конвейер: фильтруем → преобразуем → сортируем → выводим.",
        ],
        pitfalls: [
          "sort() без compareFn сортирует лексикографически: [1, 10, 2].sort() → [1, 10, 2].",
          "reduce требует начальное значение вторым параметром — иначе ошибка на пустом массиве.",
          "map всегда возвращает новый массив той же длины — не используй map для побочных эффектов (для этого forEach).",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h4-f1",
      title: "const vs let",
      description: "Выбери правильное объявление для каждой переменной.",
      code:
        "// Имя — не изменится\n{{0}} username = 'alice';\n\n" +
        "// Счётчик — будет увеличиваться\n{{1}} counter = 0;\ncounter++;\n\n" +
        "// Список — мутируется, но переменная не переприсваивается\n{{2}} todos = [];\ntodos.push('Сделать домашку');",
      answers: [["const"], ["let"], ["const"]],
      hints: [
        "Переменная не будет переприсвоена — используй более строгий вариант",
        "Значение будет изменяться (counter++) — нужен изменяемый вариант",
        "Переменная указывает на тот же массив — const подходит",
      ],
    },
    {
      type: "fill",
      id: "h4-f2",
      title: "filter и reduce",
      description: "Отфильтруй числа больше 10, потом найди их сумму.",
      code:
        "const scores = [5, 15, 8, 22, 3, 11];\n\n" +
        "const bigScores = scores.{{0}}(n => n > 10);\n" +
        "// [15, 22, 11]\n\n" +
        "const total = bigScores.{{1}}(({{2}}, n) => acc + n, 0);\n" +
        "// 48",
      answers: [["filter"], ["reduce"], ["acc"]],
      hints: [
        "Метод, оставляющий только элементы где условие истинно",
        "Метод, сворачивающий массив в одно значение",
        "Имя аккумулятора в reduce — сокращение от accumulator",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h4-q1",
      title: "Деструктуризация",
      question:
        "Что выведет этот код?\nconst [first, , third] = [10, 20, 30];\nconsole.log(first, third);",
      answers: [
        "10 30", "10, 30", "10 и 30", "first=10 third=30",
        "10 30 (20 пропускается)", "10 30 — пропускается второй элемент",
      ],
      hint: "Двойная запятая пропускает элемент при деструктуризации массива",
      explanation:
        "Деструктуризация [first, , third] = [10, 20, 30]: first = 10, средний (20) пропускается двойной запятой, third = 30. Итог: 10 30.",
    },
    {
      type: "question",
      id: "h4-q2",
      title: "map vs forEach",
      question:
        "Чем отличается map() от forEach()? Какой из них возвращает новый массив?",
      answers: [
        "map", "map()", "map возвращает массив", "map — возвращает новый массив",
        "map возвращает, forEach нет", "map возвращает новый массив, foreach нет",
        "map creates new array", "map returns array",
      ],
      hint: "forEach — просто перебирает, ничего не возвращает",
      explanation:
        "map() применяет функцию к каждому элементу и возвращает новый массив той же длины. forEach() просто выполняет функцию для каждого элемента и всегда возвращает undefined. Используй map, когда нужен преобразованный массив, и forEach — только для побочных эффектов.",
    },
    {
      type: "question",
      id: "h4-q3",
      title: "const vs let",
      question: "Можно ли изменить поле объекта объявленного через const?\nconst user = { age: 25 };\nuser.age = 26; // ?",
      answers: [
        "да", "можно", "yes", "да, можно", "да — это мутация объекта",
        "можно, это мутация", "да, const защищает ссылку не объект",
        "да, поля можно изменять", "const защищает только ссылку",
        "работает, объект мутируется", "да — переприсваивать нельзя но мутировать можно",
      ],
      hint: "const запрещает переприсвоение переменной, но не мутацию самого объекта",
      explanation:
        "Да, можно. const защищает только ссылку переменной — нельзя написать user = {} или user = null. Но поля объекта (user.age) можно менять. Для полной защиты используй Object.freeze(user).",
    },
    {
      type: "question",
      id: "h4-q4",
      title: "Стрелочные функции и this",
      question: "В чём главное отличие стрелочной функции от обычной в отношении this?",
      answers: [
        "нет собственного this", "стрелочная не имеет this",
        "берёт this из окружения", "лексический this",
        "наследует this из внешнего контекста", "this из замыкания",
        "arrow function не имеет собственного this",
        "no own this", "lexical this", "this из внешнего scope",
        "берёт this от родителя",
      ],
      hint: "Стрелочные функции не создают собственный контекст выполнения",
      explanation:
        "Стрелочные функции не имеют собственного this. Они берут его из лексического окружения (из функции/контекста где были объявлены). Это делает их удобными в callback-функциях и методах класса, где нужен this родителя. Обычные функции создают свой this при вызове.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h4-w1",
      title: "Обработка данных",
      task:
        "Есть массив объектов пользователей. Напиши код:\n" +
        "1. Отфильтруй только активных (active: true)\n" +
        "2. Преобразуй в массив имён (поле name)\n" +
        "3. Выведи в консоль\n\n" +
        "const users = [\n  {name:'Аня', active:true},\n  {name:'Иван', active:false},\n  {name:'Катя', active:true}\n];",
      hints: [
        "Начни с users.filter(u => u.active)",
        "Цепочкой добавь .map(u => u.name)",
        "Сохрани в const activeNames и выведи console.log(activeNames)",
      ],
      required: ["filter", "map", "active", "name"],
      minLines: 3,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 5 · JavaScript DOM и события
// ─────────────────────────────────────────────────────────────────────────────
const h5: Round = {
  number: 5,
  title: "JavaScript · DOM и обработка событий",
  level: "Средний",
  intro:
    "DOM (Document Object Model) — программный интерфейс к HTML-документу. Через него JS читает и изменяет страницу в реальном времени.\n\n" +
    "**Что изучим:**\n" +
    "• querySelector / querySelectorAll\n" +
    "• Изменение содержимого и стилей\n" +
    "• addEventListener — обработка кликов, ввода, форм\n" +
    "• Создание и удаление элементов",
  lesson: {
    title: "JavaScript: DOM и события",
    summary:
      "Поиск элементов; изменение DOM; обработчики событий; создание/удаление узлов.",
    readingMinutes: 12,
    sections: [
      {
        heading: "Поиск элементов и изменение DOM",
        tagline: "querySelector — швейцарский нож для поиска элементов",
        body:
          "**DOM (Document Object Model)** — дерево объектов, которое браузер строит из HTML. Каждый тег → узел дерева. JavaScript может читать и изменять это дерево в любой момент, и браузер мгновенно перерисует страницу.\n\n" +
          "**Поиск элементов:**\n" +
          "- `document.querySelector(selector)` — первый элемент по CSS-селектору, или `null` если не найден\n" +
          "- `document.querySelectorAll(selector)` — NodeList всех совпадений (итерируемый, но не Array)\n" +
          "- `document.getElementById('id')` — быстрее, но только по id\n" +
          "- `el.querySelector(...)` — поиск внутри конкретного элемента (не всего документа)\n\n" +
          "**Изменение содержимого:**\n" +
          "- `el.textContent = 'текст'` — устанавливает текстовое содержимое (безопасно, HTML экранируется)\n" +
          "- `el.innerHTML = '<b>текст</b>'` — парсит строку как HTML (опасно с пользовательскими данными — XSS!)\n" +
          "- `el.value` — значение input/textarea/select\n\n" +
          "**Изменение атрибутов:**\n" +
          "- `el.setAttribute('src', url)` / `el.getAttribute('src')` — любой атрибут\n" +
          "- `el.href`, `el.src`, `el.disabled` — прямой доступ к стандартным атрибутам\n" +
          "- `el.dataset.userId` ↔ `data-user-id` в HTML\n\n" +
          "**Изменение стилей (предпочитай классы!):**\n" +
          "- `el.classList.add('active')` — добавить класс\n" +
          "- `el.classList.remove('hidden')` — убрать класс\n" +
          "- `el.classList.toggle('open')` — переключить: если есть — убери, если нет — добавь\n" +
          "- `el.classList.contains('active')` — проверить наличие класса (true/false)\n" +
          "- `el.classList.replace('old', 'new')` — заменить класс\n" +
          "- `el.style.display = 'none'` — инлайн-стиль (избегай, только для динамических вычислений)\n\n" +
          "**Создание и добавление элементов:**\n" +
          "- `document.createElement('div')` — создать новый элемент\n" +
          "- `parent.appendChild(child)` — добавить в конец\n" +
          "- `parent.prepend(child)` — добавить в начало\n" +
          "- `parent.insertBefore(newEl, refEl)` — вставить перед\n" +
          "- `el.remove()` — удалить элемент\n" +
          "- `parent.replaceChild(newEl, oldEl)` — заменить\n\n" +
          "**`el.closest(selector)`** — ищет ближайшего предка, соответствующего селектору (включая сам элемент). Полезно в делегировании.",
        code:
          "// Поиск\nconst btn = document.querySelector('#submit-btn');\nconst items = document.querySelectorAll('.card');\nconst input = document.querySelector('input[name=\"email\"]');\n\n" +
          "// NodeList → Array для array-методов\nconst cardsArr = Array.from(items);\nconst visibleCards = [...items].filter(c => !c.hidden);\n\n" +
          "// Содержимое\nbtn.textContent = 'Отправить'; // безопасно\nbtn.innerHTML = '<span>✓</span> Готово'; // HTML — осторожно!\n\n" +
          "// Классы\nbtn.classList.add('loading');\nbtn.classList.remove('error');\nbtn.classList.toggle('active');\nif (btn.classList.contains('disabled')) { /* ... */ }\n\n" +
          "// Атрибуты\nconst img = document.querySelector('img');\nimg.setAttribute('alt', 'Новое описание');\nconsole.log(img.src);  // полный URL\n\n" +
          "// Создание элементов\nconst newCard = document.createElement('article');\nnewCard.classList.add('card');\nnewCard.textContent = 'Новая карточка';\ndocument.querySelector('.grid').appendChild(newCard);\n\n" +
          "// dataset\nconst deleteBtn = document.querySelector('[data-action=\"delete\"]');\nconsole.log(deleteBtn.dataset.action); // 'delete'\nconsole.log(deleteBtn.dataset.userId); // значение data-user-id",
        keyTakeaways: [
          "querySelector принимает любой CSS-селектор — тот же синтаксис что в CSS-файле.",
          "textContent безопаснее innerHTML при работе с пользовательскими данными (защита от XSS).",
          "classList предпочтительнее el.style — стили остаются в CSS-файле, JS только переключает классы.",
          "querySelectorAll возвращает NodeList (не Array). Для методов массивов нужен Array.from() или [...list].",
          "el.closest(selector) — удобен для всплытия событий: находит нужного предка из любого потомка.",
        ],
        pitfalls: [
          "querySelector возвращает null если элемент не найден — el.textContent на null → TypeError.",
          "innerHTML с пользовательскими данными — XSS атака. Используй textContent или DOMPurify.",
          "appendChild перемещает существующий элемент, не клонирует — используй cloneNode(true) для копии.",
        ],
      },
      {
        heading: "События: addEventListener",
        tagline: "Браузер сообщает о действиях пользователя через события",
        body:
          "**Событие** — сигнал браузера о том, что что-то произошло: пользователь кликнул, нажал клавишу, страница загрузилась.\n\n" +
          "`element.addEventListener(type, handler, options)` — подписаться на событие.\n" +
          "`element.removeEventListener(type, handler)` — отписаться (handler должен быть той же функцией!).\n\n" +
          "**Основные типы событий:**\n" +
          "- Мышь: `click`, `dblclick`, `mousedown`, `mouseup`, `mousemove`, `mouseenter`, `mouseleave`\n" +
          "- Клавиатура: `keydown`, `keyup`, `keypress` (устарел)\n" +
          "- Форма: `submit`, `input`, `change`, `focus`, `blur`, `reset`\n" +
          "- Документ: `DOMContentLoaded`, `load`, `resize`, `scroll`\n\n" +
          "**Объект события `event`:**\n" +
          "- `e.target` — элемент, на котором произошло событие\n" +
          "- `e.currentTarget` — элемент, к которому прикреплён обработчик\n" +
          "- `e.type` — тип события (`'click'`, `'submit'` и т.д.)\n" +
          "- `e.key`, `e.code` — для keyboard events\n" +
          "- `e.clientX`, `e.clientY` — для mouse events (координаты)\n\n" +
          "**`e.preventDefault()`** — отменить действие браузера:\n" +
          "- submit → не перезагружать страницу\n" +
          "- ссылка → не переходить\n" +
          "- Drag → не тащить\n\n" +
          "**`e.stopPropagation()`** — остановить всплытие события (событие не поднимется к родителям).\n\n" +
          "**Всплытие событий (bubbling):** события всплывают от целевого элемента вверх по дереву DOM. Клик по `<button>` → событие получают и button, и его родители.\n\n" +
          "**Делегирование событий (event delegation):**\n" +
          "Вместо N обработчиков на N элементах — один обработчик на родителе. Работает благодаря всплытию.\n" +
          "Преимущества: меньше памяти, работает для динамически добавляемых элементов.",
        code:
          "// Простой клик\nconst btn = document.querySelector('#btn');\nbtn.addEventListener('click', (e) => {\n  btn.textContent = 'Нажато!';\n  btn.disabled = true;\n});\n\n" +
          "// Форма без перезагрузки страницы\nconst form = document.querySelector('#login-form');\nform.addEventListener('submit', (e) => {\n  e.preventDefault(); // ВАЖНО: без этого страница перезагрузится\n  const email = form.querySelector('[name=\"email\"]').value;\n  const password = form.querySelector('[name=\"password\"]').value;\n  console.log('Логин:', { email });\n});\n\n" +
          "// input — реагируем на каждое нажатие клавиши\nconst searchInput = document.querySelector('#search');\nsearchInput.addEventListener('input', (e) => {\n  console.log('Поиск:', e.target.value);\n  filterList(e.target.value);\n});\n\n" +
          "// Клавиатура\ndocument.addEventListener('keydown', (e) => {\n  if (e.key === 'Escape') closeModal();\n  if (e.ctrlKey && e.key === 's') {\n    e.preventDefault();\n    saveDocument();\n  }\n});\n\n" +
          "// Делегирование — один обработчик для всех .card\nconst grid = document.querySelector('.card-grid');\ngrid.addEventListener('click', (e) => {\n  // e.target — элемент по которому кликнули\n  // e.currentTarget — grid (где обработчик)\n  const card = e.target.closest('.card');\n  if (!card) return; // кликнули не на карточку\n  card.classList.toggle('selected');\n});\n\n" +
          "// DOMContentLoaded — DOM готов, но картинки ещё грузятся\ndocument.addEventListener('DOMContentLoaded', () => {\n  console.log('DOM готов, инициализируем приложение');\n  initApp();\n});\n\n" +
          "// Удаление обработчика — нужна ссылка на ту же функцию!\nfunction handleClick(e) { /* ... */ }\nbtn.addEventListener('click', handleClick);\nbtn.removeEventListener('click', handleClick); // OK",
        keyTakeaways: [
          "addEventListener(type, handler) — тип события и функция-обработчик. 'click', 'submit', 'input' самые частые.",
          "e.preventDefault() — отменить действие браузера по умолчанию (перезагрузку формы, переход по ссылке).",
          "e.target — где кликнули. e.currentTarget — где обработчик. При делегировании — разные элементы.",
          "Делегирование событий: один обработчик на родителе через всплытие. Меньше памяти, работает для новых элементов.",
          "DOMContentLoaded — запуск кода когда DOM готов, но раньше чем картинки. load — когда всё загружено.",
        ],
        pitfalls: [
          "removeEventListener требует ту же функцию — анонимные функции нельзя удалить.",
          "e.stopPropagation() нарушает делегирование — используй только если очень нужно.",
          "change на input срабатывает при потере фокуса. input срабатывает при каждом символе.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h5-f1",
      title: "Поиск элементов и classList",
      description: "Найди элементы и управляй классами.",
      code:
        "const header = document.{{0}}('#main-header');\nconst cards = document.{{1}}('.card');\n\n" +
        "header.{{2}} = 'Новый заголовок';\nheader.{{3}}.add('sticky');",
      answers: [["querySelector"], ["querySelectorAll"], ["textContent"], ["classList"]],
      hints: [
        "Метод для поиска одного элемента",
        "Метод для поиска всех элементов",
        "Свойство для текстового содержимого (безопасно)",
        "Объект для управления CSS-классами",
      ],
    },
    {
      type: "fill",
      id: "h5-f2",
      title: "Обработка клика и форма",
      description: "Обработай клик на кнопку и submit формы без перезагрузки.",
      code:
        "btn.{{0}}('{{1}}', () => {\n  box.classList.toggle('hidden');\n});\n\n" +
        "form.addEventListener('submit', ({{2}}) => {\n  {{2}}.{{3}}();\n  const text = input.{{4}};\n});",
      answers: [["addEventListener"], ["click"], ["e", "event", "ev"], ["preventDefault"], ["value"]],
      hints: [
        "Метод для подписки на событие",
        "Тип события при нажатии кнопки мыши",
        "Параметр-объект события",
        "Метод для отмены действия браузера по умолчанию",
        "Свойство input-элемента с введённым значением",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h5-q1",
      title: "Делегирование событий",
      question:
        "Зачем использовать делегирование событий (один обработчик на родителе)?",
      answers: [
        "производительность",
        "для производительности",
        "динамические элементы",
        "работает для динамически добавленных",
        "меньше обработчиков",
        "экономия памяти",
        "один обработчик для всех дочерних",
        "работает для новых элементов",
        "меньше памяти и динамические элементы",
        "event bubbling — всплытие событий",
        "меньше слушателей",
        "performance and dynamic elements",
      ],
      hint: "Подумай: что происходит если добавить новые элементы после навешивания обработчиков?",
      explanation:
        "Два главных преимущества: 1) Производительность — один обработчик вместо N; 2) Динамические элементы — работает для элементов, добавленных после регистрации обработчика. Работает благодаря всплытию событий (bubbling): событие поднимается от target вверх по дереву DOM.",
    },
    {
      type: "question",
      id: "h5-q2",
      title: "e.preventDefault()",
      question: "Для чего используется e.preventDefault() в обработчике события submit формы?",
      answers: [
        "чтобы страница не перезагружалась",
        "отменить перезагрузку страницы",
        "без перезагрузки",
        "предотвратить стандартное поведение браузера",
        "не перезагружать страницу при submit",
        "отмена действия браузера",
        "prevent page reload",
        "prevent default browser behavior",
        "отмена дефолтного поведения",
        "форма не отправляется на сервер стандартным способом",
        "не отправлять форму как обычно",
      ],
      hint: "Что браузер делает по умолчанию при submit формы?",
      explanation:
        "По умолчанию при submit браузер отправляет данные формы на сервер и перезагружает страницу. e.preventDefault() отменяет это действие, позволяя обработать данные через JavaScript (например, через fetch). Без этого страница перезагрузится и все JS-операции прервутся.",
    },
    {
      type: "question",
      id: "h5-q3",
      title: "textContent vs innerHTML",
      question: "Почему textContent безопаснее innerHTML при работе с пользовательскими данными?",
      answers: [
        "xss атака", "защита от xss", "html не интерпретируется",
        "теги не выполняются", "экранирует html",
        "textContent не парсит html", "безопасность от инъекций",
        "html экранируется", "скрипты не запустятся",
        "не интерпретирует скрипты и html",
        "cross-site scripting protection",
        "innerHTML может запустить скрипт",
      ],
      hint: "Что произойдёт если пользователь введёт <script>alert('hack')</script>?",
      explanation:
        "innerHTML парсит строку как HTML — если пользователь введёт <script>alert('hack')</script> или вредоносный HTML, он может выполниться. textContent устанавливает текст как есть, без парсинга — все HTML-теги отображаются как текст. Это защита от XSS (Cross-Site Scripting) атак.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h5-w1",
      title: "Счётчик",
      task:
        "Напиши JS для счётчика:\n" +
        "HTML: <button id=\"inc\">+</button> <span id=\"count\">0</span> <button id=\"dec\">-</button>\n\n" +
        "• let count = 0\n" +
        "• Клик на #inc увеличивает count и обновляет #count\n" +
        "• Клик на #dec уменьшает count и обновляет #count\n" +
        "• Если count < 0 — текст #count становится красным\n\n" +
        "💡 Смотри вкладку «Методичка» → «Счётчик кликов» для полного пошагового объяснения!",
      hints: [
        "let count = 0; const display = document.querySelector('#count');",
        "document.querySelector('#inc').addEventListener('click', () => { count++; display.textContent = count; })",
        "display.style.color = count < 0 ? 'red' : '';",
      ],
      required: ["addEventListener", "click", "textContent", "count"],
      minLines: 8,
      language: "web" as const,
      docRef: "js-click-counter",
    },
    {
      type: "write",
      id: "h5-w2",
      title: "Изменение цвета текста через JS",
      task:
        "Напиши JS для изменения цвета текста:\n" +
        "HTML уже есть:\n" +
        "<input type=\"color\" id=\"picker\" value=\"#6366f1\">\n" +
        "<p id=\"demo-text\">Этот текст меняет цвет!</p>\n\n" +
        "• При каждом изменении #picker (событие 'input') — устанавливай style.color у #demo-text\n" +
        "• При загрузке страницы (событие 'DOMContentLoaded') — установи начальный цвет из picker.value\n\n" +
        "💡 Кнопка «Методичка» ниже → «Изменение цвета текста» — там полное объяснение!",
      hints: [
        "const picker = document.getElementById('picker'); const text = document.getElementById('demo-text');",
        "picker.addEventListener('input', (e) => { text.style.color = e.target.value; });",
        "document.addEventListener('DOMContentLoaded', () => { text.style.color = picker.value; });",
      ],
      required: ["addEventListener", "style.color", "value", "input"],
      minLines: 5,
      language: "web" as const,
      docRef: "js-text-color",
    },
    {
      type: "write",
      id: "h5-w3",
      title: "Живой предпросмотр: поле ввода",
      task:
        "Напиши JS для живого предпросмотра имени:\n" +
        "HTML уже есть:\n" +
        "<input type=\"text\" id=\"name-input\" placeholder=\"Введи своё имя\">\n" +
        "<p id=\"greeting\">Привет, незнакомец!</p>\n\n" +
        "• При каждом вводе символа (событие 'input') — обновляй текст #greeting\n" +
        "• Если поле не пустое — показывай: «Привет, [имя]!»\n" +
        "• Если поле пустое — показывай: «Привет, незнакомец!»\n\n" +
        "💡 Кнопка «Методичка» ниже → «Поля ввода» — там полное объяснение!",
      hints: [
        "const input = document.getElementById('name-input'); const greeting = document.getElementById('greeting');",
        "input.addEventListener('input', () => { ... })",
        "const name = input.value.trim(); greeting.textContent = name ? `Привет, ${name}!` : 'Привет, незнакомец!';",
      ],
      required: ["addEventListener", "input", "value", "textContent"],
      minLines: 5,
      language: "web" as const,
      docRef: "js-text-input",
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 6 · JavaScript — async/await и fetch
// ─────────────────────────────────────────────────────────────────────────────
const h6: Round = {
  number: 6,
  title: "JavaScript · Асинхронность: Promise, async/await, fetch",
  level: "Средний → Сложный",
  intro:
    "Асинхронность — самая важная концепция в JS. Понять её — значит уметь загружать данные с сервера без перезагрузки страницы.\n\n" +
    "**Что изучим:**\n" +
    "• Что такое Promise\n" +
    "• async / await — читаемый асинхронный код\n" +
    "• fetch — HTTP-запросы к API\n" +
    "• try/catch для обработки ошибок сети",
  lesson: {
    title: "JS: асинхронность, Promise, fetch",
    summary:
      "Зачем нужна асинхронность; Promise; async/await; fetch API; обработка ошибок.",
    readingMinutes: 13,
    sections: [
      {
        heading: "Promise и async/await",
        tagline: "async/await — синхронный вид для асинхронного кода",
        body:
          "**Зачем нужна асинхронность?** JavaScript однопоточный — пока идёт одна операция, остальные ждут. Сетевые запросы, таймеры, чтение файлов — могут занять секунды. Без асинхронности браузер бы «замерзал» на всё это время.\n\n" +
          "**Promise** — объект-обещание, который представляет будущий результат операции. Три состояния:\n" +
          "- `pending` — ожидание\n" +
          "- `fulfilled` — успешно завершён (resolve)\n" +
          "- `rejected` — завершён с ошибкой (reject)\n\n" +
          "**Старый стиль (Promise chain):**\n" +
          "```js\nfetch('/api/users')\n  .then(res => res.json())\n  .then(data => render(data))\n  .catch(err => showError(err))\n  .finally(() => hideSpinner());\n```\n\n" +
          "**Современный стиль (async/await):**\n" +
          "- `async function` — объявляет асинхронную функцию, всегда возвращает Promise\n" +
          "- `await expression` — ждёт разрешения Promise. Работает только ВНУТРИ async-функции\n" +
          "- Исключения перехватываются через `try/catch` как в синхронном коде\n\n" +
          "**Параллельное выполнение:**\n" +
          "- `Promise.all([p1, p2, p3])` — ждёт ВСЕ промисы параллельно. Если один упал — все\n" +
          "- `Promise.allSettled([p1, p2])` — ждёт все, возвращает результат каждого\n" +
          "- `Promise.race([p1, p2])` — возвращает первый завершившийся\n\n" +
          "**Последовательное vs параллельное:**\n" +
          "```js\n// Последовательно (медленно: 2 секунды)\nconst a = await fetchA(); // 1 сек\nconst b = await fetchB(); // 1 сек\n\n// Параллельно (быстро: 1 секунда)\nconst [a, b] = await Promise.all([fetchA(), fetchB()]);\n```",
        code:
          "// ── Promise chain (старый стиль) ──────────────────────\nfetch('/api/users')\n  .then(res => {\n    if (!res.ok) throw new Error('HTTP ' + res.status);\n    return res.json();\n  })\n  .then(users => renderUsers(users))\n  .catch(err => console.error('Ошибка:', err))\n  .finally(() => hideLoadingSpinner());\n\n" +
          "// ── async/await (современный стиль) ───────────────────\nasync function loadUsers() {\n  try {\n    const res = await fetch('/api/users');\n    if (!res.ok) throw new Error(`HTTP ${res.status}`);\n    const users = await res.json(); // вторая await!\n    renderUsers(users);\n    return users;\n  } catch (err) {\n    console.error('Ошибка загрузки:', err.message);\n    showErrorMessage(err.message);\n  } finally {\n    hideLoadingSpinner(); // выполняется всегда\n  }\n}\n\n" +
          "// ── Параллельные запросы ──────────────────────────────\nasync function loadDashboard() {\n  // Оба запроса идут ОДНОВРЕМЕННО\n  const [users, posts] = await Promise.all([\n    fetch('/api/users').then(r => r.json()),\n    fetch('/api/posts').then(r => r.json()),\n  ]);\n  console.log(users, posts);\n}\n\n" +
          "// ── Top-level await (в модулях) ───────────────────────\n// В .mjs файлах или type=\"module\" скриптах:\nconst data = await fetch('/api/init').then(r => r.json());\ninitApp(data);",
        keyTakeaways: [
          "async/await — синтаксический сахар над Promise. Делает асинхронный код похожим на синхронный.",
          "await можно использовать ТОЛЬКО внутри async-функции (или в top-level module).",
          "res.ok проверяет HTTP-статус (200-299). fetch НЕ бросает исключение при 404/500!",
          "Promise.all — параллельно, fail-fast. Promise.allSettled — параллельно, ждёт всех.",
          "try/catch/finally: catch для ошибок, finally — для кода который выполняется всегда.",
        ],
        pitfalls: [
          "fetch не бросает ошибку при HTTP 404 или 500 — ВСЕГДА проверяй if (!res.ok) вручную.",
          "await без try/catch — необработанный rejected Promise (UnhandledPromiseRejection).",
          "Два последовательных await — последовательное выполнение. Для параллельного — Promise.all.",
        ],
      },
      {
        heading: "fetch API: GET, POST, DELETE",
        tagline: "fetch — встроенный браузерный HTTP-клиент",
        body:
          "`fetch(url, options?)` — отправляет HTTP-запрос и возвращает Promise с объектом `Response`.\n\n" +
          "**Объект Response:**\n" +
          "- `res.ok` — `true` если статус 200-299\n" +
          "- `res.status` — числовой HTTP-статус (200, 404, 500...)\n" +
          "- `res.json()` — Promise с телом ответа как JS-объект\n" +
          "- `res.text()` — Promise с телом ответа как строка\n" +
          "- `res.blob()` — Promise с телом ответа как бинарные данные\n\n" +
          "**GET запрос:**\n" +
          "`fetch('/api/users')` — метод GET по умолчанию, нет тела.\n\n" +
          "**POST запрос с JSON:**\n" +
          "```js\nfetch('/api/users', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ name: 'Иван' })\n})\n```\n\n" +
          "**DELETE запрос:**\n" +
          "`fetch('/api/users/42', { method: 'DELETE' })`\n\n" +
          "**Отправка FormData** (файлы и поля формы):\n" +
          "```js\nconst formData = new FormData(formElement);\nfetch('/upload', { method: 'POST', body: formData });\n// Content-Type автоматически multipart/form-data\n```\n\n" +
          "**Параметры URL (query params):**\n" +
          "```js\nconst params = new URLSearchParams({ page: 1, limit: 20 });\nfetch('/api/posts?' + params); // /api/posts?page=1&limit=20\n```\n\n" +
          "**CORS:** Запросы к другому домену могут быть заблокированы браузером. Сервер должен отправлять заголовок `Access-Control-Allow-Origin`.",
        code:
          "// ── GET: загрузить пользователей ─────────────────────\nasync function getUsers(page = 1) {\n  const params = new URLSearchParams({ page, limit: 20 });\n  const res = await fetch('/api/users?' + params);\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return res.json();\n}\n\n" +
          "// ── POST: создать пользователя ────────────────────────\nasync function createUser(userData) {\n  const res = await fetch('/api/users', {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n      'Authorization': `Bearer ${getToken()}`, // если нужна авторизация\n    },\n    body: JSON.stringify(userData),\n  });\n  if (!res.ok) {\n    const err = await res.json();\n    throw new Error(err.message || 'Ошибка создания');\n  }\n  return res.json();\n}\n\n" +
          "// ── PUT: обновить ────────────────────────────────────\nasync function updateUser(id, updates) {\n  const res = await fetch(`/api/users/${id}`, {\n    method: 'PUT',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(updates),\n  });\n  if (!res.ok) throw new Error('Ошибка обновления');\n  return res.json();\n}\n\n" +
          "// ── DELETE ───────────────────────────────────────────\nasync function deleteUser(id) {\n  const res = await fetch(`/api/users/${id}`, { method: 'DELETE' });\n  if (!res.ok) throw new Error('Ошибка удаления');\n  return res.status === 204 ? null : res.json();\n}\n\n" +
          "// ── Универсальная обёртка ─────────────────────────────\nasync function api(method, path, body) {\n  const res = await fetch(path, {\n    method,\n    headers: body ? { 'Content-Type': 'application/json' } : {},\n    body: body ? JSON.stringify(body) : undefined,\n  });\n  if (!res.ok) throw new Error(`${method} ${path}: HTTP ${res.status}`);\n  return res.status === 204 ? null : res.json();\n}",
        keyTakeaways: [
          "Content-Type: application/json обязателен при отправке JSON тела запроса.",
          "JSON.stringify — объект → строка (для body). res.json() — строка → объект (из ответа).",
          "204 No Content — успешный ответ без тела. Не вызывай res.json() при 204.",
          "URLSearchParams — стандартный способ передать query параметры без ручного кодирования.",
          "FormData + fetch без Content-Type — браузер сам выставит multipart/form-data с boundary.",
        ],
        pitfalls: [
          "Не вызывай res.json() дважды — тело читается один раз (stream).",
          "Забытый Content-Type при POST JSON — сервер получит данные как text/plain.",
          "fetch не поддерживает timeout нативно — используй AbortController + AbortSignal.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h6-f1",
      title: "async/await: загрузка данных",
      description: "Заполни async-функцию, загружающую пользователей.",
      code:
        "{{0}} function fetchUsers() {\n" +
        "  try {\n" +
        "    const res = {{1}} fetch('/api/users');\n" +
        "    if (!res.{{2}}) throw new Error('HTTP error');\n" +
        "    const data = {{1}} res.{{3}}();\n" +
        "    return data;\n" +
        "  } catch (err) {\n" +
        "    console.error(err);\n" +
        "  }\n" +
        "}",
      answers: [["async"], ["await"], ["ok"], ["json"]],
      hints: [
        "Ключевое слово перед function для создания асинхронной функции",
        "Ключевое слово для ожидания Promise",
        "Свойство Response, true если статус 200-299",
        "Метод Response для получения тела как объекта JS",
      ],
    },
    {
      type: "fill",
      id: "h6-f2",
      title: "fetch POST с JSON",
      description: "Заполни опции для POST-запроса.",
      code:
        "const res = await fetch('/api/data', {\n" +
        "  {{0}}: 'POST',\n" +
        "  {{1}}: { 'Content-Type': 'application/json' },\n" +
        "  {{2}}: JSON.{{3}}(payload),\n" +
        "});",
      answers: [["method"], ["headers"], ["body"], ["stringify"]],
      hints: [
        "HTTP-метод",
        "Объект с HTTP-заголовками",
        "Тело запроса",
        "Метод JSON для преобразования объекта в строку",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h6-q1",
      title: "fetch и коды ошибок",
      question: "Бросит ли fetch() исключение автоматически при ответе сервера 404?",
      answers: [
        "нет", "не бросит", "нет, не бросит", "нет — нужно проверять res.ok",
        "нет, fetch не бросает при 404", "нет — только при сетевой ошибке",
        "no", "нет, нужно проверить res.ok", "не выбросит исключение",
        "не кинет ошибку", "нет — 404 это успешный ответ для fetch",
      ],
      hint: "fetch отклоняет Promise только при сетевой ошибке, а не при HTTP-ошибках",
      explanation:
        "Нет. fetch() отклоняет Promise только если запрос не дошёл до сервера (нет интернета, DNS-ошибка). Ответы 4xx и 5xx считаются «успешными» с точки зрения fetch — промис resolve с объектом Response. Поэтому ВСЕГДА проверяй if (!res.ok) и бросай ошибку явно.",
    },
    {
      type: "question",
      id: "h6-q2",
      title: "Promise.all",
      question:
        "Что делает Promise.all([p1, p2, p3])? Что произойдёт если хотя бы один Promise отклонится?",
      answers: [
        "ждёт все и если один упадёт — вернёт ошибку",
        "отклонится если один упадёт",
        "fail fast — выбросит ошибку при первом отклонении",
        "вернёт rejected promise если хотя бы один rejected",
        "параллельно ждёт все, упадёт если один упал",
        "всё или ничего",
        "если один упал — все упали",
        "reject при первом отклонении",
        "выполняет параллельно, ошибка любого — общая ошибка",
      ],
      hint: "Promise.all — «всё или ничего»",
      explanation:
        "Promise.all ждёт завершения всех переданных Promise параллельно и возвращает массив результатов. Но если хотя бы один Promise отклоняется (reject), Promise.all немедленно отклоняется с той же ошибкой («fail fast»). Для независимой обработки каждого используй Promise.allSettled.",
    },
    {
      type: "question",
      id: "h6-q3",
      title: "async/await и await",
      question: "Где можно использовать await? Можно ли его написать вне async-функции (в обычной функции)?",
      answers: [
        "нет, только внутри async", "нет", "только в async функции",
        "нельзя вне async функции", "только в async",
        "нет, синтаксическая ошибка", "только внутри async function",
        "no, only inside async function", "нет — SyntaxError",
        "только в асинхронной функции",
      ],
      hint: "await — специальный синтаксис, работает только в определённом контексте",
      explanation:
        "await можно использовать только внутри async-функции. Вне её — SyntaxError. Исключение: top-level await в ES2022 модулях (файлы с type=\"module\" или .mjs). В обычных функциях для работы с промисами используй .then()/.catch().",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h6-w1",
      title: "Загрузка и отображение данных",
      task:
        "Напиши async-функцию `loadPosts()`:\n" +
        "1. GET-запрос к 'https://jsonplaceholder.typicode.com/posts?_limit=3'\n" +
        "2. Проверь res.ok и брось ошибку если не ok\n" +
        "3. Парси JSON\n" +
        "4. Выведи title каждого поста через map и console.log\n" +
        "5. try/catch для ошибок\n" +
        "Вызови loadPosts() в конце.",
      hints: [
        "async function loadPosts() { try { const res = await fetch('...'); }",
        "if (!res.ok) throw new Error('Ошибка ' + res.status);",
        "const posts = await res.json(); console.log(posts.map(p => p.title));",
      ],
      required: ["async", "await", "fetch", "res.ok", "json", "map", "title"],
      minLines: 10,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 7 · CSS Grid и адаптивная вёрстка
// ─────────────────────────────────────────────────────────────────────────────
const h7: Round = {
  number: 7,
  title: "CSS · Grid и адаптивная вёрстка",
  level: "Средний → Сложный",
  intro:
    "CSS Grid — мощнейший инструмент для двумерных макетов. В паре с media queries это основа адаптивной вёрстки.\n\n" +
    "**Что изучим:**\n" +
    "• display: grid, grid-template-columns\n" +
    "• fr, auto, minmax — гибкие единицы Grid\n" +
    "• Размещение: grid-column, grid-row\n" +
    "• Media queries: mobile-first подход",
  lesson: {
    title: "CSS Grid и медиа-запросы",
    summary:
      "Grid-контейнер и треки; fr и minmax; размещение элементов; адаптивность через media queries.",
    readingMinutes: 12,
    sections: [
      {
        heading: "CSS Grid: двумерная сетка",
        tagline: "Grid — для строк И колонок одновременно",
        body:
          "**Flexbox vs Grid:** Flexbox — одномерный (строка ИЛИ колонка). Grid — двумерный (строки И колонки одновременно). На практике используют оба: Grid для макета страницы, Flexbox для компонентов.\n\n" +
          "**Как включить Grid:** `display: grid` на родительском элементе.\n\n" +
          "**Основные свойства контейнера:**\n" +
          "- `grid-template-columns` — определяет колонки. `grid-template-rows` — строки.\n" +
          "- `gap` (или `column-gap`/`row-gap`) — расстояния между ячейками\n" +
          "- `grid-template-areas` — именованные области (для сложных макетов)\n\n" +
          "**Единицы Grid:**\n" +
          "- `fr` (fraction) — доля ОСТАВШЕГОСЯ свободного пространства\n" +
          "  - `1fr 2fr 1fr` — три колонки в пропорции 1:2:1\n" +
          "  - `repeat(3, 1fr)` — три равные колонки\n" +
          "- `minmax(min, max)` — диапазон: не меньше min, не больше max\n" +
          "  - `minmax(200px, 1fr)` — минимум 200px, максимум вся доля\n" +
          "- `auto` — занимает место по содержимому\n" +
          "- `repeat(auto-fit, minmax(240px, 1fr))` — **волшебная формула** для адаптивных сеток!\n\n" +
          "**auto-fit vs auto-fill:**\n" +
          "- `auto-fill` — заполняет пустыми треками до конца\n" +
          "- `auto-fit` — схлопывает пустые треки. Обычно нужен auto-fit.\n\n" +
          "**Размещение элементов:**\n" +
          "- `grid-column: 1 / 3` — занять колонки 1 и 2 (до 3)\n" +
          "- `grid-column: 1 / -1` — занять все колонки (от первой до последней)\n" +
          "- `grid-row: 2 / 4` — занять строки 2 и 3\n" +
          "- `grid-area: header` — разместить в именованной области\n\n" +
          "**grid-template-areas** — визуальный ASCII-макет:\n" +
          "```css\ngrid-template-areas:\n  'header header'\n  'sidebar content'\n  'footer footer';\n```",
        code:
          "/* Простая сетка 3 колонки */\n.grid-3 {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 24px;\n}\n\n" +
          "/* Адаптивная сетка БЕЗ media queries */\n.card-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));\n  gap: 16px;\n  /* При ширине 700px → 2 колонки, при 1000px → 4 колонки */\n}\n\n" +
          "/* Классический макет sidebar + content */\n.app-layout {\n  display: grid;\n  grid-template-columns: 260px 1fr;\n  grid-template-rows: auto 1fr auto;\n  min-height: 100vh;\n  gap: 0;\n}\n\n" +
          "/* Именованные области */\n.page {\n  display: grid;\n  grid-template-areas:\n    'header header header'\n    'nav     main   aside'\n    'footer footer footer';\n  grid-template-columns: 200px 1fr 200px;\n  grid-template-rows: 64px 1fr 40px;\n  min-height: 100vh;\n}\n.page-header { grid-area: header; }\n.page-nav    { grid-area: nav; }\n.page-main   { grid-area: main; }\n.page-aside  { grid-area: aside; }\n.page-footer { grid-area: footer; }\n\n" +
          "/* Элемент на всю ширину */\n.full-width {\n  grid-column: 1 / -1; /* от первой до последней линии */\n}\n\n" +
          "/* Плотная упаковка (для галерей с разными размерами) */\n.gallery {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));\n  grid-auto-rows: 200px;\n  grid-auto-flow: dense; /* заполняет дыры */\n  gap: 8px;\n}\n.gallery .large {\n  grid-column: span 2;\n  grid-row: span 2;\n}",
        keyTakeaways: [
          "repeat(auto-fit, minmax(240px, 1fr)) — адаптивная сетка без единой media query.",
          "fr — дробная часть ОСТАВШЕГОСЯ пространства (после фиксированных колонок).",
          "grid-column: 1 / -1 — растянуть на всю ширину независимо от числа колонок.",
          "grid-template-areas — визуальный ASCII макет, делает код самодокументирующимся.",
          "Grid для двумерного макета (строки + колонки). Flexbox для одномерного (строка ИЛИ колонка).",
        ],
        pitfalls: [
          "fr считается от свободного пространства. Если колонки имеют фиксированный min-content — fr может быть меньше ожидаемого.",
          "grid-template-areas: имена без кавычек — ошибка. Каждая строка — отдельная строка в кавычках.",
          "Не путай grid-column-gap (устарел) с column-gap (современный). Используй gap.",
        ],
      },
      {
        heading: "Media queries — адаптивность",
        tagline: "Разные стили для разных экранов",
        body:
          "**Адаптивная вёрстка (Responsive Design)** — страница корректно отображается на любом устройстве: смартфон, планшет, ноутбук, десктоп, TV.\n\n" +
          "**Viewport meta tag** (обязателен!): `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">`. Без него на мобильных страница масштабируется как desktop.\n\n" +
          "**@media** — условный CSS:\n" +
          "```css\n@media (min-width: 768px) { /* планшеты и шире */ }\n@media (max-width: 767px) { /* только мобильные */ }\n@media (prefers-color-scheme: dark) { /* тёмная тема */ }\n@media print { /* при печати */ }\n```\n\n" +
          "**Mobile-first подход (рекомендован):**\n" +
          "- Пишешь стили для мобильных по умолчанию\n" +
          "- Добавляешь стили для бо́льших экранов через `min-width`\n" +
          "- Преимущества: проще код, быстрее загрузка на мобильных\n\n" +
          "**Стандартные брейкпоинты (ориентир, не догма):**\n" +
          "- xs: 0px — мобильные (умолчание)\n" +
          "- sm: 640px — большие телефоны\n" +
          "- md: 768px — планшеты\n" +
          "- lg: 1024px — ноутбуки\n" +
          "- xl: 1280px — десктопы\n" +
          "- 2xl: 1536px — большие мониторы\n\n" +
          "**Современные инструменты без media queries:**\n" +
          "- `clamp(min, preferred, max)` — плавный размер: `font-size: clamp(1rem, 2.5vw, 2rem)`\n" +
          "- `repeat(auto-fit, minmax(240px, 1fr))` — адаптивная Grid сетка\n" +
          "- `flex-wrap: wrap` — перенос Flexbox элементов\n" +
          "- CSS Container Queries `@container` — стили на основе размера родителя (современный стандарт)\n\n" +
          "**Единицы для адаптивности:**\n" +
          "- `vw` / `vh` — % от ширины/высоты viewport\n" +
          "- `svh` / `dvh` — small/dynamic viewport height (для мобильных с адресной строкой)\n" +
          "- `%` — от родителя",
        code:
          "/* ── Mobile-first CSS ────────────────────────────────── */\n\n" +
          "/* По умолчанию: мобильные */\n.nav {\n  display: flex;\n  flex-direction: column; /* вертикальное меню */\n  gap: 4px;\n}\n.hero-title {\n  font-size: 1.5rem;\n}\n.card-grid {\n  grid-template-columns: 1fr; /* одна колонка */\n}\n\n" +
          "/* Планшеты и шире */\n@media (min-width: 768px) {\n  .nav {\n    flex-direction: row; /* горизонтальное меню */\n  }\n  .hero-title {\n    font-size: 2rem;\n  }\n  .card-grid {\n    grid-template-columns: repeat(2, 1fr);\n  }\n}\n\n" +
          "/* Десктопы */\n@media (min-width: 1024px) {\n  .hero-title {\n    font-size: 3rem;\n  }\n  .card-grid {\n    grid-template-columns: repeat(3, 1fr);\n  }\n}\n\n" +
          "/* Тёмная тема */\n@media (prefers-color-scheme: dark) {\n  :root {\n    --bg: #0f172a;\n    --text: #f1f5f9;\n  }\n}\n\n" +
          "/* clamp: плавный заголовок без media query */\n.hero-title {\n  font-size: clamp(1.5rem, 5vw + 1rem, 4rem);\n  /* min: 1.5rem, preferred: 5% viewport + 1rem, max: 4rem */\n}\n\n" +
          "/* CSS Container Queries (современный стандарт) */\n.card-container { container-type: inline-size; }\n@container (min-width: 400px) {\n  .card { flex-direction: row; }\n}",
        keyTakeaways: [
          "Mobile-first: пиши базовые стили для мобильных, расширяй через min-width для больших экранов.",
          "clamp(min, preferred, max) — плавный размер без media query. Идеально для typography.",
          "Viewport meta tag — обязателен для любой адаптивной страницы.",
          "@media (prefers-color-scheme: dark) — поддержка системной тёмной темы.",
          "auto-fit + minmax и flex-wrap: wrap — часто лучше media query для сеток.",
        ],
        pitfalls: [
          "max-width media queries (desktop-first) — устаревший подход, избегай в новых проектах.",
          "1px border при zoom — используй outline или box-shadow для фокус-стилей.",
          "100vh на iOS Safari включает адресную строку — используй 100svh или dvh.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h7-f1",
      title: "Grid: три колонки",
      description: "Создай сетку из трёх равных колонок с отступами.",
      code:
        ".gallery {\n" +
        "  display: {{0}};\n" +
        "  grid-template-{{1}}: {{2}}(3, 1fr);\n" +
        "  {{3}}: 20px;\n" +
        "}",
      answers: [["grid"], ["columns"], ["repeat"], ["gap"]],
      hints: [
        "Значение display для включения Grid",
        "Свойство для задания колонок сетки",
        "CSS-функция для повторения трека",
        "Свойство для расстояний между ячейками",
      ],
    },
    {
      type: "fill",
      id: "h7-f2",
      title: "Адаптивная сетка без media queries",
      description: "Используй auto-fit и minmax для автоматической сетки.",
      code:
        ".cards {\n" +
        "  display: grid;\n" +
        "  grid-template-columns: repeat({{0}}, minmax({{1}}, 1fr));\n" +
        "  gap: 16px;\n" +
        "}",
      answers: [["auto-fit", "auto-fill"], ["200px", "220px", "240px", "250px", "280px"]],
      hints: [
        "Ключевое слово вместо числа — браузер сам считает количество колонок",
        "Минимальная ширина колонки в пикселях (200-280px)",
      ],
    },
    {
      type: "fill",
      id: "h7-f3",
      title: "Media query: mobile → desktop",
      description: "Перепиши сетку на desktop при ширине от 1024px.",
      code:
        ".grid { grid-template-columns: 1fr; }\n\n" +
        "@{{0}} ({{1}}-width: 1024px) {\n" +
        "  .grid { grid-template-columns: repeat(3, 1fr); }\n" +
        "}",
      answers: [["media"], ["min"]],
      hints: [
        "CSS at-правило для условных стилей",
        "Mobile-first использует min-width",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h7-q1",
      title: "fr единица",
      question:
        "Каков размер каждой колонки при grid-template-columns: 1fr 3fr, ширина контейнера 800px (без gap)?",
      answers: [
        "200px и 600px", "200 и 600", "200px, 600px",
        "200px 600px", "первая 200px вторая 600px",
        "200 и 600 пикселей", "200px + 600px",
      ],
      hint: "Всего 4 части: 1fr + 3fr = 4fr. 800px / 4 = ?",
      explanation:
        "1fr + 3fr = 4 части. 800px / 4 = 200px на 1fr. Первая колонка 200px (1×200), вторая 600px (3×200).",
    },
    {
      type: "question",
      id: "h7-q2",
      title: "Grid vs Flexbox",
      question: "В чём принципиальное отличие Grid от Flexbox?",
      answers: [
        "grid двумерный flexbox одномерный",
        "grid для двух осей flexbox для одной",
        "grid — 2D, flexbox — 1D",
        "grid управляет строками и колонками, flexbox только одной осью",
        "grid двумерная раскладка",
        "2D vs 1D",
        "grid rows and columns, flex one axis",
        "grid для таблиц flexbox для строк или колонок",
        "grid — сетки, flex — горизонталь или вертикаль",
        "grid одновременно строки и колонки",
      ],
      hint: "Подумай о количестве направлений, которыми управляет каждый метод",
      explanation:
        "Flexbox — одномерная раскладка: работает либо по горизонтальной, либо по вертикальной оси. Grid — двумерная: управляет одновременно строками И колонками. Практика: Flexbox для навигационных полосок и карточек в строку; Grid для страничных макетов, сеток карточек, сложных форм.",
    },
    {
      type: "question",
      id: "h7-q3",
      title: "repeat и minmax",
      question: "Что делает grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))?",
      answers: [
        "автоматически вычисляет количество колонок",
        "адаптивная сетка без media queries",
        "колонки минимум 200px максимум 1fr",
        "браузер сам решает сколько колонок поместится",
        "responsive grid без media query",
        "auto количество колонок шириной от 200px до 1fr",
        "адаптивная сетка с минимальной шириной 200px",
        "столько колонок сколько влезет минимум 200px",
        "responsive columns fit as many as possible min 200px",
      ],
      hint: "auto-fit = браузер сам считает количество. minmax(200px, 1fr) = минимум 200px",
      explanation:
        "repeat(auto-fit, minmax(200px, 1fr)) — «волшебная» адаптивная формула Grid. auto-fit: браузер сам вычисляет сколько колонок влезет. minmax(200px, 1fr): каждая колонка минимум 200px, максимум равная доля. При узком экране — 1 колонка, при широком — много. Без единого media query!",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h7-w1",
      title: "Адаптивный двухколоночный макет",
      task:
        "Напиши CSS для `.page-layout`:\n" +
        "• Мобильный (по умолчанию): одна колонка, gap 16px\n" +
        "• При width ≥ 768px: sidebar 240px + контент 1fr\n" +
        "• min-height: 100vh\n" +
        "Используй display: grid и @media.",
      hints: [
        ".page-layout { display: grid; grid-template-columns: 1fr; gap: 16px; min-height: 100vh; }",
        "@media (min-width: 768px) {",
        "  .page-layout { grid-template-columns: 240px 1fr; }",
      ],
      required: ["display: grid", "@media", "min-width", "grid-template-columns", "240px"],
      minLines: 7,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 8 · Итоговый проект: LocalStorage и динамический DOM
// ─────────────────────────────────────────────────────────────────────────────
const h8: Round = {
  number: 8,
  title: "HTML/CSS/JS · Итоговый проект: LocalStorage и динамический DOM",
  level: "Сложный",
  intro:
    "Финальный раунд — объединяем всё: семантику, стили, Grid/Flex и JavaScript. Строим полноценное мини-приложение.\n\n" +
    "**Что изучим:**\n" +
    "• LocalStorage для хранения данных\n" +
    "• createElement — динамическое создание элементов\n" +
    "• JSON.stringify / JSON.parse\n" +
    "• Компонентный подход без фреймворка",
  lesson: {
    title: "LocalStorage, createElement, компонентный подход",
    summary:
      "Хранение данных в браузере; динамическое создание DOM; компонентный JS без фреймворка.",
    readingMinutes: 18,
    sections: [
      {
        heading: "LocalStorage",
        tagline: "Данные сохраняются между перезагрузками страницы и браузера",
        body:
          "**LocalStorage** — встроенное хранилище браузера объёмом ~5 МБ. Данные живут постоянно — даже после закрытия браузера и перезагрузки компьютера.\n\n" +
          "**API хранилища:**\n" +
          "- `localStorage.setItem('key', value)` — записать (value обязательно строка)\n" +
          "- `localStorage.getItem('key')` — прочитать (или `null` если ключа нет)\n" +
          "- `localStorage.removeItem('key')` — удалить один ключ\n" +
          "- `localStorage.clear()` — очистить всё хранилище\n" +
          "- `localStorage.length` — количество ключей\n\n" +
          "**Критически важно**: хранит **только строки**! Объекты, массивы и числа нужно сериализовать:\n" +
          "- Сохранить объект/массив: `JSON.stringify(data)`\n" +
          "- Прочитать обратно: `JSON.parse(localStorage.getItem('key') ?? '[]')`\n\n" +
          "**Паттерн «дефолтное значение»:** `?? '[]'` защищает от `null` (когда ключ ещё не создан). Для объекта используй `?? '{}'`, для числа — `?? '0'`.",
        code:
          "// Сохранить массив\nconst todos = ['купить молоко', 'погулять с собакой'];\nlocalStorage.setItem('todos', JSON.stringify(todos));\n\n" +
          "// Прочитать массив (с дефолтом пустого массива)\nconst saved = JSON.parse(localStorage.getItem('todos') ?? '[]');\nconsole.log(saved); // ['купить молоко', 'погулять с собакой']\n\n" +
          "// Утилиты сохранения/загрузки\nfunction load() {\n  return JSON.parse(localStorage.getItem('todos') ?? '[]');\n}\nfunction save(todos) {\n  localStorage.setItem('todos', JSON.stringify(todos));\n}\n\n" +
          "// Пример: счётчик посещений\nconst visits = parseInt(localStorage.getItem('visits') ?? '0', 10) + 1;\nlocalStorage.setItem('visits', String(visits));\nconsole.log(`Ты посетил сайт ${visits} раз(а)`);",
        keyTakeaways: [
          "localStorage хранит строки — всегда оборачивай объекты/массивы в JSON.stringify.",
          "getItem вернёт null если ключа нет — используй `?? '[]'` или `?? '{}'` как дефолт.",
          "localStorage постоянный; sessionStorage очищается при закрытии вкладки.",
          "Объём ~5 МБ — для больших данных используй IndexedDB.",
        ],
        pitfalls: [
          "`localStorage.setItem('arr', [1,2,3])` сохранит строку '1,2,3', а не массив — нет JSON.stringify.",
          "`localStorage.setItem('obj', {a:1})` сохранит '[object Object]' — та же проблема.",
        ],
      },
      {
        heading: "localStorage vs sessionStorage vs cookies",
        tagline: "Три способа хранить данные в браузере — у каждого своя ниша",
        body:
          "Браузер предоставляет три механизма клиентского хранения:\n\n" +
          "**localStorage** — постоянно (до явной очистки). Один origin. JS-only (не отправляется серверу). Применение: настройки, кэш, offline.\n\n" +
          "**sessionStorage** — пока открыта вкладка. Очищается при закрытии. Не шарится между вкладками. Применение: данные многошаговой формы, черновики.\n\n" +
          "**Cookies** — время жизни задаётся `Expires`/`Max-Age`. Отправляются с каждым HTTP-запросом! Поддерживают `HttpOnly` (недоступны JS) и `Secure` (только HTTPS). Применение: токены авторизации, идентификатор сессии.\n\n" +
          "API sessionStorage **идентичен** localStorage — просто замени `localStorage` на `sessionStorage`.",
        code:
          "// localStorage и sessionStorage — одинаковый API\nlocalStorage.setItem('theme', 'dark');       // постоянно\nsessionStorage.setItem('step', '3');          // только эта вкладка\n\n" +
          "// Пример: тема сохраняется навсегда\nfunction setTheme(theme) {\n  document.body.className = theme;\n  localStorage.setItem('theme', theme);\n}\nconst saved = localStorage.getItem('theme') ?? 'light';\nsetTheme(saved);",
        keyTakeaways: [
          "localStorage — постоянно; sessionStorage — только пока открыта вкладка.",
          "Cookies отправляются на сервер автоматически — для авторизации/сессий.",
          "Не храни токены в localStorage — уязвимость к XSS. Для авторизации — httpOnly cookie.",
        ],
      },
      {
        heading: "Динамическое создание элементов",
        tagline: "createElement — программно строим DOM без шаблонных строк",
        body:
          "**Два подхода к созданию HTML из JavaScript:**\n\n" +
          "**1. innerHTML (быстро, но опасно с данными пользователя)**\n" +
          "Безопасно только для статического контента, где данные не от пользователя.\n\n" +
          "**2. createElement + textContent (безопасно всегда)**\n" +
          "- `document.createElement('tag')` — создаёт элемент в памяти\n" +
          "- `el.textContent = text` — текст без разбора HTML (XSS-защита)\n" +
          "- `el.className = 'cls'` — добавляет CSS-класс\n" +
          "- `el.setAttribute('attr', val)` — любой атрибут\n" +
          "- `el.addEventListener('event', fn)` — обработчик событий\n" +
          "- `parent.appendChild(el)` / `parent.prepend(el)` — вставить в DOM\n" +
          "- `el.remove()` — удалить из DOM\n\n" +
          "**Правило:** если данные приходят от пользователя — всегда `textContent`, никогда `innerHTML`.",
        code:
          "// Фабричная функция — возвращает готовый DOM-элемент\nfunction createTodoItem(text, onDelete) {\n  const li = document.createElement('li');\n  li.className = 'todo-item';\n\n  const span = document.createElement('span');\n  span.textContent = text;  // безопасно! экранирует HTML\n\n  const btn = document.createElement('button');\n  btn.textContent = '✕';\n  btn.setAttribute('aria-label', 'Удалить задачу');\n  btn.addEventListener('click', onDelete);  // навешиваем ДО вставки\n\n  li.append(span, btn);  // append принимает несколько аргументов\n  return li;\n}\n\nconst list = document.getElementById('todo-list');\nconst item = createTodoItem('Выучить JS', () => item.remove());\nlist.appendChild(item);",
        keyTakeaways: [
          "textContent автоматически экранирует HTML — защита от XSS.",
          "Навешивай addEventListener до appendChild — элемент в памяти, события уже работают.",
          "Фабричные функции делают код повторно используемым: createTodoItem, createCard и т.д.",
        ],
        pitfalls: [
          "innerHTML с данными пользователя — XSS: `el.innerHTML = userInput` опасно.",
          "appendChild перемещает существующий элемент, а не копирует — если нужна копия, используй cloneNode().",
        ],
      },
      {
        heading: "Event delegation и render-функция",
        tagline: "Один обработчик на весь список и паттерн синхронизации UI с данными",
        body:
          "**Event Delegation (делегирование событий)**\n\n" +
          "Вместо навешивания обработчика на каждый элемент — вешаем **один** на родителя. Событие всплывает вверх (`bubbling`), цель определяем через `event.target`.\n\n" +
          "**Зачем:** динамически добавленные элементы автоматически поддерживаются. Намного эффективнее для больших списков.\n\n" +
          "**Render Function (паттерн перерисовки)**\n\n" +
          "Идея: держи данные в JS-массиве. DOM — только отражение этих данных. При любом изменении вызывай `render()` — он пересоздаёт весь список из актуальных данных.\n\n" +
          "```js\nfunction render() {\n  list.innerHTML = '';\n  todos.forEach(t => list.appendChild(createItem(t)));\n}\n```\n\n" +
          "Это основа работы React, Vue и других фреймворков — просто в них `render` вызывается автоматически при изменении state.",
        code:
          "let todos = load();\nconst list = document.getElementById('todo-list');\n\n// Event Delegation: один обработчик для всех кнопок\nlist.addEventListener('click', (e) => {\n  const btn = e.target.closest('button[data-action]');\n  if (!btn) return;\n\n  const id = Number(btn.closest('li').dataset.id);\n\n  if (btn.dataset.action === 'delete') {\n    todos = todos.filter(t => t.id !== id);\n    save(todos);\n    render();  // перерисовать весь список\n  }\n});\n\n// Render function — UI всегда синхронен с массивом todos\nfunction render() {\n  list.innerHTML = '';\n  todos.forEach(todo => {\n    const li = document.createElement('li');\n    li.dataset.id = todo.id;\n\n    const span = document.createElement('span');\n    span.textContent = todo.text;  // textContent безопасен\n\n    const btn = document.createElement('button');\n    btn.textContent = '✕';\n    btn.dataset.action = 'delete';\n\n    li.append(span, btn);\n    list.appendChild(li);\n  });\n}",
        keyTakeaways: [
          "Event delegation: один слушатель на родителе через e.target.closest() — работает и для динамических элементов.",
          "Render function: данные в массиве — DOM только отражение. При изменении вызови render().",
          "Это основа React/Vue: state меняется → UI автоматически перерисовывается.",
        ],
        pitfalls: [
          "event.target — куда кликнули; event.currentTarget — где навешен обработчик.",
          "innerHTML = '' удаляет все дочерние элементы и их обработчики событий — ок для render-паттерна.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h8-f1",
      title: "LocalStorage: сохранение и загрузка",
      description: "Заполни код сохранения массива и чтения.",
      code:
        "function save(data) {\n" +
        "  localStorage.{{0}}('key', JSON.{{1}}(data));\n" +
        "}\n\n" +
        "function load() {\n" +
        "  return JSON.{{2}}(localStorage.{{3}}('key') ?? '[]');\n" +
        "}",
      answers: [["setItem"], ["stringify"], ["parse"], ["getItem"]],
      hints: [
        "Метод LocalStorage для записи значения",
        "Метод JSON для преобразования объекта в строку",
        "Метод JSON для преобразования строки в объект",
        "Метод LocalStorage для чтения значения",
      ],
    },
    {
      type: "fill",
      id: "h8-f2",
      title: "createElement и удаление",
      description: "Создай элемент и добавь кнопку удаления.",
      code:
        "function addItem(text) {\n" +
        "  const li = document.{{0}}('li');\n" +
        "  li.{{1}} = text;\n\n" +
        "  const btn = document.createElement('button');\n" +
        "  btn.textContent = 'Удалить';\n" +
        "  btn.addEventListener('click', () => li.{{2}}());\n\n" +
        "  li.{{3}}(btn);\n" +
        "  return li;\n" +
        "}",
      answers: [["createElement"], ["textContent"], ["remove"], ["appendChild", "append"]],
      hints: [
        "Метод для создания нового DOM-элемента",
        "Свойство для установки текстового содержимого",
        "Метод DOM-элемента для самоудаления",
        "Метод для добавления дочернего элемента",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h8-q1",
      title: "JSON и LocalStorage",
      question: "Почему нельзя сохранить массив в localStorage без JSON.stringify?",
      answers: [
        "хранит строки",
        "localStorage хранит только строки",
        "только строки",
        "принимает только строки",
        "localstorage принимает только строки",
        "всё конвертируется в строку",
        "объект становится object object",
        "массив становится строкой без структуры",
        "stores only strings",
        "только string тип данных",
        "без json теряется структура данных",
        "превращается в object object или 1,2,3",
      ],
      hint: "Что вернёт localStorage.setItem('k', [1,2,3]) и что сохранится?",
      explanation:
        "localStorage принимает только строки. [1,2,3] конвертируется через .toString() в '1,2,3', а {a:1} — в '[object Object]'. JSON.stringify сохраняет полную структуру данных, JSON.parse восстанавливает её обратно в JS-объект/массив.",
    },
    {
      type: "question",
      id: "h8-q2",
      title: "localStorage vs sessionStorage",
      question: "В чём разница между localStorage и sessionStorage?",
      answers: [
        "localstorage постоянное sessionstorage до закрытия вкладки",
        "localstorage сохраняется, sessionstorage очищается при закрытии",
        "session — пока вкладка открыта, local — постоянно",
        "localstorage не исчезает, sessionstorage до закрытия",
        "sessionStorage живёт до закрытия вкладки",
        "local permanent, session temporary per tab",
        "localstorage persists, sessionstorage clears on tab close",
        "sessionstorage очищается, localstorage нет",
        "сессионное vs постоянное",
      ],
      hint: "Слово 'session' намекает на временность — сессия браузерной вкладки",
      explanation:
        "localStorage — постоянное хранилище: данные сохраняются даже после закрытия браузера и перезагрузки компьютера. sessionStorage — временное: данные живут пока открыта вкладка. При закрытии вкладки sessionStorage очищается. API идентичный — разница только в времени жизни.",
    },
    {
      type: "question",
      id: "h8-q3",
      title: "innerHTML vs createElement",
      question: "Когда лучше использовать createElement вместо innerHTML для создания DOM-элементов?",
      answers: [
        "когда данные от пользователя",
        "с пользовательским вводом",
        "для защиты от xss",
        "безопасность",
        "textContent безопаснее",
        "когда нужна безопасность и навешивание событий",
        "если данные небезопасные",
        "createElement безопаснее с пользовательскими данными",
        "для добавления обработчиков событий",
        "when user data is involved",
        "security and event handlers",
        "если нужны обработчики событий на элементах",
      ],
      hint: "Подумай: что если текст пользователя содержит <script>...</script>?",
      explanation:
        "createElement + textContent безопаснее при работе с пользовательскими данными — защита от XSS. Также createElement удобен для добавления обработчиков событий напрямую. innerHTML проще для статических шаблонов без пользовательских данных, но опасен если в данных может быть HTML.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h8-w1",
      title: "Мини Todo-приложение",
      task:
        "Напиши Todo-приложение на чистом JS:\n\n" +
        "HTML уже есть:\n" +
        "<input id=\"todo-input\" /> <button id=\"add-btn\">Добавить</button> <ul id=\"todo-list\"></ul>\n\n" +
        "Реализуй:\n" +
        "1. При клике #add-btn — добавь задачу в массив todos\n" +
        "2. Создай <li> с текстом и кнопкой 'Удалить'\n" +
        "3. Кнопка удаляет из массива и перерисовывает\n" +
        "4. Сохраняй todos в localStorage при каждом изменении\n" +
        "5. При загрузке — восстанавливай из localStorage",
      hints: [
        "let todos = JSON.parse(localStorage.getItem('todos') ?? '[]');",
        "function render() { list.innerHTML = ''; todos.forEach((t, i) => { ... }) }",
        "Кнопка удаления: todos.splice(i, 1); сохрани и вызови render()",
      ],
      required: ["localStorage", "JSON", "addEventListener", "createElement", "todos"],
      minLines: 20,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Round 9 · Flask + HTML/CSS/JS — полноценный веб-проект
// ─────────────────────────────────────────────────────────────────────────────
const h9: Round = {
  number: 9,
  title: "Flask + HTML/CSS/JS — полноценный веб-проект",
  level: "Средний → Сложный",
  intro:
    "Финальный раунд объединяет всё, что ты изучил. HTML/CSS/JS работают в браузере, а Flask — на сервере. Вместе они образуют полноценное веб-приложение.\n\n" +
    "**Что изучим:**\n" +
    "• Структура Flask-проекта со статическими файлами (CSS, JS)\n" +
    "• Jinja2 — шаблонизатор Flask: переменные, условия, циклы, наследование\n" +
    "• Как JS делает запросы к Flask API через fetch()\n" +
    "• Передача данных из формы в Flask и обратно\n" +
    "• Финальный проект: Todo-приложение с Flask-бэкендом",
  lesson: {
    title: "Flask + HTML/CSS/JS: полный стек",
    summary:
      "Как Flask отдаёт HTML-страницы через Jinja2; как CSS и JS подключаются к Flask; как JavaScript делает API-запросы к серверу через fetch(); полная архитектура простого веб-приложения.",
    readingMinutes: 18,
    sections: [
      {
        heading: "Структура Flask-проекта",
        tagline: "Где живут шаблоны, CSS и JavaScript",
        body:
          "Flask требует строгой структуры папок. Шаблоны (HTML-файлы) хранятся в папке `templates/`, а все статические файлы (CSS, JS, изображения) — в `static/`.\n\n" +
          "```\n" +
          "myproject/\n" +
          "├── app.py              ← Flask-приложение\n" +
          "├── templates/\n" +
          "│   ├── base.html       ← базовый шаблон\n" +
          "│   └── index.html      ← конкретная страница\n" +
          "└── static/\n" +
          "    ├── css/\n" +
          "    │   └── style.css\n" +
          "    └── js/\n" +
          "        └── main.js\n" +
          "```\n\n" +
          "**Подключение статики через `url_for`:**\n\n" +
          "Никогда не пиши `href=\"/static/css/style.css\"` напрямую — Flask может работать в подпапке. Используй `url_for('static', filename='css/style.css')`. Эта функция сама сгенерирует правильный путь.\n\n" +
          "**Запуск приложения:**\n\n" +
          "```python\nfrom flask import Flask\napp = Flask(__name__)\n\n@app.route('/')\ndef index():\n    return render_template('index.html')\n\nif __name__ == '__main__':\n    app.run(debug=True)\n```",
        code:
          "<!-- templates/base.html -->\n" +
          "<!DOCTYPE html>\n" +
          "<html lang=\"ru\">\n" +
          "<head>\n" +
          "  <meta charset=\"UTF-8\" />\n" +
          "  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n" +
          "  <title>{% block title %}Мой сайт{% endblock %}</title>\n" +
          "  <link rel=\"stylesheet\" href=\"{{ url_for('static', filename='css/style.css') }}\">\n" +
          "</head>\n" +
          "<body>\n" +
          "  <nav>\n" +
          "    <a href=\"{{ url_for('index') }}\">Главная</a>\n" +
          "  </nav>\n" +
          "  <main>\n" +
          "    {% block content %}{% endblock %}\n" +
          "  </main>\n" +
          "  <script src=\"{{ url_for('static', filename='js/main.js') }}\"></script>\n" +
          "</body>\n" +
          "</html>",
        keyTakeaways: [
          "Шаблоны → папка templates/, статика (CSS/JS) → папка static/.",
          "url_for('static', filename='...') генерирует правильный путь к файлу.",
          "url_for('имя_функции') генерирует URL для маршрута Flask.",
          "debug=True перезапускает сервер при каждом сохранении файла.",
        ],
        pitfalls: [
          "Не прописывай пути к статике вручную — url_for делает это правильно.",
          "Папки templates/ и static/ должны быть рядом с app.py.",
        ],
      },
      {
        heading: "Jinja2 — шаблонизатор Flask",
        tagline: "Вставляем данные из Python прямо в HTML",
        body:
          "Jinja2 — это язык шаблонов, встроенный в Flask. Он позволяет вставлять Python-переменные в HTML, делать условия и циклы прямо в разметке.\n\n" +
          "**Три вида скобок в Jinja2:**\n\n" +
          "| Синтаксис | Назначение |\n" +
          "|---|---|\n" +
          "| `{{ переменная }}` | Вывести значение |\n" +
          "| `{% if/for/block %}` | Управляющие конструкции |\n" +
          "| `{# комментарий #}` | Комментарий (не виден в HTML) |\n\n" +
          "**Передача данных из Flask в шаблон:**\n\n" +
          "```python\n@app.route('/users')\ndef users():\n    data = [\n        {'name': 'Алиса', 'age': 25},\n        {'name': 'Боб', 'age': 30},\n    ]\n    return render_template('users.html', users=data, title='Пользователи')\n```\n\n" +
          "**Использование в шаблоне:**\n\n" +
          "```html\n<h1>{{ title }}</h1>\n{% for user in users %}\n  <div class=\"user-card\">\n    <strong>{{ user.name }}</strong>\n    {% if user.age >= 18 %}\n      <span class=\"badge\">Взрослый</span>\n    {% else %}\n      <span class=\"badge\">Несовершеннолетний</span>\n    {% endif %}\n  </div>\n{% endfor %}\n```\n\n" +
          "**Наследование шаблонов** (`{% extends %}` + `{% block %}`):\n\n" +
          "Базовый шаблон содержит общую структуру (nav, footer, подключение CSS). Дочерние шаблоны наследуют его и переопределяют только нужные блоки. Это исключает дублирование кода.",
        code:
          "<!-- templates/users.html -->\n" +
          "{% extends 'base.html' %}\n\n" +
          "{% block title %}Пользователи — Мой сайт{% endblock %}\n\n" +
          "{% block content %}\n" +
          "<h1>Список пользователей ({{ users|length }})</h1>\n\n" +
          "{% if users %}\n" +
          "  <ul class=\"user-list\">\n" +
          "  {% for user in users %}\n" +
          "    <li class=\"user-item\">\n" +
          "      <strong>{{ user.name }}</strong> — {{ user.age }} лет\n" +
          "    </li>\n" +
          "  {% endfor %}\n" +
          "  </ul>\n" +
          "{% else %}\n" +
          "  <p>Пользователей пока нет.</p>\n" +
          "{% endif %}\n" +
          "{% endblock %}",
        keyTakeaways: [
          "{{ }} — вывод данных; {% %} — управление; {# #} — комментарий.",
          "{% extends 'base.html' %} — наследование; {% block content %}{% endblock %} — зона замены.",
          "Фильтры Jinja2: {{ text|length }}, {{ text|upper }}, {{ text|default('—') }}.",
          "Доступ к полям объекта: user.name или user['name'] — оба варианта работают.",
        ],
        pitfalls: [
          "Блок должен называться одинаково в base.html и в дочернем шаблоне.",
          "Переменные из Python приходят только те, что передал render_template().",
        ],
      },
      {
        heading: "JavaScript делает запросы к Flask API",
        tagline: "fetch() соединяет фронтенд и бэкенд",
        body:
          "Современные веб-приложения не перезагружают страницу при каждом действии. Вместо этого JavaScript делает фоновые HTTP-запросы к серверу через `fetch()`, а Flask отвечает JSON-данными.\n\n" +
          "**Шаг 1 — Flask-маршрут для API:**\n\n" +
          "```python\nfrom flask import Flask, jsonify, request\n\n@app.route('/api/todos', methods=['GET'])\ndef get_todos():\n    return jsonify({'todos': todos})\n\n@app.route('/api/todos', methods=['POST'])\ndef add_todo():\n    data = request.get_json()\n    todo = {'id': len(todos)+1, 'text': data['text'], 'done': False}\n    todos.append(todo)\n    return jsonify(todo), 201\n```\n\n" +
          "**Шаг 2 — JavaScript делает запрос:**\n\n" +
          "```javascript\n// GET-запрос — получить данные\nasync function loadTodos() {\n  const res = await fetch('/api/todos');\n  const data = await res.json();\n  renderTodos(data.todos);\n}\n\n// POST-запрос — отправить данные\nasync function addTodo(text) {\n  const res = await fetch('/api/todos', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ text })\n  });\n  const newTodo = await res.json();\n  return newTodo;\n}\n```\n\n" +
          "**Обработка ошибок:**\n\n" +
          "```javascript\ntry {\n  const res = await fetch('/api/todos');\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  const data = await res.json();\n  // ...\n} catch (err) {\n  console.error('Ошибка:', err.message);\n  showError('Не удалось загрузить данные');\n}\n```",
        code:
          "// static/js/main.js\nconst todoInput = document.getElementById('todo-input');\nconst addBtn = document.getElementById('add-btn');\nconst todoList = document.getElementById('todo-list');\n\n// Загрузка при старте\ndocument.addEventListener('DOMContentLoaded', loadTodos);\n\nasync function loadTodos() {\n  try {\n    const res = await fetch('/api/todos');\n    if (!res.ok) throw new Error(`Ошибка ${res.status}`);\n    const data = await res.json();\n    renderAll(data.todos);\n  } catch (err) {\n    todoList.innerHTML = `<li style=\"color:red\">${err.message}</li>`;\n  }\n}\n\naddBtn.addEventListener('click', async () => {\n  const text = todoInput.value.trim();\n  if (!text) return;\n  const res = await fetch('/api/todos', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ text })\n  });\n  const todo = await res.json();\n  appendTodo(todo);\n  todoInput.value = '';\n});\n\nfunction renderAll(todos) {\n  todoList.innerHTML = '';\n  todos.forEach(appendTodo);\n}\n\nfunction appendTodo(todo) {\n  const li = document.createElement('li');\n  li.textContent = todo.text;\n  todoList.appendChild(li);\n}",
        keyTakeaways: [
          "Flask-маршрут с jsonify() → отдаёт JSON; JS с fetch() → получает JSON.",
          "POST-запрос требует Content-Type: application/json и JSON.stringify в body.",
          "Flask читает JSON через request.get_json(), а не request.form.",
          "DOMContentLoaded гарантирует, что DOM готов до выполнения JS.",
          "Всегда проверяй res.ok перед .json() — иначе можно не заметить HTTP-ошибку.",
        ],
        pitfalls: [
          "Без 'Content-Type': 'application/json' Flask не сможет прочитать тело запроса.",
          "Не смешивай fetch() с формами — либо то, либо другое.",
        ],
      },
      {
        heading: "Работа с формами: Flask + HTML",
        tagline: "Два подхода: обычная отправка и AJAX",
        body:
          "Есть два способа отправить данные формы в Flask:\n\n" +
          "**1. Классический `<form method=\"POST\">` — перезагрузка страницы:**\n\n" +
          "```python\n@app.route('/login', methods=['GET', 'POST'])\ndef login():\n    if request.method == 'POST':\n        username = request.form['username']\n        password = request.form['password']\n        if check_user(username, password):\n            return redirect(url_for('dashboard'))\n        return render_template('login.html', error='Неверный логин')\n    return render_template('login.html')\n```\n\n" +
          "```html\n<form method=\"POST\" action=\"/login\">\n  <input name=\"username\" placeholder=\"Логин\" />\n  <input name=\"password\" type=\"password\" />\n  <button type=\"submit\">Войти</button>\n  {% if error %}<p class=\"error\">{{ error }}</p>{% endif %}\n</form>\n```\n\n" +
          "**2. AJAX-отправка — без перезагрузки:**\n\n" +
          "```javascript\nconst form = document.querySelector('form');\nform.addEventListener('submit', async (e) => {\n  e.preventDefault();  // ← блокируем стандартную отправку\n  const formData = new FormData(form);\n  const body = Object.fromEntries(formData);\n  const res = await fetch('/api/login', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(body)\n  });\n  const data = await res.json();\n  if (data.ok) {\n    window.location.href = '/dashboard';\n  } else {\n    showError(data.message);\n  }\n});\n```\n\n" +
          "**Когда что использовать?**\n\n" +
          "- Классический `<form>` → простые формы, SEO-страницы, не нужен JS\n" +
          "- AJAX (fetch) → нужна динамика, валидация без перезагрузки, SPA",
        code:
          "# app.py — полный пример мини-приложения\nfrom flask import Flask, render_template, request, jsonify, redirect, url_for\n\napp = Flask(__name__)\ntodos = []\n\n@app.route('/')\ndef index():\n    return render_template('index.html', todos=todos)\n\n@app.route('/api/todos', methods=['GET'])\ndef api_get_todos():\n    return jsonify({'todos': todos})\n\n@app.route('/api/todos', methods=['POST'])\ndef api_add_todo():\n    data = request.get_json()\n    if not data or not data.get('text'):\n        return jsonify({'error': 'text required'}), 400\n    todo = {'id': len(todos) + 1, 'text': data['text'], 'done': False}\n    todos.append(todo)\n    return jsonify(todo), 201\n\n@app.route('/api/todos/<int:todo_id>', methods=['DELETE'])\ndef api_delete_todo(todo_id):\n    global todos\n    todos = [t for t in todos if t['id'] != todo_id]\n    return jsonify({'ok': True})\n\nif __name__ == '__main__':\n    app.run(debug=True)",
        keyTakeaways: [
          "request.form → данные из классической HTML-формы (application/x-www-form-urlencoded).",
          "request.get_json() → данные из fetch() с JSON-телом.",
          "e.preventDefault() останавливает стандартную отправку формы.",
          "FormData + Object.fromEntries() — удобный способ собрать данные формы в объект.",
          "Flask может одновременно отдавать HTML-страницы И JSON для API.",
        ],
        pitfalls: [
          "Не забывай e.preventDefault() при AJAX-отправке формы.",
          "request.form и request.get_json() — разные объекты, не перепутай.",
        ],
      },
    ],
  },
  fills: [
    {
      type: "fill",
      id: "h9-f1",
      title: "url_for в шаблоне Flask",
      description: "Заполни пропуски: подключи CSS и JS к Flask-шаблону через url_for.",
      code:
        "<!-- base.html -->\n" +
        "<!DOCTYPE html>\n" +
        "<html lang=\"ru\">\n" +
        "<head>\n" +
        "  <link rel=\"stylesheet\"\n" +
        "    href=\"{{ {{0}}('static', filename='css/style.css') }}\">\n" +
        "</head>\n" +
        "<body>\n" +
        "  {% {{1}} content %}{% endblock %}\n" +
        "  <script src=\"{{ url_for('{{2}}', filename='js/main.js') }}\"></script>\n" +
        "</body>\n" +
        "</html>",
      answers: [["url_for"], ["block"], ["static"]],
      hints: [
        "Функция Flask для генерации URL к файлам и маршрутам",
        "Тег Jinja2 для определения области, которую дочерний шаблон может заменить",
        "Имя встроенного маршрута Flask для отдачи статических файлов",
      ],
    },
    {
      type: "fill",
      id: "h9-f2",
      title: "Jinja2: цикл и условие",
      description: "Заполни пропуски в шаблоне Jinja2, выводящем список пользователей.",
      code:
        "{% {{0}} 'base.html' %}\n" +
        "{% block content %}\n" +
        "<h1>Пользователи: {{ users|{{1}} }}</h1>\n" +
        "{% {{2}} user in users %}\n" +
        "  <p>{{ {{3}}.name }}</p>\n" +
        "{% endfor %}\n" +
        "{% endblock %}",
      answers: [["extends"], ["length"], ["for"], ["user"]],
      hints: [
        "Тег Jinja2 для наследования базового шаблона",
        "Фильтр Jinja2 для получения длины коллекции",
        "Тег Jinja2 для перебора элементов списка",
        "Имя переменной-элемента из цикла for user in users",
      ],
    },
    {
      type: "fill",
      id: "h9-f3",
      title: "Flask: маршрут и рендер",
      description: "Заполни пропуски в Flask-маршруте, который читает POST-данные и возвращает JSON.",
      code:
        "from flask import Flask, {{0}}, {{1}}\n\n" +
        "app = Flask(__name__)\ntodos = []\n\n" +
        "@app.{{2}}('/api/todos', methods=['POST'])\n" +
        "def add_todo():\n" +
        "    data = request.{{3}}()\n" +
        "    todos.append({'text': data['text']})\n" +
        "    return jsonify({'ok': True}), 201",
      answers: [["jsonify"], ["request"], ["route"], ["get_json"]],
      hints: [
        "Функция Flask для создания JSON-ответа",
        "Объект Flask для доступа к данным входящего запроса",
        "Декоратор Flask для регистрации маршрута (без @app.)",
        "Метод request для чтения JSON-тела запроса",
      ],
    },
  ],
  questions: [
    {
      type: "question",
      id: "h9-q1",
      title: "Зачем url_for вместо прямого пути?",
      question: "Почему лучше использовать url_for('static', filename='...') вместо '/static/css/style.css'?",
      answers: [
        "url_for генерирует правильный путь",
        "url_for автоматически генерирует путь",
        "автоматически генерирует правильный url",
        "генерирует путь независимо от расположения приложения",
        "работает при любом расположении приложения",
        "не сломается при изменении структуры",
        "автоматически адаптируется к префиксу приложения",
        "generates correct path regardless of app location",
        "не нужно жёстко прописывать пути",
        "url_for добавляет кэш-буст версию",
      ],
      hint: "Что происходит, если Flask запускается не в корне сайта, а в подпапке?",
      explanation:
        "url_for генерирует правильный URL независимо от того, где запущено Flask-приложение. Если приложение развёрнуто в подпапке (например, /myapp/), то '/static/...' сломается, а url_for('static', filename='...') автоматически добавит нужный префикс. Кроме того, url_for добавляет ?v=... для кэш-буста файлов.",
    },
    {
      type: "question",
      id: "h9-q2",
      title: "request.form vs request.get_json()",
      question: "В чём разница между request.form и request.get_json() во Flask?",
      answers: [
        "form для html формы, get_json для json",
        "request.form читает html-форму, request.get_json читает json тело",
        "form из html формы, get_json из fetch с json",
        "form для application/x-www-form-urlencoded, get_json для application/json",
        "form для обычных форм, get_json для fetch/ajax",
        "request.form из POST-формы, get_json из fetch c content-type json",
        "form читает urlencoded, get_json читает json body",
        "form — для <form method=post>, get_json — для fetch json",
        "html form vs fetch json body",
      ],
      hint: "Посмотри на Content-Type запроса: что отправляет обычная HTML-форма и что отправляет fetch с JSON?",
      explanation:
        "request.form читает данные из классической HTML-формы (Content-Type: application/x-www-form-urlencoded или multipart/form-data). request.get_json() читает JSON-тело запроса (Content-Type: application/json), которое отправляет fetch() с JSON.stringify(). Если перепутать — получишь None или пустой объект.",
    },
    {
      type: "question",
      id: "h9-q3",
      title: "Роль e.preventDefault()",
      question: "Зачем вызывать e.preventDefault() в обработчике submit при AJAX-отправке формы?",
      answers: [
        "чтобы форма не перезагружала страницу",
        "остановить стандартную отправку формы",
        "предотвратить перезагрузку",
        "не перезагружать страницу",
        "отменить стандартное поведение браузера",
        "чтобы js обработал форму вместо браузера",
        "браузер не делает post запрос сам",
        "обработка через fetch вместо перезагрузки",
        "отмена дефолтного submit браузера",
        "prevent default form submission",
      ],
      hint: "Что браузер делает по умолчанию при отправке формы?",
      explanation:
        "По умолчанию при отправке формы браузер перезагружает страницу (делает обычный POST-запрос). e.preventDefault() отменяет это стандартное поведение, позволяя JavaScript обработать отправку самостоятельно: собрать данные, сделать fetch(), обновить только нужную часть DOM без перезагрузки.",
    },
    {
      type: "question",
      id: "h9-q4",
      title: "Jinja2: три типа тегов",
      question: "Какие три типа скобок использует Jinja2? Назови их назначение.",
      answers: [
        "{{ }} для вывода, {% %} для логики, {# #} для комментариев",
        "двойные скобки, процент скобки, решётка скобки",
        "{{ переменная }}, {% if/for/block %}, {# комментарий #}",
        "вывод значений, управляющие конструкции, комментарии",
        "{{ для вывода {% для кода {# для комментария",
        "{{ }} output, {% %} control, {# #} comment",
        "три типа: переменные, управление, комментарии",
      ],
      hint: "Один для вывода данных, один для if/for/block, один для скрытых комментариев",
      explanation:
        "Jinja2 использует три вида скобок: {{ переменная }} — вывести значение в HTML; {% if/for/block/extends %} — управляющие конструкции (условия, циклы, блоки шаблонов); {# комментарий #} — комментарии, которые не попадают в готовый HTML.",
    },
    {
      type: "question",
      id: "h9-q4",
      title: "Где Flask ищет шаблоны?",
      question: "В какой папке Flask ищет HTML-шаблоны по умолчанию?",
      answers: [
        "templates",
        "папка templates",
        "templates/",
        "/templates",
        "директория templates",
      ],
      hint: "Это жёстко заданная конвенция Flask, не настраивается без параметра template_folder.",
      explanation:
        "Flask по умолчанию ищет шаблоны в папке templates/ рядом с app.py. Если папка называется иначе (например, views/), нужно явно указать: Flask(__name__, template_folder='views'). То же с static/: можно переопределить через static_folder='assets'.",
    },
  ],
  writes: [
    {
      type: "write",
      id: "h9-w1",
      title: "Flask API + JS fetch: список задач",
      task:
        "Напиши полноценное Flask-приложение с JavaScript-фронтендом:\n\n" +
        "Серверная часть (Python):\n" +
        "1. Flask-маршрут GET /api/todos — возвращает JSON с массивом задач\n" +
        "2. Flask-маршрут POST /api/todos — принимает JSON {text: '...'}, добавляет задачу\n" +
        "3. Маршрут GET / — отдаёт HTML-страницу через render_template\n\n" +
        "Клиентская часть (JavaScript — напиши отдельно):\n" +
        "4. loadTodos() — fetch('/api/todos') и отображает в <ul>\n" +
        "5. addTodo(text) — fetch POST с JSON-телом и JSON.stringify\n" +
        "6. Обработчик кнопки «Добавить» вызывает addTodo() без перезагрузки страницы",
      hints: [
        "from flask import Flask, jsonify, request, render_template",
        "@app.route('/api/todos', methods=['GET', 'POST'])",
        "JS: await fetch('/api/todos', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({text}) })",
      ],
      required: ["flask", "jsonify", "request", "fetch", "JSON.stringify", "render_template"],
      minLines: 25,
      language: "flask-web" as const,
    },
    {
      type: "write",
      id: "h9-w2",
      title: "Jinja2: шаблон с циклом и наследованием",
      task:
        "Напиши два Jinja2-шаблона (HTML-код):\n\n" +
        "1. base.html:\n" +
        "   - DOCTYPE, html, head с charset и viewport\n" +
        "   - {% block title %} и {% block content %}\n" +
        "   - Подключение CSS через url_for('static', filename='css/style.css')\n\n" +
        "2. users.html:\n" +
        "   - {% extends 'base.html' %}\n" +
        "   - Заголовок H1 с количеством пользователей через {{ users|length }}\n" +
        "   - {% for user in users %} — вывести имя и возраст каждого\n" +
        "   - {% if user.age >= 18 %} — пометить взрослых значком",
      hints: [
        "{% block content %}...{% endblock %} — зона замены в дочернем шаблоне",
        "{{ users|length }} — фильтр Jinja2 для длины списка",
        "Не забудь {% endfor %} и {% endif %}",
      ],
      required: ["extends", "block", "url_for", "for", "length", "if"],
      minLines: 20,
      language: "web" as const,
    },
  ],
};

// ─────────────────────────────────────────────────────────────────────────────
// Exports
// ─────────────────────────────────────────────────────────────────────────────
export const HTML_ROUNDS: Round[] = [h1, h2, h3, h4, h5, h6, h7, h8, h9];
export const HTML_TOTAL_ROUNDS = HTML_ROUNDS.length;
